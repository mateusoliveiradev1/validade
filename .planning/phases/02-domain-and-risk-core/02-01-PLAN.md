---
phase: 02-domain-and-risk-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - eslint.config.mjs
  - packages/domain/package.json
  - packages/domain/tsconfig.json
  - packages/domain/src/index.ts
  - packages/domain/src/types.ts
  - packages/domain/src/profiles.ts
  - packages/domain/src/types.test.ts
  - packages/domain/src/profiles.test.ts
autonomous: true
requirements:
  - CAT-04
  - LOC-04
user_setup: []
must_haves:
  truths:
    - "D-01: Product modeling separates formal-validity items from FLV quality-inspection items through discriminated unions."
    - "D-02: Product modes include `formal_validity`, `flv_inspection`, and `receiving_monitored`."
    - "D-03: Category profiles provide defaults and product profiles can override them."
    - "D-04: Missing essential data is represented as blocking uncertainty instead of silent safety."
    - "D-08: Rule profiles are category-first with product override support."
    - "D-09: Default windows start as 60 / 15 / 3 / 0 days."
    - "D-16: Code identifiers use stable English technical names."
    - "CAT-04: Formal-validity and FLV items are distinguishable in the domain package."
  artifacts:
    - path: packages/domain/src/types.ts
      provides: "Stable domain vocabulary for product modes, profiles, lot inputs, states, commands, and reason codes."
    - path: packages/domain/src/profiles.ts
      provides: "Default rule profile and profile resolution helpers."
    - path: vitest.config.ts
      provides: "Root Vitest domain project wiring."
    - path: packages/domain/package.json
      provides: "Real domain package scripts for tests and typecheck."
  key_links:
    - from: vitest.config.ts
      to: packages/domain/src/types.test.ts
      via: "domain project includes domain unit tests."
    - from: packages/domain/src/profiles.ts
      to: packages/domain/src/types.ts
      via: "profile helpers return typed rule profiles."
    - from: eslint.config.mjs
      to: packages/domain/src/**/*.test.ts
      via: "typed lint can parse new domain tests."
---

<objective>
Wire the domain package into the monorepo test matrix and create the stable product-mode and rule-profile vocabulary that later risk logic will use.

Purpose: CAT-04 and D-01 through D-04 require the domain to distinguish formal validity, FLV quality inspection, and receiving-monitored items before risk state calculation can be reliable.
Output: Real domain test scripts, root Vitest project entry, discriminated TypeScript types, default profile constants, and tests proving the vocabulary is not an optional-field bag.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-domain-and-risk-core/02-CONTEXT.md
@.planning/phases/02-domain-and-risk-core/02-RESEARCH.md
@.planning/phases/02-domain-and-risk-core/02-VALIDATION.md
@.planning/phases/02-domain-and-risk-core/02-PATTERNS.md
@.planning/phases/01-engineering-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable real domain unit tests in the monorepo</name>
  <read_first>
    - @.planning/phases/02-domain-and-risk-core/02-CONTEXT.md
    - @.planning/phases/02-domain-and-risk-core/02-RESEARCH.md
    - @.planning/phases/02-domain-and-risk-core/02-PATTERNS.md
    - vitest.config.ts
    - packages/domain/package.json
    - packages/domain/tsconfig.json
    - packages/config/package.json
    - packages/config/tsconfig.json
    - eslint.config.mjs
  </read_first>
  <files>vitest.config.ts, eslint.config.mjs, packages/domain/package.json, packages/domain/tsconfig.json</files>
  <action>Add a `domain` project to `vitest.config.ts` with `root: "packages/domain"`, `include: ["src/**/*.test.ts"]`, and `environment: "node"`. Replace the placeholder `packages/domain/package.json` `test` script with `cd ../.. && vitest run --config vitest.config.ts --project domain`, keep `typecheck` as `tsc -p tsconfig.json --noEmit`, and keep `test:smoke` as `pnpm typecheck`. Add `vitest` as a domain devDependency matching the existing workspace style. Update `packages/domain/tsconfig.json` to exclude `src/**/*.test.ts` from build output, following `packages/config/tsconfig.json`. Add exact new test paths `packages/domain/src/types.test.ts` and `packages/domain/src/profiles.test.ts` to `eslint.config.mjs` `parserOptions.projectService.allowDefaultProject`; raise `maximumDefaultProjectFileMatchCount_THIS_WILL_SLOW_DOWN_LINTING` only if the exact path count would exceed the existing limit.</action>
  <verify>
    <automated>pnpm --filter @validade-zero/domain test</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains a test project with `name: "domain"` and `root: "packages/domain"`.
    - `packages/domain/package.json` `scripts.test` contains `--project domain`.
    - `packages/domain/tsconfig.json` excludes `src/**/*.test.ts`.
    - `eslint.config.mjs` allowDefaultProject includes `packages/domain/src/types.test.ts` and `packages/domain/src/profiles.test.ts`.
    - `pnpm --filter @validade-zero/domain test` exits 0 after Task 2 creates the tests.
  </acceptance_criteria>
  <done>The domain package has a real test command and participates in the root Vitest matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Define product modes, rule profiles, and missing-data vocabulary with tests first</name>
  <read_first>
    - @.planning/phases/02-domain-and-risk-core/02-CONTEXT.md
    - @.planning/phases/02-domain-and-risk-core/02-RESEARCH.md
    - @.planning/phases/02-domain-and-risk-core/02-PATTERNS.md
    - packages/domain/src/index.ts
    - packages/test-utils/src/index.ts
    - packages/test-utils/src/fixtures.ts
  </read_first>
  <files>packages/domain/src/index.ts, packages/domain/src/types.ts, packages/domain/src/profiles.ts, packages/domain/src/types.test.ts, packages/domain/src/profiles.test.ts</files>
  <action>First create Vitest tests in `types.test.ts` and `profiles.test.ts` for the locked vocabulary. Then implement `types.ts` with exact exported unions/types for `ProductMode = "formal_validity" | "flv_inspection" | "receiving_monitored"`, `RiskState = "safe" | "radar" | "markdown_due" | "critical" | "expired" | "uncertain"`, `OperationalCommand = "check_presence" | "request_markdown" | "withdraw_now" | "monitor" | "correct_data"`, and structured `RiskReasonCode` values including `missing_required_date`, `missing_received_date`, `missing_quality_window`, `presence_stale`, `expired`, `expires_in_60_days`, `expires_in_15_days`, and `expires_in_3_days`. Define discriminated category/product shapes so `formal_validity` requires an expiry-capable lot input, `flv_inspection` uses quality-window inputs, and `receiving_monitored` uses received-date monitoring. Implement `profiles.ts` with `DEFAULT_RISK_WINDOWS` values `{ radarDays: 60, markdownDays: 15, criticalDays: 3, expiredDays: 0 }`, a default profile object, and a `resolveRuleProfile(categoryProfile, productOverride)` helper where product override values replace category defaults without mutating inputs. Update `index.ts` to re-export the new public types and helpers while preserving an explicit boundary marker or equivalent exported note.</action>
  <verify>
    <automated>pnpm --filter @validade-zero/domain test && pnpm --filter @validade-zero/domain typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/src/types.ts` exports `ProductMode`, `RiskState`, `OperationalCommand`, and `RiskReasonCode`.
    - `ProductMode` contains exactly `formal_validity`, `flv_inspection`, and `receiving_monitored`.
    - `DEFAULT_RISK_WINDOWS` contains `radarDays: 60`, `markdownDays: 15`, `criticalDays: 3`, and `expiredDays: 0`.
    - A test proves a `formal_validity` product and a `flv_inspection` product cannot be represented by the same ambiguous optional-date object.
    - A test proves `resolveRuleProfile` applies product override values without mutating the category profile input.
  </acceptance_criteria>
  <done>The domain package has tested vocabulary for product modes, profiles, states, commands, and structured reasons.</done>
</task>

</tasks>

<traceability>
- D-01: Discriminated product modes prevent one ambiguous model with optional dates.
- D-02: `formal_validity`, `flv_inspection`, and `receiving_monitored` are first-class domain modes.
- D-03: `resolveRuleProfile` applies product overrides over category defaults.
- D-04: Missing-data reason codes exist before risk logic uses them.
- D-08: Rule profiles are category-first with product override support.
- D-09: Default windows are 60 / 15 / 3 / 0 days.
- D-16: Public identifiers use English technical names.
- CAT-04: Formal-validity and FLV items are distinguishable by type and tests.
</traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| domain package -> future apps | Future UI/API code will trust these exported domain types. |
| test fixtures -> public repo | Domain tests can accidentally introduce real product/store examples. |
| profile input -> risk engine | Mutating shared profile inputs could corrupt later calculations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `RuleProfile` resolution | mitigate | Test that product overrides do not mutate category default inputs. |
| T-02-02 | Information Disclosure | domain tests | mitigate | Use only fictitious fixture names from `packages/test-utils` or clearly fake inline values. |
| T-02-03 | Elevation of Privilege | domain imports | mitigate | Keep domain pure and run `pnpm lint` boundary checks in later plans. |
</threat_model>

<verification>
- `pnpm --filter @validade-zero/domain test`
- `pnpm --filter @validade-zero/domain typecheck`
- `pnpm test -- --project domain`
</verification>

<success_criteria>
- CAT-04 is represented by executable type and unit-test coverage.
- Domain tests run from both package and root Vitest surfaces.
- Default risk profile and product override helpers exist before risk calculation plans.
- No UI, database, provider, adapter, or app imports are added to `packages/domain`.
</success_criteria>

<output>
Create `.planning/phases/02-domain-and-risk-core/02-01-SUMMARY.md` when done.
</output>
