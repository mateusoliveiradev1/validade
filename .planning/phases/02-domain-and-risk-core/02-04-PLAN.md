---
phase: 02-domain-and-risk-core
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - eslint.config.mjs
  - packages/domain/src/risk.scenarios.test.ts
  - docs/testing/strategy.md
autonomous: true
requirements:
  - CAT-04
  - LOC-04
  - RSK-01
  - RSK-02
user_setup: []
must_haves:
  truths:
    - "D-01 through D-18: Critical domain decisions are covered by a scenario matrix or explicit verification note."
    - "CAT-04: Scenario coverage proves formal-validity, FLV inspection, and receiving-monitored paths stay distinct."
    - "LOC-04: Scenario coverage proves unverified risky lots remain uncertain."
    - "RSK-01: Scenario coverage proves state calculation uses rules, current date, due dates/windows, and physical confirmation."
    - "RSK-02: Scenario coverage proves configurable 60/15/3/0 windows."
    - "Critical domain rules are mutation-ready through Stryker's existing `packages/domain/src/**/*.ts` target."
    - "Domain logic has no dependency on UI, database, provider SDKs, apps, or adapters."
  artifacts:
    - path: packages/domain/src/risk.scenarios.test.ts
      provides: "End-to-end domain scenario matrix for Phase 2 decisions and requirements."
    - path: docs/testing/strategy.md
      provides: "Updated testing notes for Phase 2 domain mutation and scenario coverage."
  key_links:
    - from: packages/domain/src/risk.scenarios.test.ts
      to: .planning/phases/02-domain-and-risk-core/02-CONTEXT.md
      via: "scenario table cites Phase 2 decisions in test titles or comments."
    - from: stryker.config.json
      to: packages/domain/src/**/*.ts
      via: "existing mutation target covers domain source files."
    - from: scripts/check-boundaries.mjs
      to: packages/domain/src/**/*.ts
      via: "lint command enforces pure domain imports."
---

<objective>
Harden the completed domain rule engine with a scenario matrix, mutation-readiness verification, and updated testing documentation.

Purpose: Phase 2 success requires critical domain rules to have TDD coverage and mutation-ready structure, and it must be obvious to future phases that the domain is pure and executable.
Output: Table-driven scenario tests covering requirements and decisions, testing docs update, and final verification through test, mutation, lint, and full check commands.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-domain-and-risk-core/02-CONTEXT.md
@.planning/phases/02-domain-and-risk-core/02-RESEARCH.md
@.planning/phases/02-domain-and-risk-core/02-VALIDATION.md
@.planning/phases/02-domain-and-risk-core/02-PATTERNS.md
@.planning/phases/02-domain-and-risk-core/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Phase 2 decision and requirement scenario matrix</name>
  <read_first>
    - @.planning/phases/02-domain-and-risk-core/02-CONTEXT.md
    - @.planning/phases/02-domain-and-risk-core/02-RESEARCH.md
    - @.planning/phases/02-domain-and-risk-core/02-VALIDATION.md
    - packages/domain/src/types.ts
    - packages/domain/src/profiles.ts
    - packages/domain/src/presence.ts
    - packages/domain/src/risk.ts
    - packages/domain/src/risk.test.ts
    - packages/domain/src/presence.test.ts
    - packages/domain/src/commands.test.ts
    - eslint.config.mjs
  </read_first>
  <files>eslint.config.mjs, packages/domain/src/risk.scenarios.test.ts</files>
  <action>Add `packages/domain/src/risk.scenarios.test.ts` to `eslint.config.mjs` `allowDefaultProject`. Create a table-driven Vitest scenario matrix that covers at minimum: formal-validity safe/radar/markdown/critical/expired; FLV quality-window critical/expired; receiving-monitored missing received date as `uncertain`; missing formal date as `uncertain`; stale physical presence as `uncertain`; fresh physical presence preserving concrete risk; product override changing a markdown window; `not_found` conditional resolution; `probably_sold_out` conditional resolution; and no scenario relying on UI copy for assertions. Include requirement or decision references in test names or comments, such as `CAT-04`, `LOC-04`, `RSK-01`, `RSK-02`, `D-07`, and `D-13`.</action>
  <verify>
    <automated>pnpm --filter @validade-zero/domain test -- scenarios && pnpm --filter @validade-zero/domain test</automated>
  </verify>
  <acceptance_criteria>
    - `risk.scenarios.test.ts` includes scenario labels for `CAT-04`, `LOC-04`, `RSK-01`, and `RSK-02`.
    - The scenario matrix includes formal-validity, FLV, and receiving-monitored paths.
    - The scenario matrix includes stale presence, missing date, product override, `not_found`, and `probably_sold_out`.
    - Assertions check structured `state`, `command`, and reason/conditional markers, not Portuguese UI copy.
  </acceptance_criteria>
  <done>Phase 2 behavior is covered by an end-to-end domain scenario matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Update testing docs for Phase 2 domain rules and mutation</name>
  <read_first>
    - @.planning/phases/02-domain-and-risk-core/02-CONTEXT.md
    - @.planning/phases/02-domain-and-risk-core/02-RESEARCH.md
    - @.planning/phases/02-domain-and-risk-core/02-VALIDATION.md
    - docs/testing/strategy.md
    - stryker.config.json
    - package.json
  </read_first>
  <files>docs/testing/strategy.md</files>
  <action>Update `docs/testing/strategy.md` to add a Phase 2 section explaining that domain rules now have real Vitest coverage, `pnpm --filter @validade-zero/domain test` is the fast domain command, and `pnpm test:mutation` uses Stryker against `packages/domain/src/**/*.ts` for mutation readiness. Keep the docs honest: do not claim high mutation thresholds unless they are configured. State that mutation results should be reviewed for critical rule branches before declaring Phase 2 verified.</action>
  <verify>
    <automated>Select-String -Path docs/testing/strategy.md -Pattern 'Phase 2','@validade-zero/domain','pnpm test:mutation','packages/domain/src'</automated>
  </verify>
  <acceptance_criteria>
    - `docs/testing/strategy.md` contains `Phase 2`.
    - `docs/testing/strategy.md` contains `pnpm --filter @validade-zero/domain test`.
    - `docs/testing/strategy.md` contains `pnpm test:mutation`.
    - `docs/testing/strategy.md` contains `packages/domain/src`.
  </acceptance_criteria>
  <done>Testing docs describe the Phase 2 domain and mutation verification surface accurately.</done>
</task>

<task type="auto">
  <name>Task 3: Run final domain, mutation, boundary, and full-suite verification</name>
  <read_first>
    - @.planning/phases/02-domain-and-risk-core/02-VALIDATION.md
    - package.json
    - vitest.config.ts
    - stryker.config.json
    - eslint.config.mjs
    - scripts/check-boundaries.mjs
  </read_first>
  <files>packages/domain/src/risk.scenarios.test.ts, docs/testing/strategy.md</files>
  <action>Run the final verification sequence for Phase 2. Execute `pnpm --filter @validade-zero/domain test`, `pnpm --filter @validade-zero/domain typecheck`, `pnpm test -- --project domain`, `pnpm lint`, `pnpm test:mutation`, and `pnpm check`. If `pnpm test:mutation` is too slow or fails due tooling/runtime rather than surviving domain mutants, record the exact failure and command output in the summary; do not silently mark mutation as passed. If `pnpm check` fails because of unrelated pre-existing repo state, record the exact failing command and keep Phase 2 domain-specific commands visible.</action>
  <verify>
    <automated>pnpm --filter @validade-zero/domain test && pnpm --filter @validade-zero/domain typecheck && pnpm lint && pnpm test:mutation && pnpm check</automated>
  </verify>
  <acceptance_criteria>
    - Summary records the result of `pnpm --filter @validade-zero/domain test`.
    - Summary records the result of `pnpm --filter @validade-zero/domain typecheck`.
    - Summary records the result of `pnpm lint`.
    - Summary records the result of `pnpm test:mutation`, including exact blocker if it cannot complete.
    - Summary records the result of `pnpm check`, including exact blocker if it cannot complete.
  </acceptance_criteria>
  <done>Phase 2 has final verification evidence for domain tests, boundaries, mutation readiness, and full suite status.</done>
</task>

</tasks>

<traceability>
- D-01-D-18: Scenario tests and docs make the full decision set visible to future phases.
- CAT-04: Scenarios cover formal-validity, FLV, and receiving-monitored distinctions.
- LOC-04: Scenarios cover stale/missing physical confirmation as uncertainty.
- RSK-01: Scenarios cover risk state calculation from rules, dates, and physical confirmation.
- RSK-02: Scenarios cover configurable 60/15/3/0 windows and product overrides.
</traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> phase verification | Weak tests can make broken risk rules look complete. |
| mutation output -> readiness claim | Mutation tooling can be skipped or fail without being documented. |
| domain imports -> shared architecture | A late dependency can couple domain to UI/provider layers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | Domain scenario coverage | mitigate | Add table-driven scenarios for each critical requirement and decision cluster. |
| T-02-11 | Repudiation | Verification evidence | mitigate | Summary must record exact command results, especially mutation and full-suite outcomes. |
| T-02-12 | Elevation of Privilege | Dependency boundary | mitigate | Final `pnpm lint` runs `scripts/check-boundaries.mjs` against domain imports. |
</threat_model>

<verification>
- `pnpm --filter @validade-zero/domain test -- scenarios`
- `pnpm --filter @validade-zero/domain test`
- `pnpm --filter @validade-zero/domain typecheck`
- `pnpm test -- --project domain`
- `pnpm lint`
- `pnpm test:mutation`
- `pnpm check`
</verification>

<success_criteria>
- All four Phase 2 requirements are represented in scenario tests.
- Critical domain branches are mutation-ready and mutation command result is documented.
- Testing docs describe domain-specific and full-suite verification commands.
- Final boundary verification confirms domain remains pure.
</success_criteria>

<output>
Create `.planning/phases/02-domain-and-risk-core/02-04-SUMMARY.md` when done.
</output>
