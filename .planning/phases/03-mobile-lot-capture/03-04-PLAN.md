---
phase: 03-mobile-lot-capture
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - apps/mobile/app.json
  - apps/mobile/package.json
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/BarcodeLookupAssistant.tsx
  - apps/mobile/src/capture/capture-copy.ts
  - apps/mobile/src/capture/camera-fallback.test.tsx
  - apps/mobile/src/capture/mobile-capture.accessibility.test.tsx
  - .maestro/smoke.yaml
  - eslint.config.mjs
  - pnpm-lock.yaml
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - LOC-01
  - LOC-02
  - LOC-03
user_setup: []
must_haves:
  truths:
    - "D-01: Camera or barcode assists product lookup only; the collaborator always confirms the product before entering lot data."
    - "D-06: Manual name/code lookup remains available when camera permission is denied, unavailable, or scanning fails."
    - "D-09: Guided date and operational-window feedback stays readable and accessible on a phone-size flow."
    - "D-14: The post-registration operational list remains the initial focused surface rather than a metrics dashboard."
    - "CAT-03: Camera/barcode assists lookup without becoming mandatory for product, lot, validity, or quantity confirmation."
    - "CAT-01: Product registration stays mobile-first and accessible after hardware assistance is added."
    - "CAT-02: Lot registration remains usable in the manual path after camera denial or scan failure."
    - "LOC-01: Location movement stays usable through the verified mobile critical journey."
    - "LOC-02: Physical outcomes stay usable through the verified mobile critical journey."
    - "LOC-03: Recent and detail views preserve last-seen facts through the verified mobile critical journey."
  artifacts:
    - path: apps/mobile/src/capture/BarcodeLookupAssistant.tsx
      provides: "Explicit camera permission and barcode-assistance UI that emits lookup text only."
    - path: apps/mobile/src/capture/camera-fallback.test.tsx
      provides: "Mocked permission-denied, unavailable, and scan-result contract coverage."
    - path: apps/mobile/src/capture/mobile-capture.accessibility.test.tsx
      provides: "Accessibility and direct-operational-copy checks for the completed capture journey."
    - path: .maestro/smoke.yaml
      provides: "Critical mobile path assertions replacing the old Phase 1 smoke strings."
  key_links:
    - from: apps/mobile/src/capture/BarcodeLookupAssistant.tsx
      to: apps/mobile/src/capture/ProductDiscoveryScreen.tsx
      via: "A scan result fills the existing lookup path and returns to mandatory product confirmation."
    - from: apps/mobile/app.json
      to: apps/mobile/src/capture/BarcodeLookupAssistant.tsx
      via: "Expo camera permission configuration explains the optional scanner use."
    - from: .maestro/smoke.yaml
      to: apps/mobile/src/capture/CaptureApp.tsx
      via: "E2E smoke asserts current Portuguese operational labels instead of the retired foundation screen."
---

<objective>
Add optional barcode assistance and complete the mobile accessibility, native-smoke, and regression evidence for the Phase 3 capture journey.

Purpose: Scanner hardware can speed lookup, but it must never bypass human confirmation or break the manual corridor workflow when permission, hardware, or scanning fails.
Output: Expo Camera integration behind an explicit action, tested fallback behavior, accessibility hardening against the approved UI contract, updated Maestro smoke, and documented automated/native verification evidence.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-mobile-lot-capture/03-CONTEXT.md
@.planning/phases/03-mobile-lot-capture/03-RESEARCH.md
@.planning/phases/03-mobile-lot-capture/03-VALIDATION.md
@.planning/phases/03-mobile-lot-capture/03-UI-SPEC.md
@.planning/phases/03-mobile-lot-capture/03-03-SUMMARY.md
@apps/mobile/app.json
@apps/mobile/package.json
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/ProductDiscoveryScreen.tsx
@.maestro/smoke.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional barcode lookup with safe permission and fallback behavior</name>
  <read_first>
    - @.planning/phases/03-mobile-lot-capture/03-CONTEXT.md
    - @.planning/phases/03-mobile-lot-capture/03-RESEARCH.md
    - @.planning/phases/03-mobile-lot-capture/03-UI-SPEC.md
    - apps/mobile/app.json
    - apps/mobile/package.json
    - apps/mobile/src/capture/CaptureApp.tsx
    - apps/mobile/src/capture/ProductDiscoveryScreen.tsx
    - apps/mobile/src/capture/capture-copy.ts
  </read_first>
  <files>apps/mobile/app.json, apps/mobile/package.json, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/BarcodeLookupAssistant.tsx, apps/mobile/src/capture/capture-copy.ts, apps/mobile/src/capture/camera-fallback.test.tsx, eslint.config.mjs, pnpm-lock.yaml</files>
  <action>Install `expo-camera` through `pnpm.cmd --filter @validade-zero/mobile exec expo install expo-camera --pnpm`; do not manually pin its version. Configure only the SDK-supported Expo camera plugin/permission text in `apps/mobile/app.json`, explaining in Portuguese-BR that camera use helps locate a product and that manual search continues to work. Add a secondary `Ler código` action in product discovery that opens `BarcodeLookupAssistant` only after explicit request. Handle permission pending, granted, denied, unavailable, and scan-error states. On a successful barcode result, close the camera and send the raw text only to the existing manual lookup field; then require the existing product confirmation card before any lot form can open. On denied, unavailable, or error, show `Não foi possível usar a câmera. Você pode buscar o produto por nome ou código.` and the visible fallback `Buscar manualmente`, with no blocked registration path. Mock the camera boundary in component tests and prove each fallback path, successful lookup handoff, and no auto-created product or lot. Add the exact test path to ESLint allowDefaultProject.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- camera-fallback && pnpm.cmd --filter @validade-zero/mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `expo-camera` is installed through Expo's resolver and lockfile metadata matches the workspace SDK.
    - `Ler código` is a secondary explicit action; product discovery and product registration work without opening it.
    - A scanner result only populates the lookup input and still renders the product confirmation step before lot registration.
    - Permission denied, camera unavailable, and scan error each show manual fallback copy and leave `Buscar manualmente` usable.
    - `pnpm.cmd --filter @validade-zero/mobile test -- camera-fallback` and `pnpm.cmd --filter @validade-zero/mobile typecheck` exit 0.
  </acceptance_criteria>
  <done>Barcode hardware assists product lookup without becoming a registration authority or a required dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the finished journey for accessibility, Maestro smoke, and full quality gates</name>
  <read_first>
    - @.planning/phases/03-mobile-lot-capture/03-UI-SPEC.md
    - @.planning/phases/03-mobile-lot-capture/03-VALIDATION.md
    - apps/mobile/src/capture/CaptureApp.tsx
    - apps/mobile/src/capture/capture-ui.tsx
    - apps/mobile/src/capture/RecentLotList.tsx
    - apps/mobile/src/capture/LotRegistrationScreen.tsx
    - apps/mobile/src/capture/ObservationComposer.tsx
    - apps/mobile/src/capture/BarcodeLookupAssistant.tsx
    - .maestro/smoke.yaml
    - package.json
  </read_first>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/capture-copy.ts, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/mobile-capture.accessibility.test.tsx, .maestro/smoke.yaml, eslint.config.mjs</files>
  <action>Audit the completed capture screens against `03-UI-SPEC.md` and add only the missing concrete behavior: accessible labels and logical screen-reader order for every visible action and field; at least 48dp targets; visible required state and inline errors announced with a polite live region where React Native supports it; non-color-only attention labels; wrapping for long product, lot, location, and confirmation text; and no icon-only primary action. Keep primary actions exactly `Registrar lote`, `Confirmar produto`, and `Registrar observação`; keep generic primary labels out of the flow. Add component coverage for these labels, accessible action names, required-field error visibility, long-text wrapping style, and manual fallback. Replace the Phase 1 strings in `.maestro/smoke.yaml` with stable assertions for the Phase 3 initial empty/list screen and `Registrar lote`, then add the smallest practical critical journey through manual product confirmation, formal-validity lot registration, movement/observation, and return to recent list. Run `pnpm.cmd test:e2e:mobile` when a configured emulator/device is available; otherwise record the exact environmental blocker and preserve the Maestro file plus component coverage. Run `pnpm.cmd --filter @validade-zero/mobile test`, `pnpm.cmd --filter @validade-zero/mobile typecheck`, `pnpm.cmd lint`, `pnpm.cmd test`, and `pnpm.cmd check`; record exact failures if native tooling or an unrelated pre-existing condition blocks a command rather than claiming a pass.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test && pnpm.cmd --filter @validade-zero/mobile typecheck && pnpm.cmd lint && pnpm.cmd test && pnpm.cmd check</automated>
  </verify>
  <acceptance_criteria>
    - Completed capture controls have accessible names, visible text labels, 48dp-or-larger primary targets, and inline required-field feedback.
    - Long product/lot/location/confirmation copy uses wrapping rather than clipping, and attention states include text rather than color alone.
    - `.maestro/smoke.yaml` contains Phase 3 operational labels and no longer asserts the retired foundation-only smoke copy.
    - Summary records the result of the mobile test/typecheck/lint/root test/root check sequence and either the Maestro result or the exact native-runtime blocker.
    - `pnpm.cmd --filter @validade-zero/mobile test`, `pnpm.cmd --filter @validade-zero/mobile typecheck`, `pnpm.cmd lint`, `pnpm.cmd test`, and `pnpm.cmd check` exit 0 unless an exact documented external blocker prevents native E2E execution.
  </acceptance_criteria>
  <done>The finished Phase 3 journey is camera-optional, accessible, operationally readable, and covered by automated plus native-smoke evidence.</done>
</task>

</tasks>

<traceability>
- D-01: Scanner output is lookup input, never automatic registration.
- D-06: Manual search is fully available after denied/unavailable/failed camera paths.
- D-09: The completed phone flow keeps guided dates and operational feedback legible.
- D-14: Maestro and component checks protect the focused recent-list entry surface.
- CAT-03: Barcode helps product discovery while all critical facts remain human-confirmed.
- CAT-01/CAT-02: Product and lot flows remain accessible under camera fallback.
- LOC-01/LOC-02/LOC-03: Critical observed-location journey is represented in final smoke coverage.
</traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera permission and barcode text -> lookup | Native hardware can fail and barcode values are untrusted lookup text. |
| visible status -> floor decision | Color, clipped labels, or inaccessible errors can conceal a critical operational condition. |
| automated evidence -> release confidence | Native E2E availability may differ from component/static tests and must be reported honestly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Barcode lookup handoff | mitigate | Validate scan output through the existing manual lookup path and require product confirmation. |
| T-03-08 | Denial of Service | Camera permission/hardware | mitigate | Preserve a fully usable manual fallback and test denied/unavailable/error states. |
| T-03-09 | Repudiation | Verification evidence | mitigate | Record exact automated and native E2E command results or environmental blockers in the plan summary. |
</threat_model>

## Artifacts this phase produces

- `BarcodeLookupAssistant` and Expo Camera permission configuration for optional lookup assistance.
- Camera fallback and accessibility component tests.
- Updated `.maestro/smoke.yaml` assertions for the mobile capture journey.

<verification>
- `pnpm.cmd --filter @validade-zero/mobile test -- camera-fallback`
- `pnpm.cmd --filter @validade-zero/mobile test`
- `pnpm.cmd --filter @validade-zero/mobile typecheck`
- `pnpm.cmd lint`
- `pnpm.cmd test`
- `pnpm.cmd test:e2e:mobile` when native runtime is available
- `pnpm.cmd check`
</verification>

<success_criteria>
- Camera/barcode is optional, explicitly requested, and cannot bypass product confirmation or manual lot facts.
- The complete mobile capture journey honors the approved Portuguese-BR copy, accessibility, focus, target-size, contrast, and responsive contracts.
- Automated component/static checks cover the phase requirements, and native E2E status is documented honestly.
- No task workflow, push/escalation, cloud sync/outbox, evidence upload, roles, full audit surface, or analytics panel is added.
</success_criteria>

<output>
Create `.planning/phases/03-mobile-lot-capture/03-04-SUMMARY.md` when done.
</output>
