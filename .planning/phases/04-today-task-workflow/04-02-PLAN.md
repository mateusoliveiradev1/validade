---
phase: 04-today-task-workflow
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/TodayScreen.tsx
  - apps/mobile/src/capture/today-copy.ts
  - apps/mobile/src/capture/capture-copy.ts
  - apps/mobile/src/capture/capture-ui.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - apps/mobile/src/capture/today-task-fixtures.test.ts
  - eslint.config.mjs
autonomous: true
requirements:
  - PSH-03
  - UI-01
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - "D-14: The Hoje header answers first whether Area de venda is safe or has current risks."
    - "D-15: Safe sales area and pending shift work are separate states in the header."
    - "D-16: Critical sales-area risk uses dominant inline visual treatment and first-task emphasis without default modal blocking."
    - "D-17: Sections are Retirar agora, Conferir na area de venda, Pedir rebaixa, Acompanhar, then Atencao futura."
    - "D-18: Each row shows action, product/lot, location, due or severity, owner, and one short reason."
    - "D-19: Copy is direct, operational, and non-blaming, with labels like Retirar agora, Conferir presenca, Pedir rebaixa, and Rever em 2h."
    - "D-20: radar appears only at the end as Atencao futura."
    - "D-21: Empty state says Area de venda segura agora and offers Registrar lote and Conferir lotes recentes."
    - "PSH-03: Collaborator can open a mobile Hoje view with current shift work."
    - "UI-01: First screen answers Area de venda segura? before passive charts or recent lists."
    - "UI-02: Visible copy uses direct operational Portuguese-BR task labels."
    - "UI-03: Layout preserves high contrast, 48dp touch targets, one-column mobile flow, and clear critical states."
  artifacts:
    - path: apps/mobile/src/capture/TodayScreen.tsx
      provides: "Safety header, task sections, refresh, task rows, future attention, and empty state."
    - path: apps/mobile/src/capture/today-copy.ts
      provides: "Centralized Portuguese-BR labels and errors for the Hoje workflow."
    - path: apps/mobile/src/capture/CaptureApp.tsx
      provides: "Hoje-first navigation while keeping registration, recent lots, detail, and observations reachable."
  key_links:
    - from: apps/mobile/src/capture/TodayScreen.tsx
      to: apps/mobile/src/capture/repository.ts
      via: "The screen refreshes and lists tasks through the local task repository port."
    - from: apps/mobile/src/capture/TodayScreen.tsx
      to: apps/mobile/src/capture/capture-ui.tsx
      via: "Today rows and actions reuse native primitives and approved Phase 4 tokens."
    - from: apps/mobile/src/capture/CaptureApp.tsx
      to: apps/mobile/src/capture/LotDetailScreen.tsx
      via: "Opening a task can navigate to lot detail without replacing Phase 3 capture flows."
---

<objective>
Deliver the mobile Hoje entry surface that answers sales-area safety and lists the current shift's actionable lot tasks.

Purpose: A collaborator should open the app and immediately know whether the sales area is safe, what to do first, and how to reach the existing registration/detail flows.
Output: Hoje-first mobile navigation, safety header, task sections, task rows, future attention, refresh/empty states, and component tests using local task fixtures.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-today-task-workflow/04-CONTEXT.md
@.planning/phases/04-today-task-workflow/04-RESEARCH.md
@.planning/phases/04-today-task-workflow/04-VALIDATION.md
@.planning/phases/04-today-task-workflow/04-UI-SPEC.md
@.planning/phases/04-today-task-workflow/04-PATTERNS.md
@.planning/phases/04-today-task-workflow/04-01-SUMMARY.md
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/RecentLotList.tsx
@apps/mobile/src/capture/LotDetailScreen.tsx
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/capture/capture-copy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Hoje the first operational screen with a stable safety header</name>
  <read_first>
    - @.planning/phases/04-today-task-workflow/04-UI-SPEC.md
    - @.planning/phases/04-today-task-workflow/04-PATTERNS.md
    - apps/mobile/src/capture/CaptureApp.tsx
    - apps/mobile/src/capture/repository.ts
    - apps/mobile/src/capture/capture-ui.tsx
    - apps/mobile/src/capture/capture-copy.ts
    - apps/mobile/src/App.test.tsx
  </read_first>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/capture-copy.ts, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/App.test.tsx, eslint.config.mjs</files>
  <action>Create `TodayScreen` and route `CaptureApp` to it immediately after repository initialization. Keep `Registrar lote`, `Conferir lotes recentes`, lot detail, and observation paths reachable; do not remove ProductDiscoveryScreen, RecentLotList, LotDetailScreen, or ObservationComposer. Add `today-copy.ts` or extend `capture-copy.ts` with exact visible labels from `04-UI-SPEC.md`: `Hoje`, `Area de venda segura`, `Area de venda segura, ainda ha tarefas do turno`, `Area de venda com {n} risco(s) agora`, `Atualizar tarefas`, `Area de venda segura agora`, `Registrar lote`, and `Conferir lotes recentes`. The header must render one of the three safety verdicts before any list content and keep its height stable during loading/refresh. Use existing colors from the UI-SPEC: background `#F5F7EF`, task surface `#E6EEE4`, ink `#112016`, muted ink `#3F5546`, accent `#166534`, critical `#B42318`, critical surface `#FCE8E6`, warning surface `#FFF4D7`. Add a secondary refresh action that calls repository refresh and keeps prior tasks visible if refresh fails. Component tests must prove the initial app smoke now includes `Hoje`, that safe-empty state offers both registration and recent-lot paths, and that refresh failure shows `Nao foi possivel atualizar agora. Confira a conexao e tente novamente.` without blanking the previous task list.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- today-screen && pnpm.cmd --filter @validade-zero/mobile test -- App && pnpm.cmd --filter @validade-zero/mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The initial mobile render contains `Hoje` and a sales-area safety verdict before registration or recent-list content.
    - Safe empty state contains `Area de venda segura agora`, `Registrar lote`, and `Conferir lotes recentes`.
    - Refresh failure keeps the previous list visible and shows the specified recovery copy.
    - `CaptureApp` still routes to product discovery, recent lots, lot detail, and observation paths.
    - `pnpm.cmd --filter @validade-zero/mobile test -- today-screen`, mobile App test, and mobile typecheck exit 0.
  </acceptance_criteria>
  <done>The app opens to Hoje and answers sales-area safety without sacrificing existing capture navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Render operational task sections, rows, future attention, and task opening</name>
  <read_first>
    - @.planning/phases/04-today-task-workflow/04-UI-SPEC.md
    - packages/domain/src/tasks.ts
    - packages/contracts/src/tasks.ts
    - apps/mobile/src/capture/TodayScreen.tsx
    - apps/mobile/src/capture/repository.ts
    - apps/mobile/src/capture/capture-ui.tsx
    - apps/mobile/src/capture/LotDetailScreen.tsx
  </read_first>
  <files>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/today-task-fixtures.test.ts, eslint.config.mjs</files>
  <action>Render task sections in this exact order when matching tasks exist: `Retirar agora`, `Conferir na area de venda`, `Pedir rebaixa`, `Acompanhar`, then future `Atencao futura`. Each task row must be a full-width touch target of at least 48dp and show, in visible text, the compatible action first, product display name, lot identity, current location, due/severity copy (`Agora`, `Ainda no turno`, `Hoje`, `Rever em {janela}`, or `Atrasada`), owner label, and one short reason derived from source risk reason codes. Keep per-lot visibility by rendering one row per task id even when product/location repeat. If a sales-area expired or critical task exists, render the critical surface inline, place it first, and use CTA copy `Retirar agora` or `Conferir agora`; do not use a modal. Future radar attention appears only under `Atencao futura`, visually quieter than active tasks, and cannot open a resolution panel. Tapping an active task stores selected task state and opens the Phase 4 resolution path placeholder or the related lot detail if the resolution panel is not implemented until Plan 03. Add tests for section order, row anatomy, per-lot duplicate product visibility, critical-first treatment, radar future attention, no color-only risk labels, and open-task navigation callback.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- today-screen && pnpm.cmd --filter @validade-zero/mobile test -- today-task-fixtures && pnpm.cmd lint</automated>
  </verify>
  <acceptance_criteria>
    - Active task sections render in the exact operational order and omit empty sections.
    - Every row includes visible action, product, lot, location, due/severity, owner, and reason text.
    - Two tasks for the same product but different lot ids render as two visible rows.
    - Radar appears only under `Atencao futura` and is not actionable as a shift task.
    - `pnpm.cmd --filter @validade-zero/mobile test -- today-screen`, fixture tests, and `pnpm.cmd lint` exit 0.
  </acceptance_criteria>
  <done>The Hoje screen lists actionable shelf work in the approved operational hierarchy.</done>
</task>

</tasks>

<traceability>
- D-14: Safety verdict leads the screen.
- D-15: Safe-with-work state separates safety from pending shift work.
- D-16: Critical sales-area risk is dominant inline and first.
- D-17: Sections match the approved operational order.
- D-18: Task rows expose action, product/lot, location, due/severity, owner, and reason.
- D-19: Copy uses direct Portuguese-BR action labels.
- D-20: Radar is future attention only.
- D-21: Empty state confirms safety and routes to registration/recent lots.
- PSH-03/UI-01/UI-02/UI-03: Hoje is mobile-first, direct, high-contrast, and task-oriented.
</traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local task list -> Hoje header | A wrong aggregate could claim the sales area is safe while blocking work exists. |
| task row presentation -> shelf action | Missing action/lot/location text could send the collaborator to the wrong item. |
| refresh failure -> user confidence | Blank refresh failure could hide active risk or imply tasks disappeared. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information disclosure | Hoje row content | mitigate | Use only local fictitious/dev data in tests and do not introduce remote data fetches. |
| T-04-05 | Tampering | Safety header aggregate | mitigate | Test header states against task fixtures and keep critical rows visible after refresh failure. |
| T-04-06 | Denial of Service | Refresh path | mitigate | Preserve the last known task list and show recovery copy instead of blanking the screen. |
</threat_model>

## Artifacts this phase produces

- `TodayScreen` with safety header, task sections, rows, refresh, future attention, and empty state.
- Hoje copy module following the approved Portuguese-BR contract.
- CaptureApp route updates that make Hoje first while preserving Phase 3 flows.

<verification>
- `pnpm.cmd --filter @validade-zero/mobile test -- today-screen`
- `pnpm.cmd --filter @validade-zero/mobile test -- App`
- `pnpm.cmd --filter @validade-zero/mobile test -- today-task-fixtures`
- `pnpm.cmd --filter @validade-zero/mobile typecheck`
- `pnpm.cmd lint`
</verification>

<success_criteria>
- The app opens with an explicit sales-area safety answer.
- Critical sales-area work is first and unmistakable.
- Task rows remain lot-level, actionable, and accessible.
- Radar/future attention stays quiet and non-actionable.
</success_criteria>

<output>
Create `.planning/phases/04-today-task-workflow/04-02-SUMMARY.md` when done.
</output>
