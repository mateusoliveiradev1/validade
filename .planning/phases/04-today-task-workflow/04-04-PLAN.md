---
phase: 04-today-task-workflow
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/TodayScreen.tsx
  - apps/mobile/src/capture/TaskResolutionPanel.tsx
  - apps/mobile/src/capture/today-copy.ts
  - apps/mobile/src/capture/capture-ui.tsx
  - apps/mobile/src/capture/today-accessibility.test.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - apps/mobile/src/capture/task-resolution.test.tsx
  - apps/mobile/src/App.test.tsx
  - .maestro/smoke.yaml
  - eslint.config.mjs
autonomous: true
requirements:
  - RSK-03
  - RSK-04
  - PSH-03
  - UI-01
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - "D-14: The final smoke still opens on Hoje and announces sales-area safety first."
    - "D-15: Safe-with-work copy remains distinct from fully empty safe state."
    - "D-16: Critical state is visually dominant but not modal-blocking by default."
    - "D-18: Long product, lot, location, and confirmation copy wraps without hiding action or risk."
    - "D-19: Final visible copy stays direct, operational, and non-blaming."
    - "D-21: Empty state keeps registration and recent-lot escape paths."
    - "D-24: Due copy remains Agora, Ainda no turno, Hoje, or Rever em {janela}."
    - "D-25: Overdue tasks are fixed at the top and use explicit Atrasada copy; push/escalation stay Phase 5."
    - "UI-01: The first mobile screen answers Area de venda segura?."
    - "UI-02: Final copy uses the approved Portuguese-BR operational labels."
    - "UI-03: Final UI keeps high contrast, one-hand use, 48dp targets, reduced ambiguity, and no color-only risk meaning."
  artifacts:
    - path: apps/mobile/src/capture/today-accessibility.test.tsx
      provides: "Accessibility, copy, target size, wrapping, and non-color-only risk assertions for Hoje."
    - path: .maestro/smoke.yaml
      provides: "Native smoke entry assertions for the Hoje-first mobile workflow when runtime is available."
    - path: apps/mobile/src/capture/TodayScreen.tsx
      provides: "Overdue pinning, final refresh behavior, and completed Phase 4 UI polish."
  key_links:
    - from: apps/mobile/src/capture/TodayScreen.tsx
      to: .planning/phases/04-today-task-workflow/04-UI-SPEC.md
      via: "Final layout and copy are audited against the approved design contract."
    - from: .maestro/smoke.yaml
      to: apps/mobile/src/capture/CaptureApp.tsx
      via: "Smoke assertions use current Hoje labels instead of retired Phase 3-only entry copy."
---

<objective>
Harden the finished Today task workflow for overdue behavior, accessibility, copy fidelity, smoke coverage, and full regression evidence.

Purpose: Before Phase 4 is executable-complete, the screen must stay usable under store conditions and all safety-critical copy/states must be protected by tests.
Output: Overdue pinning, refresh/error polish, a11y/copy tests, updated native smoke, final focused/full command results, and a truthful summary of any native runtime blocker.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-today-task-workflow/04-CONTEXT.md
@.planning/phases/04-today-task-workflow/04-RESEARCH.md
@.planning/phases/04-today-task-workflow/04-VALIDATION.md
@.planning/phases/04-today-task-workflow/04-UI-SPEC.md
@.planning/phases/04-today-task-workflow/04-PATTERNS.md
@.planning/phases/04-today-task-workflow/04-03-SUMMARY.md
@apps/mobile/src/capture/TodayScreen.tsx
@apps/mobile/src/capture/TaskResolutionPanel.tsx
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/App.test.tsx
@.maestro/smoke.yaml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden overdue, refresh, accessibility, wrapping, and copy states</name>
  <read_first>
    - @.planning/phases/04-today-task-workflow/04-UI-SPEC.md
    - apps/mobile/src/capture/TodayScreen.tsx
    - apps/mobile/src/capture/TaskResolutionPanel.tsx
    - apps/mobile/src/capture/capture-ui.tsx
    - apps/mobile/src/capture/today-copy.ts
    - apps/mobile/src/capture/today-screen.test.tsx
    - apps/mobile/src/capture/task-resolution.test.tsx
  </read_first>
  <files>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/TaskResolutionPanel.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/today-accessibility.test.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/task-resolution.test.tsx, eslint.config.mjs</files>
  <action>Audit the completed Hoje flow against `04-UI-SPEC.md` and add only missing concrete behavior. Pin overdue tasks above non-overdue tasks and render explicit `Atrasada` copy with the same row anatomy; do not add push, repeat reminders, escalation, or Phase 5 behavior. Keep loading as stable skeleton/task placeholders rather than an isolated spinner. Ensure all primary and secondary task actions have visible text labels, accessible names, and minimum 48dp touch target styles. Ensure risk/delay/success/blocked states combine text plus structure and are not color-only. Add wrapping styles or component structure so long product names, lot identifiers, locations, evidence reasons, and confirmation copy do not clip action or risk labels. Preserve exact approved labels: `Hoje`, `Area de venda segura`, `Area de venda segura, ainda ha tarefas do turno`, `Area de venda com {n} risco(s) agora`, `Retirar agora`, `Conferir agora`, `Pedir rebaixa`, `Rever em {janela}`, `Atualizar tarefas`, `Voltar e revisar`, and the evidence/no-photo labels. Add `today-accessibility.test.tsx` to assert accessible labels, no icon-only primary action, required text labels for risk states, target-size style values, long-copy wrapping style, reduced-motion-safe immediate state behavior where applicable, and no generic primary label such as `Salvar`, `Enviar`, `OK`, or `Cancelar`.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- today-accessibility && pnpm.cmd --filter @validade-zero/mobile test -- today-screen && pnpm.cmd --filter @validade-zero/mobile test -- task-resolution && pnpm.cmd --filter @validade-zero/mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Overdue tasks render above other active tasks and include visible `Atrasada` copy.
    - Accessibility tests prove labels, 48dp targets, non-color-only risk states, wrapping, and no generic primary labels.
    - Loading/refresh keeps the header stable and never replaces the full screen with a lone spinner.
    - Approved Portuguese-BR labels from `04-UI-SPEC.md` remain present in source/tests.
    - Focused Hoje, resolution, accessibility, and mobile typecheck commands exit 0.
  </acceptance_criteria>
  <done>The Hoje flow is accessible, readable under pressure, and protected against copy/layout regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Update smoke coverage and run final Phase 4 regression gates</name>
  <read_first>
    - @.planning/phases/04-today-task-workflow/04-VALIDATION.md
    - apps/mobile/src/App.test.tsx
    - apps/mobile/src/capture/TodayScreen.tsx
    - .maestro/smoke.yaml
    - package.json
  </read_first>
  <files>apps/mobile/src/App.test.tsx, apps/mobile/src/capture/today-accessibility.test.tsx, .maestro/smoke.yaml, eslint.config.mjs</files>
  <action>Update the mobile smoke test and `.maestro/smoke.yaml` so the stable entry assertions target the Phase 4 Hoje surface: `Hoje`, one sales-area safety verdict, `Atualizar tarefas`, and `Registrar lote`. Preserve Phase 3 manual registration reachability in smoke where practical, but do not overbuild a full native fixture if component coverage already proves task resolution. Run the final command ladder in order: `pnpm.cmd --filter @validade-zero/domain test -- tasks`, `pnpm.cmd --filter @validade-zero/contracts test -- tasks`, `pnpm.cmd --filter @validade-zero/mobile test`, `pnpm.cmd --filter @validade-zero/mobile typecheck`, `pnpm.cmd lint`, `pnpm.cmd test`, `pnpm.cmd test:e2e:mobile` when configured emulator/device is available, and `pnpm.cmd check`. If native E2E cannot run because of emulator/device/runtime constraints, record the exact blocker in the summary and do not claim a pass. Fix regressions inside Phase 4 scope only; leave unrelated refactors and future phases alone. Create the summary with exact command results, deviations, and whether native smoke passed or was blocked.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test && pnpm.cmd --filter @validade-zero/mobile typecheck && pnpm.cmd lint && pnpm.cmd test && pnpm.cmd check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile/src/App.test.tsx` and `.maestro/smoke.yaml` assert the Hoje-first entry surface, not only the old product-discovery entry.
    - Summary records exact focused package, mobile, lint, root test, root check, and native E2E results or native runtime blocker.
    - Final source contains no push/escalation, remote sync, R2/evidence upload, roles, shift close, or analytics dashboard expansion.
    - `pnpm.cmd --filter @validade-zero/mobile test`, mobile typecheck, `pnpm.cmd lint`, `pnpm.cmd test`, and `pnpm.cmd check` exit 0 unless an exact external native E2E blocker is documented.
  </acceptance_criteria>
  <done>Phase 4 has final automated evidence and a clear handoff to verification/UAT.</done>
</task>

</tasks>

<traceability>
- D-14: Hoje remains the smoke-protected first screen.
- D-15: Safe-with-work state remains distinct.
- D-16: Critical state is dominant but inline.
- D-18: Row/confirmation copy wraps and preserves action/risk.
- D-19: Copy stays direct and non-blaming.
- D-21: Empty state keeps registration/recent routes.
- D-24: Due copy remains severity/action based.
- D-25: Overdue tasks pin in Hoje; push/escalation are deferred.
- UI-01/UI-02/UI-03: Final tests protect safety-first mobile UX, copy, contrast, touch targets, and accessibility.
</traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| due/overdue state -> task ordering | An overdue task could be hidden below lower-risk work. |
| visual state -> accessibility tree | Risk could be visible only by color or inaccessible structure. |
| smoke evidence -> phase confidence | Passing component tests could be mistaken for native device coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Denial of Service | Overdue ordering | mitigate | Pin overdue tasks at the top and test explicit `Atrasada` copy. |
| T-04-11 | Information disclosure | Test fixtures/screenshots | mitigate | Use fictitious data only and do not add real photos or operational records. |
| T-04-12 | Repudiation | Verification evidence | mitigate | Record exact command results and native E2E blocker/pass status in the summary. |
</threat_model>

## Artifacts this phase produces

- Final Hoje accessibility/copy hardening and tests.
- Updated mobile smoke assertions for the Hoje-first app.
- Phase 4 summary with focused and full verification evidence.

<verification>
- `pnpm.cmd --filter @validade-zero/domain test -- tasks`
- `pnpm.cmd --filter @validade-zero/contracts test -- tasks`
- `pnpm.cmd --filter @validade-zero/mobile test`
- `pnpm.cmd --filter @validade-zero/mobile typecheck`
- `pnpm.cmd lint`
- `pnpm.cmd test`
- `pnpm.cmd test:e2e:mobile` when native runtime is available
- `pnpm.cmd check`
</verification>

<success_criteria>
- Hoje is accessible, high-contrast, one-hand friendly, and explicit under critical/overdue states.
- Smoke tests protect the Phase 4 entry surface.
- Full regression gates pass or native-only blockers are documented honestly.
- The phase remains within Today task workflow scope and does not absorb Phase 5-8 capabilities.
</success_criteria>

<output>
Create `.planning/phases/04-today-task-workflow/04-04-SUMMARY.md` when done.
</output>
