---
phase: 05-push-and-escalation
plan: "04"
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - packages/adapters/src/alerts.ts
  - packages/adapters/src/index.ts
  - apps/api/src/index.ts
  - apps/api/src/alerts.test.ts
  - apps/api/wrangler.toml
  - apps/mobile/src/App.test.tsx
  - .maestro/smoke.yaml
  - docs/operations/push-alerts.md
  - .planning/phases/05-push-and-escalation/05-04-SUMMARY.md
autonomous: true
requirements:
  - RSK-05
  - PSH-01
  - PSH-02
  - PSH-05
user_setup:
  - "Remote push and native smoke may require external device/build/Maestro setup; record exact blocker if unavailable."
must_haves:
  truths:
    - "D-04: Provider/cron behavior uses the default safe pilot cadence and leaves admin configuration out of scope."
    - "D-10: Channel/provider failure is surfaced as degraded alert state, not task resolution."
    - "D-11: Temporary send failures use limited retry/backoff state and visible pending/failed status."
    - "D-12: Provider payload contains task id and active key for current/stale routing."
    - "D-13/D-15: Provider payload remains privacy-safe and avoids lock-screen lot details."
    - "D-16: Provider action category only opens the task; no remote resolve action is created."
    - "Expo docs: successful tickets/receipts do not guarantee device receipt, so docs/summary must not claim delivery certainty."
    - "Cloudflare docs: scheduled handlers use UTC cron and Wrangler scheduled test routes."
    - "PSH-05: Final smoke/docs emphasize every push corresponds to an in-app task."
  artifacts:
    - path: packages/adapters/src/alerts.ts
      provides: "Expo provider adapter seam with fake/test provider and delivery result mapping."
    - path: apps/api/src/alerts.test.ts
      provides: "API/cron/provider tests for scheduled dispatch, retryable errors, and invalid tokens."
    - path: docs/operations/push-alerts.md
      provides: "Operator/dev notes for Phase 5 limitations, native setup, and push-does-not-resolve rule."
  key_links:
    - from: apps/api/src/index.ts
      to: apps/api/wrangler.toml
      via: "Scheduled handler must match configured cron expression and be locally testable."
    - from: packages/adapters/src/alerts.ts
      to: packages/contracts/src/alerts.ts
      via: "Provider send/receipt results parse through contracts before API/mobile state consumes them."
---

<objective>
Add the API/provider/cron seam, update smoke/documentation, and run final Phase 5 verification.

Purpose: Phase 5 should leave a tested path for Expo Push dispatch and Cloudflare scheduled execution while documenting current MVP limitations and protecting the Hoje-first mobile flow.
Output: provider adapter seam, API scheduled tests, wrangler cron config, smoke updates, operations docs, final command evidence, and summary.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-push-and-escalation/05-CONTEXT.md
@.planning/phases/05-push-and-escalation/05-RESEARCH.md
@.planning/phases/05-push-and-escalation/05-VALIDATION.md
@.planning/phases/05-push-and-escalation/05-UI-SPEC.md
@.planning/phases/05-push-and-escalation/05-PATTERNS.md
@.planning/phases/05-push-and-escalation/05-03-SUMMARY.md
@packages/contracts/src/alerts.ts
@packages/adapters/src/index.ts
@apps/api/src/index.ts
@apps/api/src/index.test.ts
@apps/api/wrangler.toml
@apps/mobile/src/App.test.tsx
@.maestro/smoke.yaml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Expo provider adapter seam and Cloudflare scheduled dispatch tests</name>
  <read_first>
    - packages/contracts/src/alerts.ts
    - packages/adapters/src/index.ts
    - apps/api/src/index.ts
    - apps/api/src/index.test.ts
    - apps/api/wrangler.toml
    - .planning/phases/05-push-and-escalation/05-RESEARCH.md
  </read_first>
  <files>packages/adapters/src/alerts.ts, packages/adapters/src/index.ts, apps/api/src/index.ts, apps/api/src/alerts.test.ts, apps/api/wrangler.toml</files>
  <action>Create `packages/adapters/src/alerts.ts` with an `AlertDeliveryProvider` interface and fake provider implementation. The provider must accept `AlertDispatchCommand`, validate through contracts, map provider responses to `AlertDeliveryResult`, mark HTTP 429/5xx/network as retryable, mark invalid payload and `DeviceNotRegistered` as permanent/device-not-registered, and never log raw tokens. Export it from `packages/adapters/src/index.ts`. Extend the API with a provider-injected alert dispatch service and a Cloudflare `scheduled` handler shape that can run due-alert dispatch from a repository/provider port. Because this repo has no durable remote task/token store yet, use a clearly named in-memory/fake alert repository in tests and document the production limitation. Add `[triggers] crons = [ "*/15 * * * *" ]` or a similarly minimal UTC cron in `apps/api/wrangler.toml` if the scheduled handler is present. Add `apps/api/src/alerts.test.ts` covering scheduled invocation, privacy-safe payload, retryable provider error, `DeviceNotRegistered`, no raw token in response, and no task-resolution field. Keep `/health` and `/probe` behavior unchanged.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/api test -- alerts && pnpm.cmd --filter @validade-zero/api test && pnpm.cmd --filter @validade-zero/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Provider results are contract-validated and distinguish retryable/permanent/device-not-registered outcomes.
    - Scheduled handler is testable without real Cloudflare deployment, real tokens, or real Expo credentials.
    - Cron configuration is UTC-aware and minimal.
    - Existing API health/probe tests still pass.
    - Focused API alert tests, API full tests, and API typecheck exit 0.
  </acceptance_criteria>
  <done>The API has a tested provider/cron seam without pretending durable remote fan-out exists yet.</done>
</task>

<task type="auto">
  <name>Task 2: Update smoke/docs and run final Phase 5 regression gates</name>
  <read_first>
    - .planning/phases/05-push-and-escalation/05-VALIDATION.md
    - .planning/phases/05-push-and-escalation/05-UI-SPEC.md
    - apps/mobile/src/App.test.tsx
    - apps/mobile/src/capture/TodayScreen.tsx
    - .maestro/smoke.yaml
    - package.json
  </read_first>
  <files>apps/mobile/src/App.test.tsx, .maestro/smoke.yaml, docs/operations/push-alerts.md, .planning/phases/05-push-and-escalation/05-04-SUMMARY.md</files>
  <action>Update `apps/mobile/src/App.test.tsx` and `.maestro/smoke.yaml` with deterministic Phase 5 smoke assertions that still start at `Hoje`, include the safety verdict, include `Ativar alertas do turno` or the current alert channel notice, include `Atualizar tarefas`, and preserve `Registrar lote`. Do not require native remote push delivery in smoke unless fixture/setup is deterministic. Create `docs/operations/push-alerts.md` documenting: push helps charge but Hoje remains source of truth; permission is requested only after the Hoje context; lock-screen content excludes lot details; fake-token policy for tests; Expo remote push requires development build/credentials/device support; Cloudflare cron uses UTC; delivery/tickets/receipts are not physical-resolution proof; current remote multi-device fan-out is blocked on future durable sync/auth/task store. Run final commands in order: `pnpm.cmd --filter @validade-zero/domain test -- alerts`, `pnpm.cmd --filter @validade-zero/contracts test -- alerts`, `pnpm.cmd --filter @validade-zero/mobile test -- alert`, `pnpm.cmd --filter @validade-zero/mobile test -- push`, `pnpm.cmd --filter @validade-zero/api test -- alerts`, `pnpm.cmd --filter @validade-zero/mobile test`, `pnpm.cmd --filter @validade-zero/mobile typecheck`, `pnpm.cmd lint`, `pnpm.cmd test`, `pnpm.cmd test:e2e:mobile` when native runtime is available, and `pnpm.cmd check`. If Maestro/native push cannot run, record the exact blocker and do not claim native delivery. Create the summary with exact command results, deviations, and limitations.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test && pnpm.cmd --filter @validade-zero/mobile typecheck && pnpm.cmd --filter @validade-zero/api test && pnpm.cmd lint && pnpm.cmd test && pnpm.cmd check</automated>
  </verify>
  <acceptance_criteria>
    - Smoke asserts Hoje-first plus alert-channel affordance without relying on real push credentials.
    - Operations docs explicitly state push/ticket/receipt/acknowledgement do not resolve tasks.
    - Final summary records exact focused, package, root, native smoke, and full check results or blocker.
    - Source contains no real token, secret, real operational data, or private evidence asset.
    - Full regression commands exit 0 unless an exact external native E2E blocker is documented.
  </acceptance_criteria>
  <done>Phase 5 has provider/cron evidence, smoke/docs, and a truthful verification closeout.</done>
</task>

</tasks>

<traceability>
- D-04/D-10/D-11: provider retry/failure state and default cadence remain safe.
- D-12/D-13/D-15/D-16: provider payload and smoke keep task-open-only, privacy-safe behavior.
- RSK-05/PSH-01/PSH-02/PSH-05: final regression proves alert pressure maps to persistent tasks and unresolved risk remains visible.
</traceability>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/provider -> Expo Push Service | External service can fail, rate-limit, or accept without device delivery. |
| scheduled handler -> due alerts | UTC cron and transient runtime behavior can miss or delay dispatch. |
| docs/smoke -> stakeholder confidence | Passing tests can be overread as real native remote delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Denial of Service | Expo provider | mitigate | Map 429/5xx/network to retryable result and visible pending state. |
| T-05-13 | Information disclosure | Provider payload/logs | mitigate | Contract tests reject lot details and tests assert no raw tokens in responses. |
| T-05-14 | Repudiation | Verification summary | mitigate | Summary records exact commands and native delivery blockers honestly. |
</threat_model>

## Artifacts this phase produces

- Expo provider adapter seam and API scheduled handler tests.
- Wrangler cron configuration for scheduled dispatch seam.
- Updated mobile smoke assertions.
- Push alert operations documentation.
- Final Phase 5 summary with command evidence and limitations.

<verification>
- `pnpm.cmd --filter @validade-zero/domain test -- alerts`
- `pnpm.cmd --filter @validade-zero/contracts test -- alerts`
- `pnpm.cmd --filter @validade-zero/mobile test -- alert`
- `pnpm.cmd --filter @validade-zero/mobile test -- push`
- `pnpm.cmd --filter @validade-zero/api test -- alerts`
- `pnpm.cmd --filter @validade-zero/mobile test`
- `pnpm.cmd --filter @validade-zero/mobile typecheck`
- `pnpm.cmd lint`
- `pnpm.cmd test`
- `pnpm.cmd test:e2e:mobile` when native runtime is available
- `pnpm.cmd check`
</verification>

<success_criteria>
- Provider/cron behavior is contract-tested and does not leak tokens or lot details.
- Native/remote limitations are documented honestly.
- Hoje-first mobile smoke remains stable.
- Full regression is green or external native blockers are recorded.
</success_criteria>

<output>
Create `.planning/phases/05-push-and-escalation/05-04-SUMMARY.md` when done.
</output>
