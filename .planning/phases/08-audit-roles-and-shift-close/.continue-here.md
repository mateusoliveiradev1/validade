---
context: phase
phase: 08-audit-roles-and-shift-close
task: 2
total_tasks: 3
status: blocked
last_updated: 2026-06-22T10:10:16.880Z
---

<current_state>
Phase 8 execution is paused inside Plan 08-01, Task 2: `[BLOCKING] Persist the working membership path in Neon`.

Task 1 is complete and committed as `15ab5d9`:
- Pure authorization matrix in `packages/domain/src/authorization.ts`
- Strict authorization/session contracts in `packages/contracts/src/authorization.ts`
- Hono auth/authorization seam in `apps/api/src/auth.ts` and protected `/session/context`, `/session/probe/task-act`, `/session/probe/shift-close`
- Web `CurrentScope` component preserving the health smoke
- Focused tests and web typecheck passed

Task 2 implementation is in progress and locally green except for the external Neon branch check:
- Added `@validade-zero/database`
- Added Drizzle/Neon dependencies and lockfile updates
- Added schema for `store_memberships` and append-only `audit_events`
- Added idempotent SQL migration `packages/database/drizzle/0001_phase_08_identity_audit.sql`
- Added database Vitest project
- Passed `pnpm.cmd vitest run --config vitest.config.ts --project database`
- Passed `pnpm.cmd --filter @validade-zero/database db:check`
- Passed `pnpm.cmd security:env`
- Passed `pnpm.cmd --filter @validade-zero/database typecheck`
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Deliver the thinnest role-aware protected web path — Done, committed `15ab5d9`
- Task 2: Persist the working membership path in Neon — In progress; local package/schema/migration gates done

</completed_work>

<remaining_work>

- Task 2: Run the blocking disposable Neon branch verification using `NEON_DATABASE_URL`
- Task 2: After external verification passes, commit the Task 2 changes atomically
- Task 3: Enforce server-owned identity and audit sanitized access denials
- Plan closeout: create `08-01-SUMMARY.md`, update GSD tracking, then continue Wave 2

</remaining_work>

<decisions_made>

- Use `@validade-zero/database` as the central persistence boundary for Drizzle/Neon.
- Keep migration SQL idempotent with guarded enum creation, `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, and trigger recreation.
- Do not mark Task 2 complete without real disposable Neon branch verification.
- Preserve `.agents/` and `.codex/` as unrelated untracked directories; do not stage them.

</decisions_made>

<blockers>

- `NEON_DATABASE_URL` is missing from the environment. The Plan 08-01 Task 2 acceptance criteria require applying `0001_phase_08_identity_audit.sql` to a temporary Neon branch and asserting tables, indexes, active uniqueness, append-only rejection, and migration idempotency before Task 2 can be marked done.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact pause state.
2. `.planning/phases/08-audit-roles-and-shift-close/08-01-PLAN.md` — Task 2 acceptance and Task 3 scope.
3. `packages/database/drizzle/0001_phase_08_identity_audit.sql` — migration to apply/assert against the temporary Neon branch.
4. `packages/database/src/schema.ts` and `packages/database/src/schema.test.ts` — schema/test expectations.

## Critical Anti-Patterns (do NOT repeat these)

- Do not print, commit, log, or summarize the real `NEON_DATABASE_URL`.
- Do not stage `.agents/` or `.codex/`; they are unrelated untracked directories.
- Do not create `08-01-SUMMARY.md` until Task 2 and Task 3 are complete.

## Infrastructure State

- No background servers/watchers are running.
- `NEON_DATABASE_URL` was checked and is absent.
- No `.env`, `.env.local`, `apps/api/.env`, or `packages/database/.env` file exists.

<context>
The next agent should treat this as a normal external-credential checkpoint, not a design ambiguity. The implementation direction is already chosen and local gates are green. The only missing piece is a temporary Neon branch URL supplied through the environment for the blocking migration assertion.
</context>

<next_action>
Start with: set `NEON_DATABASE_URL` to a disposable Neon branch connection string outside git, then run a non-printing migration/assertion script against that branch. After it passes, rerun the local Task 2 gates and commit the Task 2 files.
</next_action>
