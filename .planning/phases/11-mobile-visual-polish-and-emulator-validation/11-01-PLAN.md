---
phase: 11-mobile-visual-polish-and-emulator-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/capture/capture-theme.ts
  - apps/mobile/src/capture/capture-ui.tsx
  - apps/mobile/src/capture/mobile-status.ts
  - apps/mobile/src/capture/mobile-status.test.ts
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/TodayScreen.tsx
  - apps/mobile/src/capture/prepare-turn.test.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - apps/mobile/src/capture/mobile-capture.accessibility.test.ts
autonomous: true
requirements:
  - P11-POLISH-01
  - P11-STATUS-02
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08."
    - "Shared mobile status vocabulary must distinguish conflict, blocker, critical, local, pending central, syncing, synced, resolved, and safe without relying on color alone."
    - "Prepare-turn remains the gate before Hoje; local cache or stale central read never looks like safe central readiness."
  artifacts:
    - path: apps/mobile/src/capture/mobile-status.ts
      provides: "Typed local status descriptors and priority order for Phase 11 mobile state treatments."
    - path: apps/mobile/src/capture/capture-ui.tsx
      provides: "Reusable StatusNotice/status row primitives with Phase 11 tones and accessibility state."
    - path: apps/mobile/src/capture/TodayScreen.tsx
      provides: "Sales-area verdict and central/local/sync state placement near the first critical decision."
---

<objective>
**As a** mobile collaborator starting a shift, **I want to** see one consistent status language before and inside Hoje, **so that** local, pending central, conflict, critical, synced, resolved, and safe states cannot be confused during corridor work.

Purpose: establish the Phase 11 shared mobile status system and apply it to the prepare-turn gate and Hoje cockpit before polishing deeper screens.
Output: typed status vocabulary, updated local primitives, prepare-turn/Hojes first-viewport polish, and focused component/a11y tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mobile-visual-polish-and-emulator-validation/11-CONTEXT.md
@.planning/phases/11-mobile-visual-polish-and-emulator-validation/11-UI-SPEC.md
@.planning/phases/11-mobile-visual-polish-and-emulator-validation/11-VALIDATION.md
@.planning/phases/10-real-pilot-flow-rebuild/10-CONTEXT.md
@apps/mobile/src/capture/capture-theme.ts
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/TodayScreen.tsx
@apps/mobile/src/capture/today-copy.ts
@apps/mobile/src/capture/prepare-turn.test.tsx
@apps/mobile/src/capture/today-screen.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the typed mobile status vocabulary</name>
  <read_first>
    - apps/mobile/src/capture/capture-theme.ts
    - apps/mobile/src/capture/capture-ui.tsx
    - apps/mobile/src/capture/today-copy.ts
    - .planning/phases/11-mobile-visual-polish-and-emulator-validation/11-UI-SPEC.md
  </read_first>
  <files>apps/mobile/src/capture/capture-theme.ts, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/mobile-status.ts, apps/mobile/src/capture/mobile-status.test.ts</files>
  <action>Add a local typed status descriptor module named `apps/mobile/src/capture/mobile-status.ts` with descriptors for `prepare_blocker`, `critical`, `conflict`, `local_only`, `pending_central`, `syncing`, `synced_transport`, `resolved_central`, `safe_central`, `provider_degraded`, and `camera_degraded`. Each descriptor must include `tone`, `priority`, `label`, `body`, `surface`, and `isProvenSafe` or equivalent typed proof flag. Extend `StatusNotice` in `capture-ui.tsx` to support `critical`, `warning`, `info`, `neutral`, and `success` tones, plus an optional visible title and `accessibilityRole` behavior. Keep color tokens in `capture-theme.ts`; do not add another palette or any third-party UI kit.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- mobile-status mobile-capture.accessibility && pnpm.cmd --filter @validade-zero/mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `mobile-status.ts` exports a typed priority order where conflict/blocker outranks pending central, critical, local-only, degraded provider/camera, synced transport, and resolved/safe history.
    - `synced_transport.isProvenSafe` is false and `resolved_central.isProvenSafe` is true only for resolved central business proof.
    - `StatusNotice` can render warning and critical text without using green/accent styles.
    - `mobile-status.test.ts` asserts labels for `Local`, `Pendente central`, `Sincronizado com a central`, `Resolvido com criterio operacional e confirmacao central`, and `Conflito de sincronizacao`.
  </acceptance_criteria>
  <done>The status language is centralized, typed, test-covered, and ready for use by all critical mobile screens.</done>
</task>

<task type="auto">
  <name>Task 2: Apply final-status treatment to Preparar turno</name>
  <read_first>
    - apps/mobile/src/capture/CaptureApp.tsx
    - apps/mobile/src/capture/capture-ui.tsx
    - apps/mobile/src/capture/mobile-status.ts
    - apps/mobile/src/capture/prepare-turn.test.tsx
  </read_first>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/prepare-turn.test.tsx, apps/mobile/src/capture/mobile-capture.accessibility.test.ts</files>
  <action>Refactor `PrepareTurnScreen` inside `CaptureApp.tsx` so it uses the shared status descriptors and Phase 11 copy: title `Preparar turno`, loading copy `Baixando a leitura central da loja...`, blocked-offline copy `Conecte uma vez para preparar o turno neste aparelho.`, ready copy `Pronto para operar com a leitura central.`, and stale copy `Leitura central pendente. Atualize antes de declarar area segura.` Preserve the existing route gate: ordinary Hoje renders only when `prepareTurnState === "ready"`, and local cache entry stays visibly warning/non-safe.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- prepare-turn mobile-capture.accessibility</automated>
  </verify>
  <acceptance_criteria>
    - Prepare-turn blocked/error/cache-only states render critical or warning surfaces before any action that can enter ordinary Hoje.
    - `Entrar com leitura local` remains secondary and its copy states that local cache is not a safe central read.
    - Loading state keeps stable text and disabled `Preparar turno` action; no centered spinner replaces the operational decision text.
    - Tests prove `Hoje` is not visible before the prepare-turn gate reaches ready.
  </acceptance_criteria>
  <done>Preparar turno feels final on Android while preserving the central-read gate.</done>
</task>

<task type="auto">
  <name>Task 3: Polish the Hoje cockpit first viewport</name>
  <read_first>
    - apps/mobile/src/capture/TodayScreen.tsx
    - apps/mobile/src/capture/today-copy.ts
    - apps/mobile/src/capture/mobile-status.ts
    - apps/mobile/src/capture/today-screen.test.tsx
  </read_first>
  <files>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/mobile-capture.accessibility.test.ts</files>
  <action>Rework the top of `TodayScreen.tsx` so the first viewport answers, in order: `Hoje`, the sales-area verdict, central/local/sync state directly below the verdict, the first critical action, and then secondary actions. Use Phase 11 copy for unsafe verdict `Area de venda com risco agora`, safe-with-work verdict `Area sem bloqueio critico, ainda ha tarefas do turno`, and empty heading `Nenhum bloqueio ativo na leitura central`. Keep conflict and pending central status above normal task rows. Ensure all metadata remains 14px or body 16px, hero labels do not use 12px, and primary actions keep 48dp minimum touch height.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- today-screen mobile-capture.accessibility && pnpm.cmd --filter @validade-zero/mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The first rendered Hoje block contains visible central/local/sync state before the task list.
    - Critical risk uses controlled critical surface/tag treatment and explicit required action text, not a full-screen red treatment.
    - `Sincronizado com a central` appears in neutral/muted treatment, not the same green/accent treatment as resolved/safe.
    - Tests cover critical, safe-with-work, empty, local-cache, pending-sync, and conflict-priority render paths.
  </acceptance_criteria>
  <done>Hoje has a corridor-ready cockpit that keeps central truth and the next action visible.</done>
</task>

</tasks>

<traceability>
- P11-POLISH-01: prepare-turn and Hoje meet the Phase 11 first-viewport screen-final criteria.
- P11-STATUS-02: status descriptors make local, pending central, synced, conflict, critical, resolved, and safe distinct.
- UI-01/UI-02/UI-03/UI-04: mobile first screen remains operational, direct, high contrast, and one-hand friendly.
- SYN-01/SYN-02/SYN-03: pending, conflict, and synced transport state remain visible and truthful.
</traceability>

<decision_coverage>
- D-01: covers Preparar turno and Hoje as mandatory critical flow surfaces.
- D-02: uses Android legibility, one primary action, consistent spacing, and non-prototype screen criteria.
- D-03: reorganizes hierarchy without adding capabilities or changing truth semantics.
- D-04: prepare blockers, critical risk, pending central, and conflict receive strong clear treatment.
- D-05: severity hierarchy and explicit text distinguish states without painting every surface red.
- D-06: accent is reserved for primary actions and proven safe/resolved states.
- D-07: Local and Pendente central are warnings near the verdict/action, not proof.
- D-08: creates the shared mobile status system consumed by notices and task/status rows.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
|---|---|---|
| visual status -> operational truth | A green or success-like style could imply safe/resolved when only synced/local. | Typed proof flag plus tests asserting synced/local/pending are not safe. |
| prepare-turn gate -> Hoje | Local cache entry could look equivalent to central readiness. | Warning/critical descriptor and gate tests keep local cache visibly non-safe. |
| first viewport -> action choice | Critical risk could be visually below routine actions. | Priority order places conflict/blocker/pending central and first critical action before ordinary rows. |
</threat_model>

<verification>
- `pnpm.cmd --filter @validade-zero/mobile test -- mobile-status prepare-turn today-screen mobile-capture.accessibility`
- `pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Shared status vocabulary exists and is test-covered.
- Preparar turno cannot look safe when central truth is stale, local-only, blocked, or unavailable.
- Hoje first viewport shows sales-area verdict, central/local/sync state, and next critical action without ambiguity.
- No new UI kit, registry, decorative assets, or unsafe copy is introduced.
</success_criteria>

<output>
Create `.planning/phases/11-mobile-visual-polish-and-emulator-validation/11-01-SUMMARY.md` when done.
</output>
