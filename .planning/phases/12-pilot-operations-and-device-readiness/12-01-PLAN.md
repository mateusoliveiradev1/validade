---
phase: 12-pilot-operations-and-device-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/capture.ts
  - packages/contracts/src/command-center.ts
  - packages/contracts/src/command-center.test.ts
  - packages/database/src/schema.ts
  - packages/database/src/capture-repository.ts
  - packages/database/src/repositories.test.ts
  - apps/api/src/command-center.ts
  - apps/api/src/command-center.test.ts
  - apps/web/src/command-center/CommandCenter.tsx
  - apps/web/src/command-center/command-center.test.tsx
autonomous: true
requirements:
  - P12-DEVICE-01
  - P12-OPS-05
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-01, D-02, D-03, D-04, D-09, D-10, D-12, D-16."
    - "Per-device verdicts are Apto, Atencao, and Bloqueado; raw telemetry is secondary to operator action."
    - "Device presence is last foreground/read/sync truth, not live real-time presence."
  artifacts:
    - path: packages/contracts/src/command-center.ts
      provides: "Device readiness and blocker schemas for Command Center."
    - path: packages/database/src/capture-repository.ts
      provides: "Store-scoped device readiness persistence built on central device snapshots."
    - path: apps/web/src/command-center/CommandCenter.tsx
      provides: "Per-device pilot readiness panel with next actions."
---

<objective>
**As a** lead preparing the pilot, **I want to** see which registered devices are Apto, Atencao, or Bloqueado, **so that** rollout decisions do not depend on hidden assumptions about phones, users, permissions, or stale sync.

Purpose: extend central device snapshots into an operational readiness model.
Output: contracts, persistence, API projection, and Command Center UI for per-device readiness.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-pilot-operations-and-device-readiness/12-CONTEXT.md
@.planning/phases/12-pilot-operations-and-device-readiness/12-RESEARCH.md
@.planning/phases/12-pilot-operations-and-device-readiness/12-VALIDATION.md
@.planning/phases/11-mobile-visual-polish-and-emulator-validation/11-CONTEXT.md
@packages/contracts/src/capture.ts
@packages/contracts/src/command-center.ts
@packages/database/src/schema.ts
@packages/database/src/capture-repository.ts
@apps/api/src/command-center.ts
@apps/web/src/command-center/CommandCenter.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define device readiness contracts and blocker taxonomy</name>
  <read_first>
    - packages/contracts/src/capture.ts
    - packages/contracts/src/command-center.ts
    - .planning/phases/12-pilot-operations-and-device-readiness/12-CONTEXT.md
  </read_first>
  <files>packages/contracts/src/capture.ts, packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts</files>
  <action>Extend contracts with a pilot device readiness model: masked `deviceId`, `deviceLabel`, active user label, store id/name, app version/build, environment, API target, last foreground time, last sync time, last central read time, push permission/provider state, camera permission state, verdict `apto|atencao|bloqueado`, blocker codes, and next actions. Keep technical ids/tokens out of Command Center response fields except masked debug strings. Add tests for D-01 through D-04 and D-16 blocker cases.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/contracts test -- command-center capture</automated>
  </verify>
  <acceptance_criteria>
    - Device readiness verdicts parse only `apto`, `atencao`, or `bloqueado`.
    - Invalid store/user, missing first central read, stale critical sync, push-needed-without-push, and camera-needed-without-camera can become blockers.
    - Last foreground/sync/central read are timestamp facts and never described as live presence.
    - Raw push tokens, raw device ids, private URLs, and build URLs are not valid public projection fields.
  </acceptance_criteria>
  <done>Contracts can express pilot device readiness safely.</done>
</task>

<task type="auto">
  <name>Task 2: Persist and project readiness from central device snapshots</name>
  <read_first>
    - packages/database/src/schema.ts
    - packages/database/src/capture-repository.ts
    - packages/database/drizzle/0006_phase_10_central_capture.sql
    - packages/database/drizzle/0010_phase_10_store_catalog.sql
  </read_first>
  <files>packages/database/src/schema.ts, packages/database/src/capture-repository.ts, packages/database/src/repositories.test.ts</files>
  <action>Add the minimal schema/repository extensions needed for device readiness metadata without creating a separate truth island. Store human-readable labels, app/build metadata, permission/provider states, foreground/sync/read timestamps, and sanitized blocker state alongside or adjacent to `central_device_snapshots`. Preserve store scoping and avoid raw push tokens in read projections. If a migration file is needed, create the next numbered Drizzle SQL migration with idempotent DDL and indexes for store/readiness/updated time.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/database test -- repositories health</automated>
  </verify>
  <acceptance_criteria>
    - Repository tests can write/read multiple devices in the same store and deny cross-store leakage through service scope.
    - The projection can identify stale/no-first-read devices from timestamps and source.
    - Migration comments and indexes keep the table queryable by store and readiness freshness.
    - No schema field requires storing raw Expo push token in Command Center-readable rows.
  </acceptance_criteria>
  <done>Device readiness facts are durable and store-scoped.</done>
</task>

<task type="auto">
  <name>Task 3: Render the Command Center device readiness panel</name>
  <read_first>
    - apps/api/src/command-center.ts
    - apps/web/src/command-center/CommandCenter.tsx
    - apps/web/src/command-center/command-center.test.tsx
  </read_first>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, apps/web/src/command-center/CommandCenter.tsx, apps/web/src/command-center/command-center.test.tsx</files>
  <action>Include device readiness in the Command Center projection and render a dense operational panel titled `Aparelhos do piloto`. Each device row must show verdict, label, user, store, app/build, last sync/read/foreground, permission state, and next action. Place blocked devices before attention devices, then apt devices. Empty state must say no approved device exists and name the next action.</action>
  <verify>
    <automated>pnpm.cmd vitest run --config vitest.config.ts --project api -- command-center && pnpm.cmd vitest run --config vitest.config.ts --project web -- command-center</automated>
  </verify>
  <acceptance_criteria>
    - Command Center can show zero devices without implying rollout readiness.
    - `Bloqueado` rows name cause and next action.
    - `Atencao` rows can represent local-only push when the current UAT step does not require remote push.
    - UI copy stays PT-BR and does not expose raw ids/tokens.
  </acceptance_criteria>
  <done>Leadership can see device readiness before starting pilot claims.</done>
</task>

</tasks>

<traceability>
- P12-DEVICE-01: per-device health model, persistence, and UI.
- P12-OPS-05: blockers are causal and actionable from the first readiness surface.
- AUD-02/AUD-04: store scope and public-safe projection are preserved.
</traceability>

<decision_coverage>
- D-01: device rows use Apto / Atencao / Bloqueado verdicts.
- D-02: blocked criteria map only to operational risk.
- D-03: human-readable labels are visible; technical tokens/ids are masked or omitted.
- D-04: foreground/sync/read timestamps are truth, not live presence.
- D-09: build truth starts appearing per device.
- D-10: build fields are sanitized.
- D-12: old APK readiness depends on current-step risk.
- D-16: blocker causes and next actions are explicit.
</decision_coverage>

<threat_model>
| Boundary | Risk | Mitigation |
|---|---|---|
| device telemetry -> operator verdict | Raw fields confuse pilot readiness. | Contract requires verdict plus next action. |
| mobile token -> web projection | Push token leak in public/UI path. | Store token separately or omit/mask from projection. |
| timestamp -> live presence claim | Last foreground could be read as live tracking. | Copy says last seen/last read only. |
</threat_model>

<verification>
- `pnpm.cmd --filter @validade-zero/contracts test -- command-center capture`
- `pnpm.cmd --filter @validade-zero/database test -- repositories health`
- `pnpm.cmd vitest run --config vitest.config.ts --project api -- command-center`
- `pnpm.cmd vitest run --config vitest.config.ts --project web -- command-center`
</verification>

<success_criteria>
- Device readiness contracts, persistence, API projection, and web UI exist.
- Every blocked device names a cause and next action.
- Command Center does not expose raw tokens, device serials, private URLs, or build URLs.
</success_criteria>

<output>
Create `.planning/phases/12-pilot-operations-and-device-readiness/12-01-SUMMARY.md` when done.
</output>
