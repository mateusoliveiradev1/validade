---
phase: 12-pilot-operations-and-device-readiness
plan: "02"
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - packages/contracts/src/alerts.ts
  - packages/contracts/src/command-center.ts
  - packages/contracts/src/alerts.test.ts
  - packages/domain/src/authorization.ts
  - packages/domain/src/authorization.test.ts
  - apps/api/src/index.ts
  - apps/api/src/alerts.test.ts
  - apps/api/src/command-center.ts
  - apps/api/src/command-center.test.ts
  - apps/web/src/command-center/CommandCenter.tsx
  - apps/web/src/command-center/command-center.test.tsx
autonomous: true
requirements:
  - P12-PUSH-02
  - P12-DEVICE-01
  - P12-OPS-05
user_setup:
  - "Approved native APK/device/provider proof is required to mark remote push delivery as Passed; otherwise record provider as blocked or attention."
must_haves:
  truths:
    - "Decision coverage: D-05, D-06, D-07, D-08, D-16, D-17."
    - "Safe push test proves only the reminder/provider chain and never resolves tasks or shift safety."
    - "Only active same-store leadership/admin can send a push test to a registered pilot device."
  artifacts:
    - path: packages/contracts/src/alerts.ts
      provides: "Safe push-test command and result timeline contract."
    - path: apps/api/src/index.ts
      provides: "Authorized push-test endpoint or command handler."
    - path: apps/web/src/command-center/CommandCenter.tsx
      provides: "Push-test action and timeline in Command Center."
---

<objective>
**As a** store lead validating pilot communications, **I want to** send a safe push test to one registered device and see the outcome, **so that** provider readiness is visible without confusing it with task execution.

Purpose: add leadership-scoped push testing as observability, not work completion.
Output: push-test command/result contracts, RBAC enforcement, provider timeline, and web action.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-pilot-operations-and-device-readiness/12-CONTEXT.md
@.planning/phases/12-pilot-operations-and-device-readiness/12-RESEARCH.md
@.planning/phases/12-pilot-operations-and-device-readiness/12-01-PLAN.md
@docs/operations/push-alerts.md
@apps/mobile/src/capture/alert-channel.ts
@apps/mobile/src/capture/TodayScreen.tsx
@packages/contracts/src/alerts.ts
@packages/domain/src/authorization.ts
@apps/api/src/index.ts
@apps/web/src/command-center/CommandCenter.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add push-test contracts and result timeline states</name>
  <read_first>
    - packages/contracts/src/alerts.ts
    - packages/contracts/src/command-center.ts
    - docs/operations/push-alerts.md
  </read_first>
  <files>packages/contracts/src/alerts.ts, packages/contracts/src/command-center.ts, packages/contracts/src/alerts.test.ts</files>
  <action>Define a safe push-test command and result model with requester label, device label/id reference, requestedAt, permission outcome, provider/token outcome, delivery attempt state, app-open/receive signal when available, next recommended action, and sanitized failure reason. Keep payload content fictional/operator-safe and separate from task alert records. Add tests proving push-test results cannot carry task resolution fields or raw provider token values.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/contracts test -- alerts command-center</automated>
  </verify>
  <acceptance_criteria>
    - Result timeline states cover permission denied, local-only, provider accepted, provider failed, token invalid, opened, and unknown/no signal.
    - The command schema requires a registered device and requester context.
    - No push-test schema accepts `resolvedAt`, `taskStatus`, raw token, or private provider payload.
  </acceptance_criteria>
  <done>Push test is modeled as a safe diagnostic timeline.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce leadership-only same-store push testing</name>
  <read_first>
    - packages/domain/src/authorization.ts
    - apps/api/src/index.ts
    - apps/api/src/alerts.test.ts
  </read_first>
  <files>packages/domain/src/authorization.ts, packages/domain/src/authorization.test.ts, apps/api/src/index.ts, apps/api/src/alerts.test.ts</files>
  <action>Add or reuse a capability for `pilot.push_test.send` that is granted only to same-store lead/admin with active membership. Implement the API command path so collaborators, inactive memberships, revoked users, and cross-store users are denied with sanitized errors. The command must record an audit/timeline result but must not call task resolution, shift close, or Today task mutation code.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/domain test -- authorization && pnpm.cmd vitest run --config vitest.config.ts --project api -- alerts authorization</automated>
  </verify>
  <acceptance_criteria>
    - Collaborator cannot trigger push tests.
    - Lead/admin can trigger only for devices in their active store scope.
    - API tests prove task state and shift verdict remain unchanged after a push test.
    - Denial messages are operational and do not reveal tokens or cross-store facts.
  </acceptance_criteria>
  <done>Push-test authority matches pilot operational roles.</done>
</task>

<task type="auto">
  <name>Task 3: Render safe push-test action and timeline</name>
  <read_first>
    - apps/web/src/command-center/CommandCenter.tsx
    - apps/web/src/command-center/command-center.test.tsx
    - docs/operations/push-alerts.md
  </read_first>
  <files>apps/web/src/command-center/CommandCenter.tsx, apps/web/src/command-center/command-center.test.tsx, apps/api/src/command-center.ts, apps/api/src/command-center.test.ts</files>
  <action>Add a per-device `Enviar teste seguro` action when the user has permission and the device is registered. Render the push-test timeline below the readiness row with requester, time, permission/provider result, optional open signal, and next action. Copy must say the test does not resolve tarefa, does not prove area segura, and only validates the reminder channel.</action>
  <verify>
    <automated>pnpm.cmd vitest run --config vitest.config.ts --project web -- command-center && pnpm.cmd vitest run --config vitest.config.ts --project api -- command-center</automated>
  </verify>
  <acceptance_criteria>
    - Disabled state explains missing leadership permission or missing registered device.
    - Timeline row for provider accepted still says it is not physical execution proof.
    - Timeline row for local-only can remain Atencao unless current UAT objective requires remote push.
    - Tests cover provider failure and invalid token next-action copy.
  </acceptance_criteria>
  <done>Command Center makes push readiness observable without turning it into execution truth.</done>
</task>

</tasks>

<traceability>
- P12-PUSH-02: leadership-only safe push test with provider/token/permission timeline.
- P12-DEVICE-01: device readiness receives push-specific state.
- P12-OPS-05: push blockers receive cause and next action.
- PSH-05: push remains tied to persistent in-app truth and never replaces tasks.
</traceability>

<decision_coverage>
- D-05: only same-store leadership/admin can send push tests.
- D-06: push test never mutates task state or proves physical resolution.
- D-07: timeline shows requester, timestamp, provider/token/permission/open signal, and next action.
- D-08: remote-push absence can be Atencao or Bloqueado according to current UAT objective.
- D-16: invalid token and no push permission produce explicit blockers.
- D-17: token/provider details stay sanitized in public evidence.
</decision_coverage>

<threat_model>
| Boundary | Risk | Mitigation |
|---|---|---|
| push test -> operational task | A provider success could be interpreted as execution. | Tests assert no task/shift mutation and UI copy repeats reminder-only semantics. |
| user role -> noisy push | Collaborators could spam tests. | Capability requires active same-store lead/admin. |
| provider result -> public output | Token/provider internals could leak. | Result schema stores sanitized states and bounded messages only. |
</threat_model>

<verification>
- `pnpm.cmd --filter @validade-zero/contracts test -- alerts command-center`
- `pnpm.cmd --filter @validade-zero/domain test -- authorization`
- `pnpm.cmd vitest run --config vitest.config.ts --project api -- alerts command-center authorization`
- `pnpm.cmd vitest run --config vitest.config.ts --project web -- command-center`
</verification>

<success_criteria>
- Safe push test is restricted to authorized store leadership/admin.
- Timeline records permission/provider/open outcomes without raw tokens.
- Push-test success never resolves tasks, never closes shift, and never marks area safe.
</success_criteria>

<output>
Create `.planning/phases/12-pilot-operations-and-device-readiness/12-02-SUMMARY.md` when done.
</output>
