---
phase: 12-pilot-operations-and-device-readiness
plan: "03"
type: execute
wave: 3
depends_on:
  - "12-02"
files_modified:
  - package.json
  - apps/mobile/package.json
  - apps/mobile/app.json
  - apps/mobile/app.config.js
  - apps/mobile/src/build-info.ts
  - apps/mobile/src/build-info.test.ts
  - apps/mobile/App.tsx
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/repository.ts
  - apps/mobile/src/capture/prepare-turn.test.tsx
  - apps/api/src/command-center.ts
  - apps/web/src/command-center/CommandCenter.tsx
  - docs/release/android-pilot-install.md
  - docs/release/v1-readiness.md
autonomous: true
requirements:
  - P12-RELEASE-03
  - P12-DEVICE-01
  - P12-OPS-05
user_setup: []
must_haves:
  truths:
    - "Decision coverage: D-09, D-10, D-11, D-12, D-13, D-17."
    - "Phase 12 must stop shipping as 0.0.0; use a traceable pilot version such as 0.12.0 and an incrementing Android versionCode."
    - "Desatualizado is calculated against the approved staging artifact, not every commit on main."
  artifacts:
    - path: apps/mobile/src/build-info.ts
      provides: "Sanitized mobile build metadata adapter."
    - path: apps/mobile/app.json
      provides: "Phase 12 app version and Android build code."
    - path: docs/release/v1-readiness.md
      provides: "Release truth matrix with approved staging artifact compatibility."
---

<objective>
**As a** pilot owner checking an installed phone, **I want to** see the exact pilot build version and compatibility status, **so that** an old APK that can still sync is not mistaken for rollout-ready.

Purpose: make installed build truth visible and traceable across mobile, API, and Command Center.
Output: Phase 12 versioning, build-info adapter, mobile diagnostic/about surface, and web build compatibility state.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-pilot-operations-and-device-readiness/12-CONTEXT.md
@.planning/phases/12-pilot-operations-and-device-readiness/12-RESEARCH.md
@apps/mobile/app.json
@apps/mobile/package.json
@apps/mobile/app.config.js
@apps/mobile/App.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@docs/release/android-pilot-install.md
@docs/release/v1-readiness.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder app versioning with Phase 12 pilot build truth</name>
  <read_first>
    - package.json
    - apps/mobile/package.json
    - apps/mobile/app.json
    - apps/mobile/eas.json
    - apps/mobile/app.config.js
  </read_first>
  <files>package.json, apps/mobile/package.json, apps/mobile/app.json, apps/mobile/app.config.js, docs/release/android-pilot-install.md</files>
  <action>Update app/package versioning from `0.0.0` to a Phase 12 pilot version, preferably `0.12.0`, and set an Android `versionCode`/build identifier suitable for the current pilot sequence. Keep EAS internal APK profiles intact. If adding public `extra` metadata, include only sanitized environment/API target/approved artifact label fields, never build URLs, provider ids, tokens, or credentials.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile typecheck && pnpm.cmd security:evidence</automated>
  </verify>
  <acceptance_criteria>
    - Android install no longer shows `0.0.0` for the pilot APK version.
    - Version/build policy is documented in `docs/release/android-pilot-install.md`.
    - App config still supports ignored Firebase file-variable behavior for provider-capable builds.
    - No private artifact URL or credential is committed.
  </acceptance_criteria>
  <done>Pilot APK versioning is traceable and public-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Add sanitized mobile build-info adapter and diagnostic surface</name>
  <read_first>
    - apps/mobile/App.tsx
    - apps/mobile/src/auth/AuthGate.tsx
    - apps/mobile/src/capture/CaptureApp.tsx
    - apps/mobile/src/capture/prepare-turn.test.tsx
  </read_first>
  <files>apps/mobile/src/build-info.ts, apps/mobile/src/build-info.test.ts, apps/mobile/App.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/prepare-turn.test.tsx</files>
  <action>Add a small build-info adapter that reads native app version/build from `expo-application` when available and falls back safely to Expo config/package values in tests. Surface version, build/versionCode, environment, API target, masked commit/artifact identity, and package id in a mobile diagnostic/about area reachable from the authenticated app. Keep wording operational: `Build do piloto`, `Ambiente`, `API`, `Compatibilidade`. Add tests for native values, fallback values, and masked commit/artifact output.</action>
  <verify>
    <automated>pnpm.cmd --filter @validade-zero/mobile test -- build-info prepare-turn auth-flow && pnpm.cmd --filter @validade-zero/mobile typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Mobile shows installed build truth without exposing secrets or private URLs.
    - Missing native module values degrade to `nao informado`/test-safe fallback rather than crashing.
    - Prepare-turn/device-report payload includes sanitized build info.
    - Tests prove raw long commit/artifact identifiers are masked or bounded.
  </acceptance_criteria>
  <done>Operators can read installed build truth on the phone.</done>
</task>

<task type="auto">
  <name>Task 3: Compare device build against approved staging artifact in Command Center</name>
  <read_first>
    - apps/api/src/command-center.ts
    - apps/web/src/command-center/CommandCenter.tsx
    - docs/release/v1-readiness.md
    - .planning/phases/11-mobile-visual-polish-and-emulator-validation/11-UAT.md
  </read_first>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, apps/web/src/command-center/CommandCenter.tsx, apps/web/src/command-center/command-center.test.tsx, docs/release/v1-readiness.md</files>
  <action>Add approved-staging compatibility state to device readiness: `atual`, `desatualizado`, `desconhecido`, or `incompativel`. Compare devices against the approved staging artifact label/version from public-safe config or release docs, not arbitrary `main` commits. Render `Desatualizado` as Atencao or Bloqueado according to the current UAT step risk, preserving D-11 and D-12.</action>
  <verify>
    <automated>pnpm.cmd vitest run --config vitest.config.ts --project api -- command-center && pnpm.cmd vitest run --config vitest.config.ts --project web -- command-center && pnpm.cmd security:evidence</automated>
  </verify>
  <acceptance_criteria>
    - A newer unapproved commit does not make a staging-approved APK outdated by itself.
    - An old build can sync but still shows Atencao/Bloqueado when required fields/flows are missing.
    - Command Center explains what build to install next without committing an install URL.
    - Release docs separate approved staging artifact truth from current repo commits.
  </acceptance_criteria>
  <done>Build compatibility is visible and does not drift with every commit.</done>
</task>

</tasks>

<traceability>
- P12-RELEASE-03: mobile/web release truth for installed build, environment, API target, and compatibility.
- P12-DEVICE-01: device readiness incorporates build status.
- P12-OPS-05: outdated/incompatible build blockers name next action.
- AUD-04: build evidence remains public-safe.
</traceability>

<decision_coverage>
- D-09: installed-build truth appears in mobile and Command Center.
- D-10: build truth includes version/build/env/API/masked identity without private data.
- D-11: outdated is calculated against approved staging artifact.
- D-12: old APK can be Atencao or Bloqueado based on pilot proof risk.
- D-13: app stops shipping as 0.0.0 and moves to traceable Phase 12 versioning.
- D-17: build evidence is sanitized.
</decision_coverage>

<threat_model>
| Boundary | Risk | Mitigation |
|---|---|---|
| build metadata -> public UI | Private artifact URL or token could leak. | Mask identity and omit URLs/tokens from schema/UI. |
| main commit -> APK readiness | A device could be falsely outdated by unapproved code. | Compare only against approved staging artifact metadata. |
| native module -> app boot | Missing runtime module crashes tests/Expo Go. | Adapter returns bounded fallback values. |
</threat_model>

<verification>
- `pnpm.cmd --filter @validade-zero/mobile test -- build-info prepare-turn auth-flow`
- `pnpm.cmd --filter @validade-zero/mobile typecheck`
- `pnpm.cmd vitest run --config vitest.config.ts --project api -- command-center`
- `pnpm.cmd vitest run --config vitest.config.ts --project web -- command-center`
- `pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Installed pilot build no longer reports `0.0.0`.
- Mobile and web expose sanitized version/build/environment/API truth.
- Device compatibility uses approved staging artifact truth, not every repo commit.
</success_criteria>

<output>
Create `.planning/phases/12-pilot-operations-and-device-readiness/12-03-SUMMARY.md` when done.
</output>
