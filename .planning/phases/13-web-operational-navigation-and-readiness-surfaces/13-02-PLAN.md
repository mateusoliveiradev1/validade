---
phase: 13-web-operational-navigation-and-readiness-surfaces
plan: "02"
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/web/src/command-center/CommandCenter.tsx
  - apps/web/src/command-center/OperacaoRoute.tsx
  - apps/web/src/command-center/command-center-view-model.ts
  - apps/web/src/command-center/command-center.test.tsx
autonomous: true
requirements: ["WEB-01", "WEB-02"]
user_setup: []
must_haves:
  truths:
    - "D-01: Operacao first answers `area de venda segura agora?` before any device, build, UAT, or rollout diagnostic detail."
    - "D-02/D-03: Operacao keeps a compact device-readiness strip with apto/atencao/bloqueado counts and promotes only daily-execution device blockers."
    - "D-04: Operacao does not render UAT checklist, full pilot blocker matrix, APK/update instructions, or detailed push-test history."
  artifacts:
    - path: "apps/web/src/command-center/OperacaoRoute.tsx"
      provides: "Daily operation view with verdict, central freshness, causal blockers, operational funnel, and compact device strip"
      min_lines: 80
    - path: "apps/web/src/command-center/command-center-view-model.ts"
      provides: "Daily device blocker filtering and readiness counts"
      contains: "invalid_store_or_user"
  key_links:
    - from: "apps/web/src/command-center/CommandCenter.tsx"
      to: "apps/web/src/command-center/OperacaoRoute.tsx"
      via: "activeRoute === operacao render path"
      pattern: "OperacaoRoute"
    - from: "apps/web/src/command-center/OperacaoRoute.tsx"
      to: "apps/web/src/command-center/command-center-view-model.ts"
      via: "readiness-count and daily blocker helpers"
      pattern: "device"
---

<objective>
Distill the daily operation route.

Purpose: Leadership needs the daily sales-area safety answer first, while device readiness remains visible without turning UAT/build/push diagnostics into the primary workflow.
Output: `OperacaoRoute.tsx`, compact device strip, daily blocker filtering, and route-specific tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-CONTEXT.md
@.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md
@apps/web/src/command-center/CommandCenter.tsx
@apps/web/src/command-center/command-center-view-model.ts
@apps/web/src/command-center/command-center.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the Operacao daily safety route</name>
  <files>apps/web/src/command-center/OperacaoRoute.tsx, apps/web/src/command-center/CommandCenter.tsx</files>
  <read_first>apps/web/src/command-center/CommandCenter.tsx, apps/web/src/command-center/command-center-view-model.ts, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md</read_first>
  <action>Create `OperacaoRoute.tsx` that receives `projection`, `lastClientRefreshAt`, `status`, `onRefresh`, `onOpenAparelhos`, and optional `onOpenAudit`. Move or reuse the current sales-area verdict, central snapshot, causal insight, empty state, operational funnel sections, and audit action. Add a compact device strip with copy exactly shaped like `Aparelhos: {apto} aptos, {atencao} em atencao, {bloqueado} bloqueados` and a button/link labelled `Abrir Aparelhos`. Do not render `PilotUatPanel`, `PilotBlockersPanel`, detailed device list, push-test timeline, or APK/update instructions on this route.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - `OperacaoRoute.tsx` contains `Area de venda segura agora?`.
    - `OperacaoRoute.tsx` contains `Abrir Aparelhos`.
    - Operacao tests assert `UAT Loja 18` is not present in the Operacao route.
    - Operacao tests assert `phase-12-staging-apk-120` is not present in the Operacao route.
    - Operacao tests assert push-test timeline text such as `Lembrete aceito pelo provider` is not present in the Operacao route.
  </acceptance_criteria>
  <done>Operacao route is focused on daily safety and no longer hosts rollout diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement daily blocker filtering for the device strip</name>
  <files>apps/web/src/command-center/command-center-view-model.ts, apps/web/src/command-center/OperacaoRoute.tsx</files>
  <read_first>packages/contracts/src/command-center.ts, apps/web/src/command-center/CommandCenter.tsx, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-CONTEXT.md</read_first>
  <action>In `command-center-view-model.ts`, add a helper that returns only daily-operation device blockers for codes `invalid_store_or_user`, `missing_first_central_read`, `stale_critical_sync`, `sync_conflict`, and `unsafe_shift_close`. Use it in the device strip so build-only, camera-only, and push-only rollout blockers remain compact unless they affect execution. The strip must still show counts for all devices, even when all are Apto.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - `command-center-view-model.ts` contains all five daily blocker codes listed above.
    - A fixture with only `push_required_without_push` does not create an Operacao daily-blocker row.
    - A fixture with `missing_first_central_read` creates an Operacao blocker row with `Causa:` and `Agora:`.
  </acceptance_criteria>
  <done>Device readiness is permanently visible but weighted correctly in daily operation.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Operacao tests around presence and absence</name>
  <files>apps/web/src/command-center/command-center.test.tsx</files>
  <read_first>apps/web/src/command-center/command-center.test.tsx, apps/web/e2e/fixtures/v1-readiness.ts, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md</read_first>
  <action>Replace the old all-in-one daily test with an Operacao-specific test. Assert the sales-area verdict, central snapshot, `Por que venceu`, risk funnel order, compact device strip, and `Abrir Aparelhos`. Add negative assertions for `UAT Loja 18`, `Bloqueios do piloto`, `phase-12-staging-apk-120`, `Enviar teste seguro`, and push-test timeline labels.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Test name mentions Operacao or daily operation.
    - Test checks `Por que venceu` appears before operational funnel sections.
    - Test checks no UAT/build/push diagnostic detail appears in Operacao.
  </acceptance_criteria>
  <done>Tests prove the first route answers daily safety without hidden diagnostic clutter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Projection to daily safety UI | UI derives what is daily-operational versus rollout-diagnostic from trusted projection fields. |
| Leadership action to route change | `Abrir Aparelhos` changes local route only; it must not trigger operational resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Spoofing | Operacao device strip | mitigate | Do not label foreground/sync timestamps as live presence; copy only reports readiness counts and next actions. |
| T-13-09 | Information disclosure | Operacao route | mitigate | Negative tests must prove UAT/build/push details moved out of the first route. |
| T-13-SC | Tampering | npm package surface | mitigate | Do not add new UI packages or chart libraries; use existing shadcn/lucide/Tailwind patterns. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web typecheck`
</verification>

<success_criteria>
- Operacao first scan answers sales-area safety.
- Compact device readiness is always visible.
- Detailed UAT/build/push diagnostics are absent from Operacao.
- Daily blocker filtering preserves real operational blockers.
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-02-SUMMARY.md`.
</output>
