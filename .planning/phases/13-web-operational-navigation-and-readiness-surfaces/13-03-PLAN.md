---
phase: 13-web-operational-navigation-and-readiness-surfaces
plan: "03"
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - apps/web/src/command-center/CommandCenter.tsx
  - apps/web/src/command-center/AparelhosRoute.tsx
  - apps/web/src/command-center/command-center-view-model.ts
  - apps/web/src/command-center/command-center.test.tsx
autonomous: true
requirements: ["WEB-03"]
user_setup: []
must_haves:
  truths:
    - "D-09: Aparelhos leads with a readiness-ordered device list: Bloqueado, Atencao, Apto."
    - "D-10: Aparelhos shows build compatibility only as device summary and leaves update instructions to Atualizacoes."
    - "D-13: Safe push-test action lives in Aparelhos and remains diagnostic only."
  artifacts:
    - path: "apps/web/src/command-center/AparelhosRoute.tsx"
      provides: "Dedicated device readiness view with safe push-test action"
      min_lines: 100
    - path: "apps/web/src/command-center/command-center-view-model.ts"
      provides: "Readiness sorting and safe push-test disable reasons"
      contains: "bloqueado"
  key_links:
    - from: "apps/web/src/command-center/AparelhosRoute.tsx"
      to: "apps/web/src/command-center/command-center-client.ts"
      via: "onSendSafePushTest callback from shared host"
      pattern: "onSendSafePushTest"
    - from: "apps/web/src/command-center/AparelhosRoute.tsx"
      to: "packages/contracts/src/command-center.ts"
      via: "CommandCenterProjection device fields"
      pattern: "CommandCenterProjection"
---

<objective>
Build the dedicated Aparelhos route.

Purpose: Device readiness is permanent operational truth, but its detailed push/camera/build state belongs in a focused view rather than the daily Operacao first scan.
Output: `AparelhosRoute.tsx`, readiness sorting, safe push-test action, and device-focused tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-CONTEXT.md
@.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md
@apps/web/src/command-center/CommandCenter.tsx
@apps/web/src/command-center/command-center-view-model.ts
@apps/web/src/command-center/command-center.test.tsx
@packages/contracts/src/command-center.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Aparelhos route with readiness-ordered list</name>
  <files>apps/web/src/command-center/AparelhosRoute.tsx, apps/web/src/command-center/CommandCenter.tsx</files>
  <read_first>apps/web/src/command-center/CommandCenter.tsx, packages/contracts/src/command-center.ts, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md</read_first>
  <action>Create `AparelhosRoute.tsx` that renders `projection.devices` sorted by readiness rank `bloqueado`, `atencao`, `apto`, then `updatedAt` descending. Each device row must show verdict badge, device label, masked device id, active user label, `Causa:` for blockers or readiness summary, `Agora:` next action, last central read, last sync, push state, camera state, build compatibility summary, and updated timestamp. Do not render APK/QR/manual install instructions in this route; show only a route hint to `Atualizacoes` for update instructions if needed.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - `AparelhosRoute.tsx` contains route heading `Aparelhos`.
    - Device rows show `Causa:` and `Agora:`.
    - Device rows show masked id text from `deviceIdMasked`.
    - Tests prove `bloqueado` devices render before `atencao`, and `atencao` before `apto`.
  </acceptance_criteria>
  <done>Aparelhos view owns detailed per-device readiness and next action.</done>
</task>

<task type="auto">
  <name>Task 2: Keep safe push test diagnostic and capability-gated</name>
  <files>apps/web/src/command-center/AparelhosRoute.tsx, apps/web/src/command-center/CommandCenter.tsx, apps/web/src/command-center/command-center-view-model.ts</files>
  <read_first>apps/web/src/command-center/CommandCenter.tsx, apps/web/src/command-center/command-center-client.ts, apps/web/src/command-center/command-center.test.tsx, docs/operations/push-alerts.md</read_first>
  <action>Move the safe push-test button and timeline to Aparelhos. The button label stays `Enviar teste seguro`. Disable it when `canSendPilotPushTest` is false or the device lacks confirmed store/user/central-read scope. Disabled copy must include `Teste seguro exige aparelho autorizado, loja confirmada e leitura central recente.` Route copy must say the test does not resolve task state, does not prove area segura, and validates only the reminder channel. Preserve `sendSafePushTest({ storeId, deviceIdMasked, deviceLabel })` and timeline append behavior.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Aparelhos tests click `Enviar teste seguro` and assert `sendSafePushTest` receives `storeId`, `deviceIdMasked`, and `deviceLabel`.
    - Aparelhos tests assert returned timeline text appears after the click.
    - Aparelhos tests assert diagnostic copy includes `nao resolve tarefa` and `nao prova area segura`.
    - Operacao tests still assert `Enviar teste seguro` is absent.
  </acceptance_criteria>
  <done>Safe push test works only from Aparelhos and stays diagnostic-only.</done>
</task>

<task type="auto">
  <name>Task 3: Add Aparelhos route tests</name>
  <files>apps/web/src/command-center/command-center.test.tsx</files>
  <read_first>apps/web/src/command-center/command-center.test.tsx, apps/web/e2e/fixtures/v1-readiness.ts, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md</read_first>
  <action>Add Aparelhos-specific tests that render the shared Command Center host with `activeRoute="aparelhos"` or the route component directly. Assert device readiness order, last central read/sync, push/camera state, build compatibility summary, masked id, safe push-test disabled/enabled behavior, and absence of APK/QR install instructions.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Test name mentions `Aparelhos`.
    - Test checks `APK aprovado` can appear as compatibility summary but manual install instructions do not.
    - Test checks `UAT Loja 18` is not rendered in Aparelhos.
  </acceptance_criteria>
  <done>Tests prove WEB-03 without mixing update or validation responsibilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device readiness projection to UI | Device rows display public-safe state from central projection. |
| Web action to `/pilot/push-tests` | Push-test send crosses from browser to API and must remain authorized and diagnostic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information disclosure | Aparelhos device rows | mitigate | Render only `deviceIdMasked`, public labels, and bounded state; never raw ids, push tokens, provider tickets, or device serials. |
| T-13-05 | Elevation of privilege | Safe push-test button | mitigate | Keep send action disabled unless `canSendPilotPushTest` is true and device scope is confirmed. |
| T-13-10 | Repudiation | Safe push-test copy | mitigate | Copy must state the action is diagnostic and not task execution proof, preserving audit meaning. |
| T-13-SC | Tampering | npm package surface | mitigate | Do not add third-party device tables or timeline packages; use existing components. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web typecheck`
</verification>

<success_criteria>
- Aparelhos shows all per-device readiness fields required by WEB-03.
- Safe push-test action exists only here and remains diagnostic.
- No update instructions or UAT checklist compete with device readiness.
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-03-SUMMARY.md`.
</output>
