---
phase: 13-web-operational-navigation-and-readiness-surfaces
plan: "05"
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - apps/web/src/command-center/CommandCenter.tsx
  - apps/web/src/command-center/ValidacaoRoute.tsx
  - apps/web/src/command-center/command-center-view-model.ts
  - apps/web/src/command-center/command-center.test.tsx
  - apps/web/e2e/v1-readiness.spec.ts
  - apps/web/e2e/fixtures/v1-readiness.ts
autonomous: true
requirements: ["WEB-01", "WEB-02", "WEB-03", "WEB-04", "WEB-05"]
user_setup: []
must_haves:
  truths:
    - "D-14/D-15: Validacao is a rollout validation room with Loja 18 checklist, external gates, sanitized evidence, and next actions."
    - "D-16: Validacao shows an explicit Go/No-Go verdict: Go, No-Go, or Aguardando prova externa, backed by visible gates."
    - "D-17: Evidence remains public-safe: labels, states, timestamps, masked device references, and safe evidence labels only."
    - "D-18: Validacao references actions in Aparelhos, Atualizacoes, and Operacao instead of duplicating those actions."
    - "WEB-01..WEB-05 are proven by route-specific unit tests and Playwright navigation coverage."
  artifacts:
    - path: "apps/web/src/command-center/ValidacaoRoute.tsx"
      provides: "Dedicated Loja 18 validation and Go/No-Go route"
      min_lines: 100
    - path: "apps/web/e2e/v1-readiness.spec.ts"
      provides: "End-to-end proof of Phase 13 route separation"
      contains: "Operacao"
    - path: "apps/web/e2e/fixtures/v1-readiness.ts"
      provides: "Public-safe fixture data for route split"
      contains: "Provider bloqueado externamente"
  key_links:
    - from: "apps/web/src/command-center/ValidacaoRoute.tsx"
      to: "apps/web/src/command-center/AparelhosRoute.tsx"
      via: "cross-route next-action reference for push"
      pattern: "Aparelhos"
    - from: "apps/web/src/command-center/ValidacaoRoute.tsx"
      to: "apps/web/src/command-center/AtualizacoesRoute.tsx"
      via: "cross-route next-action reference for update/build"
      pattern: "Atualizacoes"
    - from: "apps/web/e2e/v1-readiness.spec.ts"
      to: "apps/web/e2e/fixtures/v1-readiness.ts"
      via: "public-safe route fixture"
      pattern: "installWebFixture"
---

<objective>
Build Validacao / Go-No-Go and finish route-level verification.

Purpose: Loja 18 rollout proof must be explicit and public-safe without competing with daily Operacao. The final plan also updates E2E so Phase 13 is verified as a navigable web split, not only component extraction.
Output: `ValidacaoRoute.tsx`, Go/No-Go synthesis helpers, route-specific tests, and updated Playwright readiness coverage.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-CONTEXT.md
@.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@apps/web/src/command-center/CommandCenter.tsx
@apps/web/src/command-center/command-center-view-model.ts
@apps/web/src/command-center/command-center.test.tsx
@apps/web/e2e/v1-readiness.spec.ts
@apps/web/e2e/fixtures/v1-readiness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Validacao route with explicit Go/No-Go</name>
  <files>apps/web/src/command-center/ValidacaoRoute.tsx, apps/web/src/command-center/command-center-view-model.ts, apps/web/src/command-center/CommandCenter.tsx</files>
  <read_first>apps/web/src/command-center/CommandCenter.tsx, packages/contracts/src/command-center.ts, .planning/milestones/v1.0-MILESTONE-AUDIT.md, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md</read_first>
  <action>Create `ValidacaoRoute.tsx` that renders `Validacao` as the rollout proof room. Add a helper that derives verdict labels `Go`, `No-Go`, or `Aguardando prova externa` from `projection.pilotUat.steps`, `projection.pilotBlockers`, and device/update gate state. Render Loja 18 checklist, external gates, blocker cause, `Agora:` next action, evidence reference labels, timestamps, and masked device references when available. Add route references such as `Resolver push em Aparelhos`, `Resolver atualizacao em Atualizacoes`, and `Revisar operacao diaria em Operacao`; do not duplicate the safe push-test button or update link action here.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - `ValidacaoRoute.tsx` contains labels `Go`, `No-Go`, and `Aguardando prova externa`.
    - Tests assert `UAT Loja 18`, `Provider bloqueado externamente`, and `Produto real da Loja 18` are visible in Validacao.
    - Tests assert `Enviar teste seguro` is absent in Validacao.
    - Tests assert body text does not match `/token|secret|password|ExpoPushToken|buildUrl|rawDeviceId/i`.
  </acceptance_criteria>
  <done>Validacao owns public-safe UAT and Go/No-Go proof without duplicating daily/device/update actions.</done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright route journey and fixture coverage</name>
  <files>apps/web/e2e/v1-readiness.spec.ts, apps/web/e2e/fixtures/v1-readiness.ts</files>
  <read_first>apps/web/e2e/v1-readiness.spec.ts, apps/web/e2e/fixtures/v1-readiness.ts, apps/web/src/shell/AppShell.tsx, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-UI-SPEC.md</read_first>
  <action>Rewrite the first Playwright test so it starts in Operacao, asserts daily safety and operational funnel order, and asserts UAT/build/push detailed diagnostics are absent. Add navigation steps to Aparelhos, Atualizacoes, and Validacao through the side nav or mobile sheet as appropriate. Assert each route's focal truth appears in the right place: per-device readiness in Aparelhos, approved artifact/installed versions/manual fallback in Atualizacoes, and Go/No-Go/UAT/external blockers in Validacao. Keep the admin-only test updated so operational routes are disabled when `canReadCommandCenter` is false.</action>
  <verify>cmd /c pnpm.cmd test:e2e:web</verify>
  <acceptance_criteria>
    - `apps/web/e2e/v1-readiness.spec.ts` no longer expects `UAT Loja 18` on the first Operacao screen.
    - E2E asserts navigation buttons named `Operacao`, `Aparelhos`, `Atualizacoes`, and `Validacao`.
    - E2E asserts `phase-12-staging-apk-120` appears only after navigating to Atualizacoes.
    - E2E asserts `Provider push sem prova atual` appears in Validacao, not as first-screen daily content.
  </acceptance_criteria>
  <done>Playwright proves Phase 13 route separation and public-safe navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Run final web and repository gates</name>
  <files>apps/web/src/command-center/command-center.test.tsx, apps/web/e2e/v1-readiness.spec.ts</files>
  <read_first>package.json, apps/web/package.json, .planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-VALIDATION.md</read_first>
  <action>Run focused web tests and typecheck, then Playwright web E2E. If those pass, run the full repo gate `cmd /c pnpm.cmd check`. Fix any Phase 13 regressions before writing the summary. If mobile E2E remains blocked by no connected device only inside a broader script, record the exact command and blocker honestly; do not convert an external Android blocker into a repo failure or false pass.</action>
  <verify>cmd /c pnpm.cmd check</verify>
  <acceptance_criteria>
    - `cmd /c pnpm.cmd --filter @validade-zero/web test` exits 0.
    - `cmd /c pnpm.cmd --filter @validade-zero/web typecheck` exits 0.
    - `cmd /c pnpm.cmd test:e2e:web` exits 0.
    - `cmd /c pnpm.cmd check` exits 0, or any external-device blocker is recorded with the exact failing command and not claimed as green.
  </acceptance_criteria>
  <done>Phase 13 implementation is ready for formal verification with route-specific evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT/blocker projection to web validation | Validation route displays rollout proof without storing or exposing raw evidence. |
| E2E fixtures to public repo | Fixture text is committed and must remain fictitious/public-safe. |
| Cross-route references | Validation points users to other routes without triggering unauthorized actions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Information disclosure | Validacao evidence rows | mitigate | Render only safe labels, timestamps, masked ids, and evidence reference labels; tests deny token/raw id/build URL patterns. |
| T-13-08 | Repudiation | Go/No-Go verdict | mitigate | Verdict must be backed by visible gates and blockers so external proof is not silently treated as passed. |
| T-13-12 | Elevation of privilege | Cross-route actions | mitigate | Cross-route references only change route; push and update actions remain gated in their owning routes. |
| T-13-SC | Tampering | npm package surface | mitigate | No new third-party route, chart, QR, or validation package is required. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web typecheck`
- [ ] `cmd /c pnpm.cmd test:e2e:web`
- [ ] `cmd /c pnpm.cmd check`
</verification>

<success_criteria>
- Validacao carries Loja 18 UAT and Go/No-Go proof.
- Route-specific tests prove Operacao, Aparelhos, Atualizacoes, and Validacao own the correct truths.
- Playwright confirms durable navigation and first-screen decluttering.
- Public-safe evidence and update-link boundaries hold.
</success_criteria>

<output>
After completion, create `.planning/phases/13-web-operational-navigation-and-readiness-surfaces/13-05-SUMMARY.md`.
</output>
