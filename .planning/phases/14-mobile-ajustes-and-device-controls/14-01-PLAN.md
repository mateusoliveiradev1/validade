---
phase: 14-mobile-ajustes-and-device-controls
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/App.tsx
  - apps/mobile/src/auth/AuthGate.tsx
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/AjustesScreen.tsx
  - apps/mobile/src/auth/auth-flow.test.tsx
  - apps/mobile/src/capture/mobile-release-journeys.test.tsx
autonomous: true
requirements: ["SET-01"]
user_setup: []
must_haves:
  truths:
    - "D-01: Ajustes opens from the authenticated session bar beside store, role, and session identity and replaces scattered Privacidade/Sair quick actions."
    - "D-02: Opening Ajustes preserves the current operational route stack and returning from Ajustes restores the exact previous route."
    - "D-03/D-04: The first Ajustes screen is a calm device readiness summary using existing mobile primitives, not a technical debug panel."
    - "SET-01 is proven by tests that open Ajustes from authenticated mobile state without losing store, role, session, or current route."
  artifacts:
    - path: "apps/mobile/src/capture/AjustesScreen.tsx"
      provides: "Initial Ajustes screen shell and readiness sections"
      contains: "Ajustes"
    - path: "apps/mobile/src/capture/CaptureApp.tsx"
      provides: "Ajustes route stack integration and Android back return"
      contains: "settings"
    - path: "apps/mobile/src/auth/AuthGate.tsx"
      provides: "Authenticated session callbacks without duplicate Privacidade/Sair quick actions"
      contains: "openPrivacyCenter"
  key_links:
    - from: "apps/mobile/App.tsx"
      to: "apps/mobile/src/capture/CaptureApp.tsx"
      via: "session context and auth/session callbacks"
      pattern: "session={session}"
    - from: "apps/mobile/src/capture/CaptureApp.tsx"
      to: "apps/mobile/src/capture/AjustesScreen.tsx"
      via: "currentRoute.name === \"settings\""
      pattern: "AjustesScreen"
---

<objective>
Create the mobile Ajustes route foundation and authenticated entry point.

Purpose: Operators need one calm control home for this device without losing the task, lot, shift-close, or Hoje context they were using. This plan creates the screen shell, route stack behavior, and entry action only; later plans fill push, sync, build, account, privacy, and sign-out controls.
Output: Ajustes opens from the mobile authenticated shell, shows session/device readiness placeholders, and returns to the exact previous operational route.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-CONTEXT.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-RESEARCH.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-PATTERNS.md
@apps/mobile/App.tsx
@apps/mobile/src/auth/AuthGate.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/capture/mobile-status.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor authenticated shell callbacks for Ajustes</name>
  <files>apps/mobile/App.tsx, apps/mobile/src/auth/AuthGate.tsx</files>
  <read_first>apps/mobile/App.tsx, apps/mobile/src/auth/AuthGate.tsx, apps/mobile/src/auth/auth-flow.test.tsx, .planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md</read_first>
  <action>Keep `AuthGate` responsible for session validation, privacy opening, and `authClient.logout()`, but stop rendering separate visible `Privacidade` and `Sair` quick-action buttons in the ready shell. Expose ready-session callbacks to the authenticated child through a typed object containing `openPrivacyCenter()` and `requestLogout()`. In `App.tsx`, pass `session`, `authClient`, and those callbacks into `AuthenticatedCaptureApp`, then into `CaptureApp`. Preserve existing login, blocked, expired, recovery, privacy, no-permission, and logout behavior.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - `apps/mobile/src/auth/AuthGate.tsx` still calls `authClient.logout()` in its logout path.
    - `apps/mobile/src/auth/AuthGate.tsx` still renders `PrivacyCenterScreen` when `openPrivacyCenter()` is called.
    - The authenticated ready shell no longer renders visible session actions labeled `Privacidade` and `Sair` side by side.
    - `apps/mobile/App.tsx` passes the active `session` and auth callbacks into `CaptureApp`.
  </acceptance_criteria>
  <done>Auth state remains in AuthGate while the visible authenticated operational shell can route into Ajustes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Ajustes route and session-bar entry in CaptureApp</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/AjustesScreen.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/capture-theme.ts, apps/mobile/src/capture/mobile-status.ts, .planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md</read_first>
  <action>Create `AjustesScreen.tsx` with `ScreenHeader` title `Ajustes`, body `Confira se este aparelho, a conta e a sincronizacao estao prontos antes de voltar para a operacao.`, and an initial readiness summary headed `Este aparelho esta pronto para operar?`. Add a `settings` route to `CaptureRoute`. Render an authenticated session bar from `CaptureApp` that shows store name, actor label, role label, and a single `Ajustes` action with accessibility label `Abrir Ajustes do aparelho`. The action must push `{ name: \"settings\" }` onto `routeStack`; `AjustesScreen` receives `onBack={goBack}`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck</verify>
  <acceptance_criteria>
    - `apps/mobile/src/capture/CaptureApp.tsx` contains a `settings` route variant in `CaptureRoute`.
    - The visible authenticated session action label is `Ajustes`.
    - The session action has accessibility label `Abrir Ajustes do aparelho`.
    - `AjustesScreen.tsx` renders the exact title `Ajustes`.
    - `AjustesScreen.tsx` renders `Este aparelho esta pronto para operar?`.
  </acceptance_criteria>
  <done>Ajustes opens as a real route with the approved UI contract skeleton.</done>
</task>

<task type="auto">
  <name>Task 3: Prove route preservation and Android back return</name>
  <files>apps/mobile/src/capture/mobile-release-journeys.test.tsx, apps/mobile/src/auth/auth-flow.test.tsx</files>
  <read_first>apps/mobile/src/capture/mobile-release-journeys.test.tsx, apps/mobile/src/auth/auth-flow.test.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/push-alerts.test.tsx</read_first>
  <action>Add mobile tests that open Ajustes from Hoje and from an existing task-resolution route. Use the existing `react-test-renderer` and fake repository patterns. Assert that the rendered text contains `Ajustes`, then press the back action or invoke the registered Android back handler and assert the previous route content is restored: `Hoje` for the Hoje case and `Acao exigida:` plus the task label for the task-resolution case. Update auth-flow assertions so privacy is still reachable through the new callback path and logout still returns to `Entrar no Validade Zero`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - A test opens Ajustes from Hoje and returns to a render containing `Hoje`.
    - A test opens Ajustes from task resolution and returns to a render containing `Acao exigida:`.
    - A test verifies the `Abrir Ajustes do aparelho` action exists after authenticated session.
    - Existing no-permission, blocked, expired, recovery, and login tests still pass.
  </acceptance_criteria>
  <done>SET-01 route preservation is covered by focused mobile tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AuthGate session state to CaptureApp shell | Server-owned session facts move into the visible mobile shell and must not be editable by the operator. |
| Ajustes route to operational route stack | A control screen can be opened while task/lot/shift state remains mounted and restorable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of privilege | Session bar and role display | mitigate | Display active session facts only from `SessionContextResponse`; do not introduce store/role switch controls. |
| T-14-02 | Tampering | Route stack preservation | mitigate | Add route-stack tests from Hoje and task resolution so Ajustes cannot reset operational work silently. |
| T-14-SC | Tampering | npm package surface | mitigate | Do not add new UI, routing, icon, or state-management packages in this plan. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Ajustes opens from the authenticated mobile shell.
- Store, actor, role, and session identity stay visible and read-only.
- Returning from Ajustes restores the exact previous operational route.
- No visible duplicate `Privacidade` and `Sair` session quick actions remain outside Ajustes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-ajustes-and-device-controls/14-01-SUMMARY.md`.
</output>
