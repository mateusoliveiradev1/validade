---
phase: 14-mobile-ajustes-and-device-controls
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/mobile/src/capture/AjustesScreen.tsx
  - apps/mobile/src/capture/ajustes-readiness.ts
  - apps/mobile/src/capture/alert-channel.ts
  - apps/mobile/src/capture/today-copy.ts
  - apps/mobile/src/capture/TodayScreen.tsx
  - apps/mobile/src/capture/push-alerts.test.tsx
  - apps/mobile/src/capture/ajustes-screen.test.tsx
autonomous: true
requirements: ["SET-02"]
user_setup: []
must_haves:
  truths:
    - "D-05: Ajustes shows push/reminder state, activation, disable path, and safe test while preserving that push never resolves tasks or proves physical execution."
    - "D-06: Disabling alerts applies only to this device and copy says tasks remain active in Hoje."
    - "D-07: The mobile safe push test proves only this device path, not global provider delivery or physical execution."
    - "D-08: Degraded push is Atencao by default and blocking only for proof flows that explicitly require remote push."
  artifacts:
    - path: "apps/mobile/src/capture/AjustesScreen.tsx"
      provides: "Push e lembretes readiness card and controls"
      contains: "Push e lembretes"
    - path: "apps/mobile/src/capture/ajustes-readiness.ts"
      provides: "Pure push verdict and copy helpers"
      contains: "push"
    - path: "apps/mobile/src/capture/alert-channel.ts"
      provides: "This-device safe test support without task execution payload"
      contains: "safe"
  key_links:
    - from: "apps/mobile/src/capture/AjustesScreen.tsx"
      to: "apps/mobile/src/capture/repository.ts"
      via: "loadAlertChannelState/registerAlertDevice"
      pattern: "registerAlertDevice"
    - from: "apps/mobile/src/capture/AjustesScreen.tsx"
      to: "apps/mobile/src/capture/alert-channel.ts"
      via: "PushAlertChannel permission/token/local scheduling"
      pattern: "PushAlertChannel"
---

<objective>
Move push and reminder controls into Ajustes with diagnostic-only semantics.

Purpose: Operators need to activate, disable, and test alerts for this device, but push must remain a reminder/cobranca path rather than task execution or sales-area proof.
Output: Ajustes push card with state, activation, disable, safe test, sanitized feedback, and tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-mobile-ajustes-and-device-controls/14-CONTEXT.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-RESEARCH.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-PATTERNS.md
@apps/mobile/src/capture/AjustesScreen.tsx
@apps/mobile/src/capture/TodayScreen.tsx
@apps/mobile/src/capture/alert-channel.ts
@apps/mobile/src/capture/repository.ts
@apps/mobile/src/capture/today-copy.ts
@apps/mobile/src/capture/push-alerts.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract reusable push state helpers for Ajustes</name>
  <files>apps/mobile/src/capture/ajustes-readiness.ts, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/TodayScreen.tsx</files>
  <read_first>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/mobile-status.ts, .planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md</read_first>
  <action>Create `ajustes-readiness.ts` with pure helpers that map `AlertChannelState` and stored `DevicePushRegistrationCommand["permissionStatus"]` to Ajustes labels `Apto`, `Atencao`, or `Bloqueado`, card body copy, and next-action labels. Move or export the existing provider-error sanitization used by `operatorSafePushFeedback()` so both Today and Ajustes can reuse it. Add copy constants for `Push e lembretes`, `Alertas ajudam a cobrar, mas nao resolvem tarefas nem provam a area de venda.`, `Ativar alertas do turno`, `Enviar teste neste aparelho`, and `Desativar neste aparelho`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck</verify>
  <acceptance_criteria>
    - `apps/mobile/src/capture/ajustes-readiness.ts` exports a push helper that returns the labels `Apto`, `Atencao`, and `Bloqueado`.
    - Shared sanitization still maps Firebase/native module errors to operator-safe copy and hides `googleServicesFile`.
    - `TodayScreen.tsx` still renders existing push copy after the helper extraction.
  </acceptance_criteria>
  <done>Push readiness vocabulary is reusable by Ajustes without weakening Today.</done>
</task>

<task type="auto">
  <name>Task 2: Add activation, disable, and this-device safe test controls</name>
  <files>apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/alert-channel.ts, apps/mobile/src/capture/repository.ts</files>
  <read_first>apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/alert-channel.ts, apps/mobile/src/capture/repository.ts, packages/contracts/src/alerts.ts, apps/mobile/src/capture/push-alerts.test.tsx</read_first>
  <action>In `AjustesScreen`, load `repository.loadAlertChannelState()` and current permission from `PushAlertChannel`. Add actions that reuse the existing activation flow: request permission, attempt Expo token, register `permissionStatus` as `granted`, `local_only`, `denied`, or `unavailable`, and refresh the card. Add `Desativar neste aparelho` by registering the same device with `permissionStatus: \"denied\"` or a locally disabled status supported by existing contracts; if the contract cannot represent disabled separately, use `denied` and visible copy saying this device stops receiving reminders. Add a this-device safe test that schedules or simulates a safe diagnostic notification with no task/lote data and records only bounded visible feedback such as `Teste enviado neste aparelho` or `Teste local indisponivel`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Ajustes renders `Push e lembretes`.
    - Pressing `Ativar alertas do turno` calls `requestPermission()` and `registerAlertDevice()`.
    - Pressing `Desativar neste aparelho` updates only this-device registration and rendered copy contains `As tarefas continuam ativas em Hoje`.
    - Pressing `Enviar teste neste aparelho` does not call `repository.resolveTodayTask`.
    - Rendered safe-test copy contains `prova apenas este aparelho` or equivalent approved UI-SPEC wording.
  </acceptance_criteria>
  <done>Push management lives in Ajustes and remains diagnostic-only.</done>
</task>

<task type="auto">
  <name>Task 3: Cover push controls and sensitive-copy denial</name>
  <files>apps/mobile/src/capture/ajustes-screen.test.tsx, apps/mobile/src/capture/push-alerts.test.tsx</files>
  <read_first>apps/mobile/src/capture/push-alerts.test.tsx, apps/mobile/src/capture/alert-channel.ts, apps/mobile/src/capture/AjustesScreen.tsx, packages/contracts/src/alerts.test.ts</read_first>
  <action>Add focused tests for not-requested, active, local-only, denied, unavailable, and failed push states in Ajustes. Assert degraded states are rendered as `Atencao` unless the test fixture explicitly models a validation-only remote-push proof requirement. Add denylist assertions that rendered Ajustes text does not contain `ExpoPushToken`, `googleServicesFile`, `firebase`, `token`, `secret`, `providerTicket`, or `rawDeviceId`. Keep existing `push-alerts.test.tsx` green after extraction.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Tests cover `not_requested`, `active`, `local_only`, `denied`, `unavailable`, and `failed`.
    - Tests assert push degraded state defaults to `Atencao`.
    - Tests assert Ajustes text does not match `/ExpoPushToken|googleServicesFile|firebase|token|secret|providerTicket|rawDeviceId/i`.
    - Existing Hoje push tests still pass.
  </acceptance_criteria>
  <done>SET-02 is covered with operator-safe copy and no task-resolution side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native notification module to operator UI | Native/provider errors may include technical or sensitive strings and must be sanitized. |
| Push controls to task state | Push activation/test/disable must not mutate task business resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information disclosure | Push feedback copy | mitigate | Reuse sanitization and add denylist tests for token/provider/native config text. |
| T-14-04 | Tampering | Safe push test | mitigate | Test action must not call task resolution APIs and must use safe content without task/lote identifiers. |
| T-14-SC | Tampering | npm package surface | mitigate | Do not add new push, notification, or analytics packages in this plan. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Ajustes shows push/reminder state and next action.
- Operators can activate and disable this-device reminders.
- Safe push test is this-device diagnostic only.
- Push copy never says a task is resolved or the sales area is safe.
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-ajustes-and-device-controls/14-02-SUMMARY.md`.
</output>

