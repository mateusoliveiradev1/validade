---
phase: 14-mobile-ajustes-and-device-controls
plan: "03"
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - apps/mobile/src/capture/AjustesScreen.tsx
  - apps/mobile/src/capture/ajustes-readiness.ts
  - apps/mobile/src/capture/offline-sync-ui.tsx
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - apps/mobile/src/capture/offline-sync-ui.test.tsx
  - apps/mobile/src/capture/ajustes-screen.test.tsx
autonomous: true
requirements: ["SET-03"]
user_setup: []
must_haves:
  truths:
    - "D-09: Ajustes leads sync with last central read, last sync/send attempt, pending count, conflict count, and whether current state blocks safe close."
    - "D-10: Ajustes owns retry, conflict review, and destructive discard with explicit reason using existing mobile conflict rules."
    - "D-11: Ajustes separates ultima leitura central from ultima sync; neither substitutes for the other."
    - "D-12: Safe close is blocked by critical sync conflict, critical local action pending central confirmation, or missing/stale central read; non-critical pending sync stays visible without automatic safe-close block."
  artifacts:
    - path: "apps/mobile/src/capture/AjustesScreen.tsx"
      provides: "Sync health readiness card and controls"
      contains: "Sincronizacao"
    - path: "apps/mobile/src/capture/ajustes-readiness.ts"
      provides: "Safe-close blocker derivation for sync and central-read state"
      contains: "safeClose"
    - path: "apps/mobile/src/capture/offline-sync-ui.tsx"
      provides: "Reusable conflict review/discard UI"
      contains: "SyncConflictPanel"
  key_links:
    - from: "apps/mobile/src/capture/AjustesScreen.tsx"
      to: "apps/mobile/src/capture/repository.ts"
      via: "loadOfflineCacheStatus/listSyncQueue/loadSyncConflict/resolveSyncConflict"
      pattern: "listSyncQueue"
    - from: "apps/mobile/src/capture/AjustesScreen.tsx"
      to: "apps/mobile/src/capture/sync-engine.ts"
      via: "syncPendingCommands({ manual: true })"
      pattern: "syncPendingCommands"
---

<objective>
Add sync health and safe-close blocker controls to Ajustes.

Purpose: Operators need to understand whether this device has central read truth, pending local work, conflicts, and safe-close blockers without treating transport sync as physical or central business proof.
Output: Ajustes sync card with separate central-read and sync-send rows, retry, conflict review/discard, blocker statement, and tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-mobile-ajustes-and-device-controls/14-CONTEXT.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-RESEARCH.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-PATTERNS.md
@apps/mobile/src/capture/AjustesScreen.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/offline-sync-ui.tsx
@apps/mobile/src/capture/repository.ts
@apps/mobile/src/capture/sync-engine.ts
@apps/mobile/src/capture/today-screen.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive sync readiness and safe-close blocker status</name>
  <files>apps/mobile/src/capture/ajustes-readiness.ts, apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/CaptureApp.tsx</files>
  <read_first>apps/mobile/src/capture/ajustes-readiness.ts, apps/mobile/src/capture/CaptureApp.tsx, packages/contracts/src/sync.ts, packages/contracts/src/capture.ts, .planning/phases/14-mobile-ajustes-and-device-controls/14-UI-SPEC.md</read_first>
  <action>Add pure helper logic that accepts `PrepareTurnCacheStatus | null`, prepare-turn source, `OfflineCacheStatus`, and `SyncQueueSummary` and returns readiness label, body, `safeCloseBlocked` boolean, blocker reason, last central read label, last sync/send label, pending count, and conflict count. Treat critical conflict, `hasCriticalConflict`, critical pending commands, local-cache-only prepare source, missing central read, and stale central read as blockers. Format missing labels as `Sem leitura central confirmada` and `Sem envio confirmado`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Helper output includes exact labels `Ultima leitura central` and `Ultima sincronizacao enviada`.
    - Helper returns `safeCloseBlocked: true` for `hasCriticalConflict: true`.
    - Helper returns `safeCloseBlocked: true` when prepare-turn source is `local_cache`.
    - Helper returns non-blocking attention for non-critical pending sync when central read is current.
  </acceptance_criteria>
  <done>Sync readiness is calculated centrally before rendering controls.</done>
</task>

<task type="auto">
  <name>Task 2: Render sync card, retry, and conflict review in Ajustes</name>
  <files>apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/offline-sync-ui.tsx</files>
  <read_first>apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/offline-sync-ui.tsx, apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/repository.ts</read_first>
  <action>In `AjustesScreen`, load offline cache and sync queue on open and after sync/conflict actions. Render a `Sincronizacao` card with the two separate rows `Ultima leitura central` and `Ultima sincronizacao enviada`, pending/conflict counts, and either `Este estado nao bloqueia o fechamento seguro por sync.` or `Este estado bloqueia fechamento seguro ate a central confirmar ou o conflito ser resolvido.` Reuse `SyncQueueSummary` for queue rows and `SyncConflictPanel` for conflict review. Wire `Sincronizar pendencias` to `syncEngine.syncPendingCommands({ manual: true })`, and wire `Revisar conflito` to `repository.loadSyncConflict()` plus `repository.resolveSyncConflict()`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Ajustes renders `Sincronizacao`.
    - Ajustes renders `Ultima leitura central`.
    - Ajustes renders `Ultima sincronizacao enviada`.
    - Pressing `Sincronizar pendencias` calls `syncPendingCommands({ manual: true })`.
    - Conflict review reuses `Motivo para descartar a acao offline` and keeps discard disabled until the reason is non-empty.
  </acceptance_criteria>
  <done>Operators can retry sync and resolve conflicts from Ajustes without weaker shortcuts.</done>
</task>

<task type="auto">
  <name>Task 3: Cover sync blocker and conflict behavior</name>
  <files>apps/mobile/src/capture/ajustes-screen.test.tsx, apps/mobile/src/capture/offline-sync-ui.test.tsx, apps/mobile/src/capture/today-screen.test.tsx</files>
  <read_first>apps/mobile/src/capture/ajustes-screen.test.tsx, apps/mobile/src/capture/offline-sync-ui.test.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/AjustesScreen.tsx</read_first>
  <action>Add tests that render Ajustes with empty queue, non-critical pending queue, critical pending queue, critical conflict, missing central read, and local-cache-only prepare state. Assert safe-close block copy appears only for the critical/missing/stale/local cases. Add a conflict discard test through Ajustes, not only `offline-sync-ui`, proving the reason is passed into `resolveSyncConflict()`. Keep TodayScreen sync tests passing after any helper extraction.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - A test asserts non-critical pending sync renders without the blocking safe-close sentence.
    - A test asserts critical conflict renders `bloqueia fechamento seguro`.
    - A test asserts missing central read renders `Sem leitura central confirmada`.
    - A test asserts conflict discard sends `{ action: "discard_offline_action", reason: "<non-empty>" }`.
  </acceptance_criteria>
  <done>SET-03 is covered with sync and central-read boundaries visible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local queue to safe-close UI | Local pending/conflict state must not be hidden or converted into central proof. |
| Central read cache to sync send state | Downloaded store truth and uploaded local actions are distinct facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | Safe-close blocker helper | mitigate | Block safe-close copy for critical conflict, critical pending local action, missing/stale central read, and local-cache-only operation. |
| T-14-06 | Repudiation | Conflict discard | mitigate | Reuse `SyncConflictPanel` reason requirement and test reason propagation into `resolveSyncConflict()`. |
| T-14-SC | Tampering | npm package surface | mitigate | Do not add queue, state-machine, or form packages in this plan. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Ajustes shows central read and sync send as separate rows.
- Operators can retry pending sync from Ajustes.
- Operators can review and discard conflicts only with a reason.
- Safe-close blocker copy matches critical sync and central-read truth.
</success_criteria>

<output>
After completion, create `.planning/phases/14-mobile-ajustes-and-device-controls/14-03-SUMMARY.md`.
</output>

