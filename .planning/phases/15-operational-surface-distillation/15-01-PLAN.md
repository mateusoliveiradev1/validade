---
phase: 15-operational-surface-distillation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/capture.ts
  - packages/contracts/src/capture.test.ts
  - packages/domain/src/product-policy.ts
  - packages/domain/src/product-policy.test.ts
  - packages/domain/src/index.ts
  - apps/mobile/src/capture/capture-copy.ts
autonomous: true
requirements: ["OPS-02", "OPS-04"]
user_setup: []
must_haves:
  truths:
    - "D-04/D-05: The product classifier is represented by the exact operator question `Como esse produto esta na loja?` and choices `Inteiro solto`, `Embalado pelo fornecedor`, `Cortado/PED`, `Fracionado ou reembalado na loja`, `Preparado pronto`, `Ovos`, `Industrial/refrigerado com validade`, and `Outro/nao sei`."
    - "D-06/D-07: A deterministic domain policy maps classifier/category/product override to existing mode, required lot dates, rebaixa permission, default rebaixa window, quality window, and terminal action semantics."
    - "D-08/D-09/D-10: Rebaixa is allowed only for formal printed validity inside policy window before expiry; PED/cut, fractioned/repacked, prepared, and `Outro/nao sei` default to no automatic rebaixa and conservative copy."
    - "OPS-04 vocabulary starts from shared public labels rather than raw `formal_validity`, `flv_inspection`, `processed_repack_loss`, or `receiving_monitored`."
  artifacts:
    - path: "packages/domain/src/product-policy.ts"
      provides: "Pure classifier-to-policy mapping"
      contains: "resolveProductOperationalPolicy"
    - path: "packages/contracts/src/capture.ts"
      provides: "Runtime classifier schema/types"
      contains: "StorePresentationKindSchema"
    - path: "apps/mobile/src/capture/capture-copy.ts"
      provides: "Public classifier labels and lot preview terms"
      contains: "Como esse produto esta na loja?"
  key_links:
    - from: "packages/contracts/src/capture.ts"
      to: "packages/domain/src/product-policy.ts"
      via: "StorePresentationKind type import"
      pattern: "StorePresentationKind"
    - from: "packages/domain/src/product-policy.ts"
      to: "packages/domain/src/risk.ts"
      via: "ProductMode and RiskWindows compatibility"
      pattern: "formal_validity"
---

<objective>
Create the deterministic product presentation policy foundation.

Purpose: Phase 15 needs one tested source of truth that turns an operator-readable product presentation into the existing mode/date/rebaixa/task semantics without exposing technical ProductMode labels.
Output: Contract schema/types, domain policy helper, exported tests, and shared mobile copy constants for later screens.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-surface-distillation/15-CONTEXT.md
@.planning/phases/15-operational-surface-distillation/15-RESEARCH.md
@.planning/phases/15-operational-surface-distillation/15-PATTERNS.md
@packages/contracts/src/capture.ts
@packages/contracts/src/capture.test.ts
@packages/domain/src/risk.ts
@packages/domain/src/tasks.ts
@packages/domain/src/index.ts
@apps/mobile/src/capture/capture-copy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add classifier contract schema and compatibility tests</name>
  <files>packages/contracts/src/capture.ts, packages/contracts/src/capture.test.ts</files>
  <read_first>packages/contracts/src/capture.ts, packages/contracts/src/capture.test.ts, .planning/phases/15-operational-surface-distillation/15-CONTEXT.md</read_first>
  <action>Add `StorePresentationKindSchema` as a Zod enum with values `loose_whole`, `supplier_packaged`, `store_cut_ped`, `store_fractioned_repacked`, `prepared_ready_to_eat`, `eggs`, `industrial_chilled_validity`, and `unknown_other`. Export `StorePresentationKind`. If this metadata is added to existing product or draft schemas, keep it optional so products/lots created before Phase 15 still parse. Add contract tests that reject unknown classifier values and prove existing product/draft fixtures without classifier metadata still parse.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/contracts test</verify>
  <acceptance_criteria>
    - `packages/contracts/src/capture.ts` exports `StorePresentationKindSchema`.
    - `packages/contracts/src/capture.ts` exports type `StorePresentationKind`.
    - `packages/contracts/src/capture.test.ts` contains all eight enum values from D-05.
    - Existing product or draft fixture tests without classifier metadata still pass.
  </acceptance_criteria>
  <done>Classifier values are runtime-validated without breaking old data.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic product operational policy</name>
  <files>packages/domain/src/product-policy.ts, packages/domain/src/product-policy.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/risk.ts, packages/domain/src/tasks.ts, packages/domain/src/index.ts, packages/contracts/src/capture.ts, .planning/phases/15-operational-surface-distillation/15-RESEARCH.md</read_first>
  <action>Create `packages/domain/src/product-policy.ts` with `resolveProductOperationalPolicy(input)` and exported result types. The input must accept `storePresentation`, `categoryRuleProfile`, optional `productRuleOverride`, and optional `currentDate`. The result must include `mode`, `requiredLotFields`, `allowMarkdown`, `defaultMarkdownDays`, `qualityWindowDays`, `terminalAction`, `requiresCentralReview`, and `publicPolicyKey`. Map `supplier_packaged`, `eggs`, and `industrial_chilled_validity` to `formal_validity` with `allowMarkdown: true`; map `loose_whole` to `flv_inspection` with `allowMarkdown: false`; map `store_cut_ped`, `store_fractioned_repacked`, and `prepared_ready_to_eat` to `processed_repack_loss` with `allowMarkdown: false`; map `unknown_other` to a conservative no-markdown policy with `requiresCentralReview: true`. Let explicit product overrides win only for mode/windows fields already supported by existing contracts. Export the helper from `packages/domain/src/index.ts`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/domain test</verify>
  <acceptance_criteria>
    - `product-policy.ts` contains `resolveProductOperationalPolicy`.
    - `product-policy.test.ts` asserts every D-05 classifier maps to one existing ProductMode.
    - `product-policy.test.ts` asserts `unknown_other` sets `requiresCentralReview: true` and `allowMarkdown: false`.
    - `product-policy.test.ts` asserts `supplier_packaged` allows markdown before expiry and `store_cut_ped` does not.
    - `packages/domain/src/index.ts` exports the new policy helper.
  </acceptance_criteria>
  <done>Policy decisions are pure, testable, and reusable by mobile screens.</done>
</task>

<task type="auto">
  <name>Task 3: Add public classifier and preview copy constants</name>
  <files>apps/mobile/src/capture/capture-copy.ts, packages/domain/src/product-policy.test.ts</files>
  <read_first>apps/mobile/src/capture/capture-copy.ts, apps/mobile/src/capture/mobile-product-polish.test.tsx, .agents/skills/impeccable/PRODUCT.md, .agents/skills/impeccable/reference/product.md</read_first>
  <action>Add copy constants for the classifier question `Como esse produto esta na loja?`, the eight visible choice labels from D-05, and policy preview terms `fica no radar`, `pedir rebaixa`, `retirar/perda`, `reembalar/perda`, and `conferir qualidade`. Keep all strings public-safe and operator-readable. Do not remove existing copy used by current tests unless the dependent screen update in later plans will replace it. Add a test or domain assertion that public policy labels do not contain raw `formal_validity`, `flv_inspection`, `processed_repack_loss`, or `receiving_monitored`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - `capture-copy.ts` contains `Como esse produto esta na loja?`.
    - `capture-copy.ts` contains all eight D-05 visible choice labels.
    - `capture-copy.ts` contains the five D-10 next-action terms.
    - Focused tests or source assertions prove the public labels do not expose raw ProductMode names.
  </acceptance_criteria>
  <done>Later mobile plans can render classifier and preview copy from one public vocabulary source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-visible classifier to domain policy | A human label becomes deterministic business behavior that affects task urgency and rebaixa eligibility. |
| Existing products/lots to new classifier metadata | Pre-Phase-15 data must continue to parse and drive tasks without forced re-registration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Classifier schema | mitigate | Zod enum rejects unrecognized classifier values; tests cover all accepted values. |
| T-15-02 | Elevation of privilege | Rebaixa policy | mitigate | Domain tests prove rebaixa is allowed only for formal printed-validity policies and denied for unknown/PED/prepared flows. |
| T-15-SC | Tampering | Dependency surface | mitigate | Do not add packages or persistence; reuse existing contracts/domain/test stack. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/domain test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
</verification>

<success_criteria>
- Classifier schema and labels cover D-04 through D-10.
- Deterministic policy helper maps to existing modes and task semantics.
- Existing product/draft data stays compatible.
- No new dependency or database migration is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-surface-distillation/15-01-SUMMARY.md`.
</output>
