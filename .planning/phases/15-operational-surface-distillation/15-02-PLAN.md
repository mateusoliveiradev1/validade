---
phase: 15-operational-surface-distillation
plan: "02"
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - apps/mobile/src/capture/ProductDiscoveryScreen.tsx
  - apps/mobile/src/capture/ProductFormScreen.tsx
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/capture-copy.ts
  - apps/mobile/src/capture/product-lookup.test.tsx
  - apps/mobile/src/capture/mobile-product-polish.test.tsx
autonomous: true
requirements: ["OPS-02", "OPS-04"]
user_setup: []
must_haves:
  truths:
    - "D-01: `Registrar lote` opens focused search/scan first, not equal-weight create/category/diagnostic shortcuts."
    - "D-02: Product creation appears only after no safe reuse; reusable products go directly to lot registration and similar products must be reviewed first."
    - "D-03: Recent/frequent/category remain secondary lookup aids and do not decide policy before search/scan."
    - "D-04/D-05/D-06: Product creation/review starts with `Como esse produto esta na loja?`, renders the eight human choices before categories, and never requires technical ProductMode knowledge."
  artifacts:
    - path: "apps/mobile/src/capture/ProductDiscoveryScreen.tsx"
      provides: "Search-first product lookup and create gating"
      contains: "no_safe_reuse"
    - path: "apps/mobile/src/capture/ProductFormScreen.tsx"
      provides: "Human classifier before category selection"
      contains: "Como esse produto esta na loja?"
    - path: "apps/mobile/src/capture/product-lookup.test.tsx"
      provides: "Regression coverage for lookup, create gating, and classifier order"
      contains: "Cadastrar produto novo"
  key_links:
    - from: "packages/domain/src/product-policy.ts"
      to: "apps/mobile/src/capture/ProductFormScreen.tsx"
      via: "resolveProductOperationalPolicy"
      pattern: "resolveProductOperationalPolicy"
    - from: "apps/mobile/src/capture/ProductDiscoveryScreen.tsx"
      to: "apps/mobile/src/capture/CaptureApp.tsx"
      via: "onConfirmProduct navigates to confirmed lot flow"
      pattern: "onConfirmProduct"
---

<objective>
Make the mobile product entry flow lot-first and classifier-first.

Purpose: Operators should start from the physical lot task, reuse safe products when possible, and answer a human product-presentation question only when a product must be created or reviewed.
Output: Search-first discovery, conditional product creation, classifier-before-category product form, and focused mobile tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-surface-distillation/15-CONTEXT.md
@.planning/phases/15-operational-surface-distillation/15-RESEARCH.md
@.planning/phases/15-operational-surface-distillation/15-PATTERNS.md
@.planning/phases/15-operational-surface-distillation/15-01-SUMMARY.md
@apps/mobile/src/capture/ProductDiscoveryScreen.tsx
@apps/mobile/src/capture/ProductFormScreen.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/capture/capture-theme.ts
@apps/mobile/src/capture/product-lookup.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate product creation behind no-safe-reuse lookup</name>
  <files>apps/mobile/src/capture/ProductDiscoveryScreen.tsx, apps/mobile/src/capture/product-lookup.test.tsx</files>
  <read_first>apps/mobile/src/capture/ProductDiscoveryScreen.tsx, apps/mobile/src/capture/product-lookup.test.tsx, apps/mobile/src/capture/repository.ts, .planning/phases/15-operational-surface-distillation/15-CONTEXT.md</read_first>
  <action>Update `ProductDiscoveryScreen` so the first visible path is the existing search field plus `Buscar manualmente` and optional scan. Hide or disable `Cadastrar produto novo` before a lookup result proves `no_safe_reuse`. When central search returns `reuse_available`, render reusable products and a primary `Usar este produto` path without a create action. When central search returns `similar_requires_review`, render similar products and keep creation unavailable until the operator explicitly reviews/continues according to the existing draft flow. Keep `Recentes`, `Frequentes`, and `Por categoria` as secondary lookup aids below the primary search path and never as the first policy decision. Preserve local repository behavior by treating zero local results after manual search as the no-safe-reuse state.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Initial render of `ProductDiscoveryScreen` does not expose an enabled `Cadastrar produto novo` action.
    - A reusable central result renders `Usar este produto` and does not render an enabled create action.
    - A similar central result shows review copy and similar rows before any draft creation.
    - A no-match local/manual lookup can open product creation with GTIN/identifier prefill when applicable.
  </acceptance_criteria>
  <done>Product creation cannot bypass search, reuse, or similar review.</done>
</task>

<task type="auto">
  <name>Task 2: Render classifier before category in product creation</name>
  <files>apps/mobile/src/capture/ProductFormScreen.tsx, apps/mobile/src/capture/capture-copy.ts, apps/mobile/src/capture/product-lookup.test.tsx</files>
  <read_first>apps/mobile/src/capture/ProductFormScreen.tsx, apps/mobile/src/capture/capture-copy.ts, packages/domain/src/product-policy.ts, apps/mobile/src/capture/product-lookup.test.tsx, .agents/skills/impeccable/reference/product.md</read_first>
  <action>Update `ProductFormScreen` so the first product-policy step after product name is the question `Como esse produto esta na loja?`. Render the eight classifier choices as `SelectionRow` options before any category rows or category filter. Only render category filtering/rows after a classifier is selected. Use `resolveProductOperationalPolicy` to preview the human policy result and to set the existing product create/draft input fields, including `productRuleOverride` only when needed. Remove or hide the manual `Definir excecao de perfil` path from the operator-facing flow for this phase. Keep supplier and GTIN optional fields after classifier/category. Use `Outro/nao sei` to show conservative review copy and no automatic rebaixa.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Rendered product form contains `Como esse produto esta na loja?` before any category row text.
    - Rendered product form contains all eight D-05 visible classifier labels.
    - The source no longer exposes `Definir excecao de perfil` in the default operator flow.
    - Selecting `Outro/nao sei` renders conservative/pending-review copy and does not show rebaixa as allowed.
  </acceptance_criteria>
  <done>Product creation starts with a human classifier and uses policy instead of technical mode choice.</done>
</task>

<task type="auto">
  <name>Task 3: Remove technical profile labels from product confirmation journey</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/ProductDiscoveryScreen.tsx, apps/mobile/src/capture/mobile-product-polish.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/ProductDiscoveryScreen.tsx, apps/mobile/src/capture/mobile-product-polish.test.tsx, apps/mobile/src/capture/capture-copy.ts, packages/domain/src/product-policy.ts</read_first>
  <action>Replace operator-facing confirmation copy that currently shows `Perfil operacional:` or raw mode values with public policy labels from the product-policy/copy layer. The confirmed product route should still show product name, category, pending-review warning when applicable, and a primary `Registrar lote` action. `ProductDiscoveryScreen` candidate details may show category/source/review status, but must not show `formal_validity`, `flv_inspection`, `processed_repack_loss`, or `receiving_monitored` to operators.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - `mobile-product-polish.test.tsx` asserts operator-facing product flow text does not contain raw ProductMode strings.
    - `CaptureApp.tsx` confirmed route still renders `Registrar lote`.
    - Candidate confirmation still distinguishes central catalog versus pending review in public language.
  </acceptance_criteria>
  <done>The product-to-lot chain is human-readable and ready for lot registration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Product lookup result to product creation | A missing/similar/reusable result controls whether a new draft can be created. |
| Classifier UI to product policy | Operator-facing choices decide domain behavior without exposing technical modes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | Product creation gate | mitigate | Tests assert create is unavailable before no-safe-reuse and similar review. |
| T-15-04 | Information disclosure | Product confirmation UI | mitigate | Tests/source assertions deny raw ProductMode labels in operator surfaces. |
| T-15-SC | Tampering | UI component vocabulary | mitigate | Use existing capture primitives and tokens only. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- D-01 through D-06 are visible in mobile behavior and tests.
- Product creation cannot bypass central/local safe reuse.
- Classifier choice happens before category selection.
- Technical mode labels are absent from operator-facing product flow.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-surface-distillation/15-02-SUMMARY.md`.
</output>
