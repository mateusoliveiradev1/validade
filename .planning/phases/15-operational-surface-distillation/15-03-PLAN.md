---
phase: 15-operational-surface-distillation
plan: "03"
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - apps/mobile/src/capture/LotRegistrationScreen.tsx
  - apps/mobile/src/capture/capture-copy.ts
  - apps/mobile/src/capture/repository.ts
  - apps/mobile/src/capture/memory-repository.ts
  - apps/mobile/src/capture/lot-registration.test.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - packages/domain/src/product-policy.test.ts
autonomous: true
requirements: ["OPS-02", "OPS-04"]
user_setup: []
must_haves:
  truths:
    - "D-07/D-10: Lot registration uses deterministic policy for fields and preview copy, while showing human next actions `fica no radar`, `pedir rebaixa`, `retirar/perda`, `reembalar/perda`, and `conferir qualidade`."
    - "D-08/D-09: Rebaixa appears only for formal/printed expiry policies inside the configured pre-expiry window and never by default for PED/cut, fractioned/repacked, prepared, or `Outro/nao sei`."
    - "D-10: Existing products/lots keep current mode/profile behavior and missing policy details fall back conservatively."
    - "OPS-02 remains compatible with first real lot capture after product reuse or draft creation."
  artifacts:
    - path: "apps/mobile/src/capture/LotRegistrationScreen.tsx"
      provides: "Policy-aware lot fields and preview"
      contains: "resolveProductOperationalPolicy"
    - path: "apps/mobile/src/capture/lot-registration.test.tsx"
      provides: "Lot form and rebaixa policy regression coverage"
      contains: "Outro/nao sei"
    - path: "apps/mobile/src/capture/today-screen.test.tsx"
      provides: "Existing lot compatibility in Hoje"
      contains: "pre-Phase-15"
  key_links:
    - from: "packages/domain/src/product-policy.ts"
      to: "apps/mobile/src/capture/LotRegistrationScreen.tsx"
      via: "policy result determines fields and preview"
      pattern: "requiredLotFields"
    - from: "apps/mobile/src/capture/memory-repository.ts"
      to: "apps/mobile/src/capture/today-screen.test.tsx"
      via: "pre-Phase-15 fixture remains task-visible"
      pattern: "refreshTodayTasks"
---

<objective>
Make lot registration policy-aware without breaking existing lots.

Purpose: The physical lot form must guide the right dates/actions for each product presentation, show useful preview copy, prevent unsafe automatic rebaixa, and keep old products/lots usable.
Output: Policy-aware lot form, preview copy, rebaixa gate tests, and compatibility fixtures.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-surface-distillation/15-CONTEXT.md
@.planning/phases/15-operational-surface-distillation/15-RESEARCH.md
@.planning/phases/15-operational-surface-distillation/15-PATTERNS.md
@.planning/phases/15-operational-surface-distillation/15-01-SUMMARY.md
@.planning/phases/15-operational-surface-distillation/15-02-SUMMARY.md
@apps/mobile/src/capture/LotRegistrationScreen.tsx
@apps/mobile/src/capture/lot-registration.test.tsx
@apps/mobile/src/capture/repository.ts
@apps/mobile/src/capture/memory-repository.ts
@packages/domain/src/risk.ts
@packages/domain/src/tasks.ts
@packages/domain/src/product-policy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drive lot fields from resolved policy</name>
  <files>apps/mobile/src/capture/LotRegistrationScreen.tsx, apps/mobile/src/capture/lot-registration.test.tsx</files>
  <read_first>apps/mobile/src/capture/LotRegistrationScreen.tsx, apps/mobile/src/capture/lot-registration.test.tsx, packages/domain/src/product-policy.ts, packages/domain/src/risk.ts</read_first>
  <action>Update `LotRegistrationScreen` to call `resolveProductOperationalPolicy` for the selected product and use the returned `requiredLotFields` instead of raw `mode` branching where possible. Keep the existing `buildLotInput` output compatible with `CaptureLotInputSchema`. Render operator labels for required dates: formal printed validity uses `Data de validade impressa`; FLV quality uses `Data de recebimento` and `Janela de qualidade (dias)`; processed/PED/repacked/prepared uses preparation/expiry language and no rebaixa default; `Outro/nao sei` allows conservative lot capture with review copy and no automatic rebaixa. Preserve identity, quantity, and location requirements.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Formal printed-validity fixture renders `Data de validade impressa`.
    - FLV/loose fixture renders `Data de recebimento` and `Janela de qualidade (dias)`.
    - `Outro/nao sei` fixture can render lot fields without showing `Solicitar rebaixa`.
    - `buildLotInput` still creates a valid `CaptureLotInput` for every existing ProductMode.
  </acceptance_criteria>
  <done>Lot form fields follow policy while preserving existing contract output.</done>
</task>

<task type="auto">
  <name>Task 2: Replace raw risk preview with operational next actions</name>
  <files>apps/mobile/src/capture/LotRegistrationScreen.tsx, apps/mobile/src/capture/capture-copy.ts, packages/domain/src/product-policy.test.ts</files>
  <read_first>apps/mobile/src/capture/LotRegistrationScreen.tsx, apps/mobile/src/capture/capture-copy.ts, packages/domain/src/product-policy.ts, packages/domain/src/risk.test.ts, packages/domain/src/tasks.test.ts</read_first>
  <action>Replace preview copy shaped like `Avaliacao operacional: {state}. Proxima orientacao: {command}.` with copy based on the policy/risk result and D-10 terms. Use `fica no radar` for future attention/radar, `pedir rebaixa` only when policy allows markdown and current risk is markdown-due before expiry, `retirar/perda` for expired formal validity, `reembalar/perda` for processed/repacked terminal action, and `conferir qualidade` for FLV quality inspection. Keep the warning/critical tone mapping from existing `riskPreviewTone` or an equivalent helper. Add tests proving expired formal validity does not say `pedir rebaixa`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Rendered lot preview contains at least one of the D-10 terms for tested fixtures.
    - Formal markdown-window fixture can render `pedir rebaixa`.
    - Expired formal fixture renders `retirar/perda` and not `pedir rebaixa`.
    - Processed/PED fixture renders `reembalar/perda` or equivalent no-rebaixa terminal action.
  </acceptance_criteria>
  <done>Lot preview tells the operator what action will happen, not internal risk enum names.</done>
</task>

<task type="auto">
  <name>Task 3: Prove pre-Phase-15 product and lot compatibility</name>
  <files>apps/mobile/src/capture/repository.ts, apps/mobile/src/capture/memory-repository.ts, apps/mobile/src/capture/lot-registration.test.tsx, apps/mobile/src/capture/today-screen.test.tsx</files>
  <read_first>apps/mobile/src/capture/repository.ts, apps/mobile/src/capture/memory-repository.ts, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/RecentLotList.tsx, packages/domain/src/tasks.ts</read_first>
  <action>Add fixtures that represent products/lots created before Phase 15: they have `categoryRuleProfile.mode` and optional `productRuleOverride`, but no classifier metadata. Assert those lots still appear in recent lots or Hoje refresh output, keep their central/local sync state, and generate the same task/rebaixa behavior according to existing mode/profile fallback. If repository helper types need optional classifier fields, keep old fixture creation unchanged or add defaults inside test setup only.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - A pre-Phase-15 formal lot fixture still appears in recent-lot or Today test output.
    - A pre-Phase-15 lot with existing mode/profile can still generate a rebaixa or expiry task when the old rule profile says so.
    - No test fixture must re-register a product solely to satisfy classifier metadata.
  </acceptance_criteria>
  <done>Existing data remains operational after policy-aware lot registration lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy result to lot form | Policy decides which date/action inputs are required for physical capture. |
| Existing local/central records to new preview | Old data must not be silently dropped or reinterpreted unsafely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering | Lot field requirements | mitigate | Tests prove each policy class renders required fields and still builds valid contract input. |
| T-15-06 | Repudiation | Existing lot compatibility | mitigate | Pre-Phase-15 fixtures prove previous records still appear and produce tasks. |
| T-15-07 | Elevation of privilege | Markdown/rebaixa preview | mitigate | Tests deny `pedir rebaixa` for expired, unknown, PED, and prepared defaults. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/domain test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Lot form fields and preview follow product policy.
- Rebaixa is not offered outside formal printed-validity policy/window.
- Existing products/lots remain visible and task-producing.
- No operator-facing preview exposes raw risk command enums.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-surface-distillation/15-03-SUMMARY.md`.
</output>
