---
phase: 15-operational-surface-distillation
plan: "04"
type: execute
wave: 4
depends_on: ["15-03"]
files_modified:
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/TodayScreen.tsx
  - apps/mobile/src/capture/today-copy.ts
  - apps/mobile/src/capture/mobile-status.ts
  - apps/mobile/src/capture/ajustes-readiness.ts
  - apps/mobile/src/capture/offline-sync-ui.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - apps/mobile/src/capture/mobile-status.test.ts
  - apps/mobile/src/capture/mobile-release-journeys.test.tsx
autonomous: true
requirements: ["OPS-01", "OPS-02", "OPS-04"]
user_setup: []
must_haves:
  truths:
    - "D-11/D-12: A central read with zero products/lots/tasks/conflicts is an expected first-store state with primary action `Registrar lote`, not a fatal prepare-turn error."
    - "D-13: Empty-store guidance must not claim the sales area is proven safe; safe close remains dependent on central revalidation, task state, sync state, and physical checklist."
    - "D-14: Ajustes owns healthy push/sync/build/camera/account diagnostics, while Hoje avoids full-width blocker cards for healthy states."
    - "D-15: Hoje promotes readiness only when it affects task execution, safe close, or validation."
    - "OPS-01/OPS-04: Hoje uses the same public readiness vocabulary as Ajustes for central read, local cache, sync queue, push, camera, build, and device authorization."
  artifacts:
    - path: "apps/mobile/src/capture/CaptureApp.tsx"
      provides: "First-store prepare-turn path into Registrar lote"
      contains: "isFirstStoreSetupState"
    - path: "apps/mobile/src/capture/TodayScreen.tsx"
      provides: "Distilled Hoje readiness blocker rendering"
      contains: "operational"
    - path: "apps/mobile/src/capture/ajustes-readiness.ts"
      provides: "Shared readiness classification for mobile surfaces"
      contains: "safeCloseBlocked"
  key_links:
    - from: "apps/mobile/src/capture/ajustes-readiness.ts"
      to: "apps/mobile/src/capture/TodayScreen.tsx"
      via: "shared blocker/compact readiness helper"
      pattern: "syncReadinessFor"
    - from: "apps/mobile/src/capture/CaptureApp.tsx"
      to: "apps/mobile/src/capture/ProductDiscoveryScreen.tsx"
      via: "empty first-store primary action opens the same lot-first discovery route"
      pattern: "navigate({ name: \"discovery\" })"
---

<objective>
Turn empty-store prepare-turn and Hoje readiness into task-first operational surfaces.

Purpose: A new real store should know how to register the first lot without seeing a fatal error, and Hoje should stay focused on active work while still surfacing real blockers at the moment they matter.
Output: First-lot empty-store path, compact healthy readiness, blocker-only Hoje notices, and mobile tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-surface-distillation/15-CONTEXT.md
@.planning/phases/15-operational-surface-distillation/15-RESEARCH.md
@.planning/phases/15-operational-surface-distillation/15-PATTERNS.md
@.planning/phases/14-mobile-ajustes-and-device-controls/14-CONTEXT.md
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/TodayScreen.tsx
@apps/mobile/src/capture/today-copy.ts
@apps/mobile/src/capture/mobile-status.ts
@apps/mobile/src/capture/ajustes-readiness.ts
@apps/mobile/src/capture/offline-sync-ui.tsx
@apps/mobile/src/capture/today-screen.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert empty first-store prepare-turn into first-lot guidance</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/mobile-release-journeys.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/mobile-release-journeys.test.tsx, apps/mobile/src/capture/ProductDiscoveryScreen.tsx, .planning/phases/15-operational-surface-distillation/15-CONTEXT.md</read_first>
  <action>Update `PrepareTurnScreen` behavior for `isFirstStoreSetupState(cache)` so the title/body says the loja is ready to register its first lot, not that the turn is blocked or fatal. The primary action must be `Registrar lote` and must call the existing `startFirstStoreSetup` path into `{ name: \"discovery\" }`. Keep `Preparar turno` available only as a secondary retry when useful. Include explicit copy that zero products/lots/tasks is not proof of safe sales area and that safe close still needs central read, tasks, sync, and checklist. Do not change non-empty blocker/error states.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Empty central first-store fixture renders `Registrar lote`.
    - Empty central first-store fixture does not render `Turno nao preparado` as the primary state.
    - Empty central first-store copy contains no phrase claiming `area segura` or equivalent proven safety.
    - Pressing `Registrar lote` from this state opens the product discovery route.
  </acceptance_criteria>
  <done>First real store setup is a guided lot-registration path, not a fatal prepare-turn stop.</done>
</task>

<task type="auto">
  <name>Task 2: Add Today readiness distillation helper</name>
  <files>apps/mobile/src/capture/ajustes-readiness.ts, apps/mobile/src/capture/mobile-status.ts, apps/mobile/src/capture/mobile-status.test.ts</files>
  <read_first>apps/mobile/src/capture/ajustes-readiness.ts, apps/mobile/src/capture/mobile-status.ts, apps/mobile/src/capture/mobile-status.test.ts, apps/mobile/src/capture/offline-sync-ui.tsx, .planning/phases/14-mobile-ajustes-and-device-controls/14-CONTEXT.md</read_first>
  <action>Add or extend a helper that classifies mobile readiness facts into `compact` versus `blocking_for_today`. It must use public labels for central read, local cache, sync queue, push, camera, build/update, and device authorization. Blocking examples must include missing/stale central read, local-cache-only central read, critical sync conflict, critical pending command, required/incompatible build, invalid store/user/device authorization, and push/camera required for current validation proof. Healthy sync/push/build/camera states must return compact/non-blocking classifications. Keep `syncReadinessFor` and `pushReadinessFor` behavior intact for Ajustes detail.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Tests prove ready sync/push/build/camera facts classify as compact/non-blocking.
    - Tests prove stale or missing central read classifies as blocking for Today.
    - Tests prove critical sync conflict and critical pending command classify as blocking for Today.
    - Public labels do not include provider token, raw device id, private build URL, or raw ProductMode text.
  </acceptance_criteria>
  <done>Mobile surfaces can share a readiness vocabulary without duplicating healthy diagnostics in Hoje.</done>
</task>

<task type="auto">
  <name>Task 3: Render compact healthy readiness and blocker-only Hoje notices</name>
  <files>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/offline-sync-ui.tsx, apps/mobile/src/capture/today-screen.test.tsx</files>
  <read_first>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/today-copy.ts, apps/mobile/src/capture/offline-sync-ui.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/ajustes-readiness.ts</read_first>
  <action>Update `TodayScreen` so the default safety header can show compact healthy metadata for sync/push/build/camera without full-width warning cards. Continue to render active tasks, sales-area risks, task sync status, and manual refresh. Promote explicit blocker cards only when the new readiness helper says the state affects execution, safe close, or validation. Keep conflict review and manual sync actions available for critical sync states. Preserve `Ajustes` as the place for detailed healthy diagnostics.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Healthy fixture does not render full-width `Alertas ajudam`, `Tudo sincronizado`, or build/camera diagnostic cards above active work.
    - Missing/stale central read fixture renders a prominent blocker with an action to prepare/refresh central read.
    - Critical sync conflict fixture renders conflict review affordance.
    - Active task rows still render and remain the priority action when risks exist.
  </acceptance_criteria>
  <done>Hoje is compact when healthy and loud only for operational blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Empty central read to operator guidance | Zero facts must guide first action without claiming safety. |
| Readiness facts to Today prominence | Healthy device/sync facts must not hide real tasks, while blockers must stay visible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Spoofing | Empty-store state | mitigate | Tests assert empty store does not claim safe area and enters real lot registration. |
| T-15-09 | Denial of service | Hoje readiness clutter | mitigate | Tests assert healthy diagnostics stay compact and do not crowd active work. |
| T-15-10 | Tampering | Critical readiness blockers | mitigate | Tests assert stale central read and critical sync are still prominent. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Empty store prepares the first-lot journey without false safety claims.
- Hoje hides healthy diagnostic detail and shows real blockers.
- Shared mobile vocabulary covers OPS-01, OPS-02, and OPS-04.
- Ajustes remains the diagnostic detail surface.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-surface-distillation/15-04-SUMMARY.md`.
</output>
