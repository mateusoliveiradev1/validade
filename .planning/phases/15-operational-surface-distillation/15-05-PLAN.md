---
phase: 15-operational-surface-distillation
plan: "05"
type: execute
wave: 5
depends_on: ["15-04"]
files_modified:
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/ShiftCloseScreen.tsx
  - apps/mobile/src/capture/shift-close.ts
  - apps/mobile/src/capture/shift-close.test.tsx
  - packages/domain/src/shift-close.ts
  - packages/domain/src/shift-close.test.ts
  - packages/contracts/src/shift-close.ts
  - packages/contracts/src/shift-close.test.ts
autonomous: true
requirements: ["OPS-03", "OPS-04"]
user_setup: []
must_haves:
  truths:
    - "D-13: Safe close still depends on central revalidation, task state, sync state, and physical checklist even after empty-store guidance."
    - "D-16: Fechamento summarizes active tasks, unresolved sync, stale central read, device/build blockers, and the physical checklist before enabling safe close."
    - "D-16: Fechamento keeps stricter blocker semantics than ordinary Hoje status copy."
    - "OPS-03: Safe-close action cannot be accepted before blockers/checklist are evaluated."
  artifacts:
    - path: "apps/mobile/src/capture/ShiftCloseScreen.tsx"
      provides: "Close summary and safe/unsafe handoff UI"
      contains: "Resumo antes do fechamento seguro"
    - path: "packages/domain/src/shift-close.ts"
      provides: "Strict safe-close blocker semantics"
      contains: "evaluateShiftClose"
    - path: "apps/mobile/src/capture/shift-close.test.tsx"
      provides: "Mobile close blocker tests"
      contains: "Fechamento do turno"
  key_links:
    - from: "apps/mobile/src/capture/CaptureApp.tsx"
      to: "apps/mobile/src/capture/ShiftCloseScreen.tsx"
      via: "prepareTurnCacheStatus, prepareTurnSource, build/device facts"
      pattern: "prepareTurnCacheStatus"
    - from: "apps/mobile/src/capture/ShiftCloseScreen.tsx"
      to: "packages/domain/src/shift-close.ts"
      via: "evaluateShiftClose remains eligibility source"
      pattern: "evaluateShiftClose"
---

<objective>
Make Fechamento do turno summarize strict safe-close blockers before accepting a close.

Purpose: Leadership needs a compact but uncompromising close surface that explains what still blocks safe close and never weakens the domain gate because Hoje became quieter.
Output: Close summary panel, strict blocker mapping for active tasks/sync/stale central/device/build/checklist, and focused tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-surface-distillation/15-CONTEXT.md
@.planning/phases/15-operational-surface-distillation/15-RESEARCH.md
@.planning/phases/15-operational-surface-distillation/15-PATTERNS.md
@apps/mobile/src/capture/ShiftCloseScreen.tsx
@apps/mobile/src/capture/shift-close.test.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@packages/domain/src/shift-close.ts
@packages/domain/src/shift-close.test.ts
@packages/contracts/src/shift-close.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safe-close summary before close actions</name>
  <files>apps/mobile/src/capture/ShiftCloseScreen.tsx, apps/mobile/src/capture/shift-close.test.tsx</files>
  <read_first>apps/mobile/src/capture/ShiftCloseScreen.tsx, apps/mobile/src/capture/shift-close.test.tsx, packages/domain/src/shift-close.ts, apps/mobile/src/capture/capture-ui.tsx</read_first>
  <action>Add a summary section titled `Resumo antes do fechamento seguro` above the physical checklist and close actions. It must show active task count, unresolved sync count/conflict count, central read status, device/build blocker status when available, and checklist progress such as `0/3 conferencias`. The safe close primary action remains disabled unless `evaluation.eligibility === \"eligible_safe\"` and central validation is available. Keep the unsafe handoff form below the blocker summary.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Shift close render contains `Resumo antes do fechamento seguro`.
    - Summary includes active task count, sync pending/conflict count, central read status, and checklist progress in tests.
    - Safe close button remains disabled when `evaluation.eligibility` is not `eligible_safe`.
    - Unsafe handoff form remains available when blockers exist.
  </acceptance_criteria>
  <done>Leadership sees the close facts before any safe/unsafe close decision.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve strict domain blockers for stale central, sync, device, and build</name>
  <files>packages/domain/src/shift-close.ts, packages/domain/src/shift-close.test.ts, packages/contracts/src/shift-close.ts, packages/contracts/src/shift-close.test.ts</files>
  <read_first>packages/domain/src/shift-close.ts, packages/domain/src/shift-close.test.ts, packages/contracts/src/shift-close.ts, packages/contracts/src/shift-close.test.ts, apps/mobile/src/capture/ajustes-readiness.ts</read_first>
  <action>Extend the existing shift-close input/types only if needed to carry device/build blockers. Keep current blocker codes and add explicit codes only for missing concepts, such as `device_authorization_blocker` and `required_build_update`, using public labels. Preserve existing blockers for `central_capture_not_ready`, `central_active_tasks`, `critical_sync_conflict`, `critical_pending_sync`, `offline_cache_stale`, `offline_mode_requires_central_revalidation`, `required_evidence_pending`, `pending_unsafe_close_sync`, and `incomplete_checklist`. Add domain tests proving stale/missing central read, critical sync conflict, critical pending command, incompatible/required update, invalid device authorization, and incomplete checklist all block safe close.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/domain test && cmd /c pnpm.cmd --filter @validade-zero/contracts test</verify>
  <acceptance_criteria>
    - Existing Phase 10 shift-close tests still pass.
    - Domain tests include stale/missing central read blocking safe close.
    - Domain tests include critical sync conflict and critical pending command blocking safe close.
    - Domain or contract tests include device authorization/build update blockers if new codes are introduced.
  </acceptance_criteria>
  <done>Safe-close semantics remain stricter than Hoje and cover all OPS-03 blockers.</done>
</task>

<task type="auto">
  <name>Task 3: Pass build/device facts into shift close and prove fallback behavior</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/ShiftCloseScreen.tsx, apps/mobile/src/capture/shift-close.ts, apps/mobile/src/capture/shift-close.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/ShiftCloseScreen.tsx, apps/mobile/src/capture/shift-close.ts, apps/mobile/src/build-info.ts, apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/shift-close.test.tsx</read_first>
  <action>Pass available `MobileBuildInfo` and device/session authorization facts from `CaptureApp` into `ShiftCloseScreen` when the screen opens. Map `buildCompatibility` values `desatualizado`, `incompativel`, and `desconhecido` to attention/blocker summary rows according to the strict close policy, and map invalid store/user/device authorization to a close blocker. If a fact is unavailable, render `nao informado` in the summary and do not claim safe proof from it. Add tests for incompatible build and invalid authorization blocking safe close without exposing private build URLs or raw device identifiers.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - `CaptureApp.tsx` passes build/device facts to `ShiftCloseScreen`.
    - Incompatible or required update fixture blocks safe close.
    - Invalid authorization fixture blocks safe close.
    - Rendered close summary does not include `buildUrl`, `ExpoPushToken`, raw device id, `secret`, or `password`.
  </acceptance_criteria>
  <done>Fechamento covers device/build blockers while staying public-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile summary to domain close evaluation | Summary must explain but not weaken `evaluateShiftClose`. |
| Device/build/auth facts to close decision | Local facts can block safe close but must not expose private identifiers or links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Tampering | Safe-close eligibility | mitigate | Domain eligibility remains the source; tests assert blockers keep safe close disabled. |
| T-15-12 | Information disclosure | Build/device close summary | mitigate | Tests deny private URLs, tokens, raw IDs, secrets, and passwords. |
| T-15-13 | Repudiation | Unsafe handoff | mitigate | Existing unsafe close continuity fields remain required and tested. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/domain test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Fechamento summarizes all OPS-03 blocker classes before safe close.
- Safe close remains disabled unless strict domain evaluation is eligible.
- Device/build/auth blockers are public-safe.
- Unsafe handoff still records continuity instead of resolving tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-surface-distillation/15-05-SUMMARY.md`.
</output>
