---
phase: 15-operational-surface-distillation
plan: "06"
type: execute
wave: 6
depends_on: ["15-05"]
files_modified:
  - apps/web/src/command-center/command-center-view-model.ts
  - apps/web/src/command-center/OperacaoRoute.tsx
  - apps/web/src/command-center/AparelhosRoute.tsx
  - apps/web/src/command-center/CommandCenter.tsx
  - apps/web/src/command-center/command-center.test.tsx
  - packages/contracts/src/command-center.ts
  - packages/contracts/src/command-center.test.ts
  - docs/testing.md
  - .planning/phases/15-operational-surface-distillation/15-VALIDATION.md
autonomous: true
requirements: ["OPS-01", "OPS-02", "OPS-03", "OPS-04"]
user_setup: []
must_haves:
  truths:
    - "D-17: Web changes are limited to vocabulary/review clarity needed by Phase 15."
    - "D-17: Command Center, Operacao, Aparelhos, Hoje, Fechamento, and Ajustes share public labels for central read, local queue/cache, push, camera, build, device authorization, product review, and lot sync."
    - "OPS-01..OPS-04 are all represented in focused tests and final validation evidence."
    - "Phase 15 final docs do not claim real provider push, installed Android/camera, or physical UAT proof unless those commands/evidence actually exist."
  artifacts:
    - path: "apps/web/src/command-center/command-center-view-model.ts"
      provides: "Public readiness labels and route references"
      contains: "central"
    - path: "apps/web/src/command-center/command-center.test.tsx"
      provides: "Web vocabulary and grouping tests"
      contains: "Command Center"
    - path: ".planning/phases/15-operational-surface-distillation/15-VALIDATION.md"
      provides: "Execution validation status and evidence map"
      contains: "nyquist_compliant"
  key_links:
    - from: "apps/web/src/command-center/command-center-view-model.ts"
      to: "apps/web/src/command-center/OperacaoRoute.tsx"
      via: "shared labels and blocker route references"
      pattern: "validationReferenceForBlocker"
    - from: "apps/web/src/command-center/AparelhosRoute.tsx"
      to: "apps/mobile/src/capture/ajustes-readiness.ts"
      via: "same public vocabulary for device readiness concepts"
      pattern: "Leitura central"
---

<objective>
Align Command Center vocabulary with mobile readiness and close Phase 15 validation honestly.

Purpose: Leadership surfaces should describe the same facts as Hoje, Ajustes, and Fechamento without reintroducing diagnostic clutter or private proof claims.
Output: Web vocabulary updates, web/contract tests, validation evidence updates, and final repository gate results.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operational-surface-distillation/15-CONTEXT.md
@.planning/phases/15-operational-surface-distillation/15-RESEARCH.md
@.planning/phases/15-operational-surface-distillation/15-PATTERNS.md
@.planning/phases/15-operational-surface-distillation/15-VALIDATION.md
@.planning/phases/15-operational-surface-distillation/15-04-SUMMARY.md
@.planning/phases/15-operational-surface-distillation/15-05-SUMMARY.md
@apps/web/src/command-center/command-center-view-model.ts
@apps/web/src/command-center/OperacaoRoute.tsx
@apps/web/src/command-center/AparelhosRoute.tsx
@apps/web/src/command-center/command-center.test.tsx
@packages/contracts/src/command-center.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align web readiness vocabulary and route references</name>
  <files>apps/web/src/command-center/command-center-view-model.ts, apps/web/src/command-center/OperacaoRoute.tsx, apps/web/src/command-center/AparelhosRoute.tsx, apps/web/src/command-center/CommandCenter.tsx</files>
  <read_first>apps/web/src/command-center/command-center-view-model.ts, apps/web/src/command-center/OperacaoRoute.tsx, apps/web/src/command-center/AparelhosRoute.tsx, apps/web/src/command-center/CommandCenter.tsx, apps/mobile/src/capture/ajustes-readiness.ts, apps/mobile/src/capture/today-copy.ts</read_first>
  <action>Update web copy and helper labels so daily-operation surfaces use these public terms consistently: `leitura central`, `cache local`, `fila local`, `fila de sincronizacao`, `push`, `camera`, `build`, `autorizacao do aparelho`, `revisao de produto`, and `lote sincronizado`. Keep Operacao focused on sales-area safety and route push/camera/device detail to Aparelhos, build detail to Atualizacoes, and product review/sync/shift-close detail to Operacao. Do not add new web routes or move validation/UAT content back into the daily Command Center.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Web rendered text uses `leitura central` and `fila local` or `fila de sincronizacao` for readiness facts.
    - Push/camera/device blockers route to Aparelhos copy.
    - Build blockers route to Atualizacoes copy.
    - Product review, sync, and shift-close blockers route to Operacao copy.
    - Daily Operacao does not render private build URLs, provider tokens, or UAT rollout proof as daily status.
  </acceptance_criteria>
  <done>Web readiness vocabulary matches the mobile operational vocabulary without expanding scope.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten Command Center contracts/tests for public labels</name>
  <files>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts, apps/web/src/command-center/command-center.test.tsx</files>
  <read_first>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts, apps/web/src/command-center/command-center.test.tsx, apps/api/src/command-center.ts</read_first>
  <action>Add or update tests around Command Center projection labels and blocker categories to ensure public text stays aligned with Phase 15 vocabulary. If contract refinements are needed, keep them backward-compatible and avoid adding required fields unless existing API projection code is updated in the same task. Preserve the existing denylist that rejects URLs, tokens, build links, and secrets in public evidence text.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/contracts test && cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Contract tests still reject private URLs/tokens/build links in public text.
    - Web tests assert `leitura central`, `fila local` or `fila de sincronizacao`, `push`, `camera`, `build`, and `autorizacao do aparelho`.
    - No required Command Center field is added without updating API projection tests.
  </acceptance_criteria>
  <done>Command Center public labels are test-protected and backward-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Run final gates and update validation evidence honestly</name>
  <files>.planning/phases/15-operational-surface-distillation/15-VALIDATION.md, docs/testing.md, .planning/phases/15-operational-surface-distillation/15-06-SUMMARY.md</files>
  <read_first>.planning/phases/15-operational-surface-distillation/15-VALIDATION.md, package.json, apps/mobile/package.json, apps/web/package.json, packages/domain/package.json, docs/testing.md</read_first>
  <action>Run the focused mobile, domain, contracts, and web tests touched by Phase 15, then run `cmd /c pnpm.cmd security:evidence` and `cmd /c pnpm.cmd check`. Update `15-VALIDATION.md` statuses and `nyquist_compliant` only after commands pass or after an exact external blocker is recorded. If installed Android/provider push/camera/physical UAT proof is not run, record that boundary in `docs/testing.md` or the plan summary instead of claiming it. Create `15-06-SUMMARY.md` with command outputs, deviations, remaining external blockers, and requirement coverage OPS-01 through OPS-04.</action>
  <verify>cmd /c pnpm.cmd check</verify>
  <acceptance_criteria>
    - `cmd /c pnpm.cmd --filter @validade-zero/mobile test` exits 0 or exact failure is recorded.
    - `cmd /c pnpm.cmd --filter @validade-zero/domain test` exits 0 or exact failure is recorded.
    - `cmd /c pnpm.cmd --filter @validade-zero/contracts test` exits 0 or exact failure is recorded.
    - `cmd /c pnpm.cmd --filter @validade-zero/web test` exits 0 or exact failure is recorded.
    - `cmd /c pnpm.cmd security:evidence` exits 0 or exact failure is recorded.
    - `15-VALIDATION.md` records no green claim without command evidence.
  </acceptance_criteria>
  <done>Phase 15 closes with honest automated evidence and explicit external proof boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile readiness vocabulary to web leadership copy | The same operational facts must not be renamed in ways that mislead leaders. |
| Repo-local verification to rollout proof | Automated tests prove code behavior, not physical store, provider, camera, or installed-device evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Information disclosure | Web public labels and evidence text | mitigate | Preserve denylist tests and run `security:evidence`. |
| T-15-15 | Repudiation | Validation closeout | mitigate | Record exact commands and external blockers; do not claim unrun physical/device/provider proof. |
| T-15-16 | Spoofing | Web/mobile vocabulary drift | mitigate | Web tests assert the same public readiness terms used by mobile. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/domain test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web test`
- [ ] `cmd /c pnpm.cmd security:evidence`
- [ ] `cmd /c pnpm.cmd check`
</verification>

<success_criteria>
- Web Command Center vocabulary aligns with mobile Hoje/Ajustes/Fechamento.
- OPS-01 through OPS-04 are all covered by plans and tests.
- Validation evidence is honest and public-safe.
- No private URLs, tokens, raw device ids, secrets, or real data are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/15-operational-surface-distillation/15-06-SUMMARY.md`.
</output>
