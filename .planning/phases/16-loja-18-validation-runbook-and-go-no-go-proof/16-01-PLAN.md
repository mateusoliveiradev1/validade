---
phase: 16-loja-18-validation-runbook-and-go-no-go-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/command-center.ts
  - packages/contracts/src/command-center.test.ts
autonomous: true
requirements: ["VAL-01", "VAL-02", "VAL-03"]
user_setup: []
must_haves:
  truths:
    - "D-01: The Loja 18 runbook keeps the mandatory nine-step sequence and no step is optional."
    - "D-03: Contract shape must not introduce manual passed fields or a UI-authoritative pass state."
    - "D-05: Validation evidence is limited to sanitized status, timestamp, owner route, masked device, generic role, and bounded evidence label."
    - "D-06: Real product and real lot proof never stores product names or readable lot identifiers in public artifacts."
    - "D-07: Camera/photo proof is represented by sanitized status/fallback labels, never a real photo in repo artifacts."
    - "D-08: Device/actor data stays masked; no real name, phone, email, raw device id, token, or private URL can appear in validation text."
  artifacts:
    - path: "packages/contracts/src/command-center.ts"
      provides: "Runtime contract for UAT steps, blockers, device readiness, and public-safe text"
      contains: "PublicSafeTextSchema"
    - path: "packages/contracts/src/command-center.test.ts"
      provides: "Contract invariant tests for public-safe validation"
      contains: "requires the guided Loja 18 UAT checklist"
  key_links:
    - from: "packages/contracts/src/command-center.ts"
      to: "apps/api/src/command-center.ts"
      via: "CommandCenterProjection and PilotUatStep schemas"
      pattern: "PilotUatStepSchema"
    - from: "packages/contracts/src/command-center.ts"
      to: "apps/web/src/command-center/ValidacaoRoute.tsx"
      via: "rendered UAT step and blocker fields"
      pattern: "pilotUat"
---

<objective>
Lock the Phase 16 validation contract before API or UI changes.

Purpose: The executor needs a hard runtime boundary for the guided runbook and public-safe evidence so later plans cannot accidentally add manual pass truth, raw evidence, or private identifiers.
Output: Contract and tests that protect step order, state semantics, blocker ownership, and sensitive-string denial.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-CONTEXT.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-RESEARCH.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-PATTERNS.md
@packages/contracts/src/command-center.ts
@packages/contracts/src/command-center.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden public-safe validation text denial</name>
  <files>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts</files>
  <read_first>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts, scripts/check-no-sensitive-evidence.mjs</read_first>
  <action>Extend the `PublicSafeTextSchema` denylist used by `PilotUatStepSchema` and `PilotOperationalBlockerSchema` to reject public validation text containing private URL schemes, provider/build/token markers, raw device markers, binary/photo payload markers, or object-storage markers. Keep already rejected patterns for `https?://`, `eas://`, `token`, `secret`, `password`, `ExpoPushToken`, and `buildUrl`; add coverage for exact sensitive markers such as `rawDeviceId`, `providerTicket`, `providerReceipt`, `photoUri`, `objectKey`, and `base64`. Add contract tests that each marker is rejected when placed in `cause`, `nextAction`, or `evidenceReferenceLabel` on a UAT step or operational blocker.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/contracts test</verify>
  <acceptance_criteria>
    - `PublicSafeTextSchema` still rejects private URLs, token/secret/password terms, Expo push token text, and build URL text.
    - Contract tests reject `rawDeviceId`, `providerTicket`, `providerReceipt`, `photoUri`, `objectKey`, and `base64` in validation evidence fields.
    - Device fields that are already masked, such as `deviceIdMasked`, remain valid when the value is a bounded masked label like `moto...001`.
  </acceptance_criteria>
  <done>Public validation text cannot carry sensitive evidence markers.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the exact guided runbook contract</name>
  <files>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts</files>
  <read_first>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts, .planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md</read_first>
  <action>Strengthen `PilotUatChecklistSchema` tests so the only valid step order is exactly `prepare_turn`, `product_real_input`, `lot_registration`, `terminal_resolution`, `second_device_convergence`, `command_center_consistency`, `safe_push_test`, `camera_evidence_or_fallback`, `shift_close`. Add negative tests for duplicate steps, missing steps, extra steps, and reordered steps. Add a test that no `manualPassed`, `passedByUser`, `manualOverride`, or equivalent manual truth field survives strict parsing.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/contracts test</verify>
  <acceptance_criteria>
    - Contract tests assert all nine step ids in the required order.
    - Contract tests fail duplicate, missing, extra, and reordered runbook steps.
    - Strict parsing rejects any manual pass/override field added to a step fixture.
  </acceptance_criteria>
  <done>The contract protects the mandatory Phase 16 runbook sequence.</done>
</task>

<task type="auto">
  <name>Task 3: Protect blocker severity and external ownership semantics</name>
  <files>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts</files>
  <read_first>packages/contracts/src/command-center.ts, packages/contracts/src/command-center.test.ts, .planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-CONTEXT.md</read_first>
  <action>Add tests proving `blocked` and `external_blocked` UAT steps require both `cause` and `nextAction`, and that `PilotOperationalBlockerSchema` requires `ownership: \"external\"` whenever `severity: \"external\"`. Add a positive fixture for an external blocker with `category: \"push\"`, `severity: \"external\"`, `ownership: \"external\"`, `evidenceReferenceLabel: \"Timeline de teste seguro\"`, and no sensitive strings. Add a positive fixture for a critical actionable blocker with `category: \"sync\"` and `ownership: \"operator\"`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/contracts test</verify>
  <acceptance_criteria>
    - `external_blocked` without `cause` and `nextAction` fails.
    - `blocked` without `cause` and `nextAction` fails.
    - `severity: \"external\"` with `ownership: \"operator\"` or `ownership: \"repo\"` fails.
    - Positive external and critical blocker fixtures parse successfully.
  </acceptance_criteria>
  <done>External gaps stay honest and actionable blockers stay causal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API/web validation text to public repo | Text rendered or committed as evidence must be sanitized and bounded. |
| UAT state contract to rollout truth | Contract must not allow manual pass truth to bypass central proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information disclosure | Public-safe validation text | mitigate | Deny sensitive markers in `PublicSafeTextSchema` and test UAT/blocker fields. |
| T-16-02 | Tampering | UAT step state | mitigate | Strict schemas reject manual pass override fields and invalid step order. |
| T-16-03 | Repudiation | External blocker ownership | mitigate | Contract requires external severity to use external ownership and causal text. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts test`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Runbook sequence is exact and contract-protected.
- Public-safe evidence denial covers validation-specific sensitive markers.
- Manual pass truth cannot enter the contract.
- External blocker ownership and actionable blocker causes are enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-01-SUMMARY.md`.
</output>
