---
phase: 16-loja-18-validation-runbook-and-go-no-go-proof
plan: "02"
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/api/src/command-center.ts
  - apps/api/src/command-center.test.ts
  - packages/contracts/src/command-center.test.ts
autonomous: true
requirements: ["VAL-01", "VAL-02", "VAL-03", "VAL-04"]
user_setup: []
must_haves:
  truths:
    - "D-01: Central runbook proof advances the mandatory guided sequence only when specific facts exist."
    - "D-02: Mobile remains the real action surface; the API reads proof produced by normal mobile flows."
    - "D-03: Product, lot, resolution, and close steps cannot pass from manual UI status."
    - "D-04: Missing central proof fails closed to pending or blocked with owner route/action."
    - "D-06: Product/lot proof uses central existence and sanitized labels, not public product names or readable lot ids."
    - "D-12: Physical Loja 18 UAT remains non-Go when central/external gates are missing."
  artifacts:
    - path: "apps/api/src/command-center.ts"
      provides: "Command Center projection and pilot UAT derivation"
      contains: "buildPilotUatChecklist"
    - path: "apps/api/src/command-center.test.ts"
      provides: "API projection behavior tests"
      contains: "Command Center API"
  key_links:
    - from: "apps/api/src/command-center.ts"
      to: "packages/contracts/src/command-center.ts"
      via: "CommandCenterProjectionSchema.parse"
      pattern: "CommandCenterProjectionSchema"
    - from: "apps/api/src/command-center.ts"
      to: "apps/mobile/src/capture/TaskResolutionPanel.tsx"
      via: "resolvedHistory central facts"
      pattern: "resolvedHistory"
---

<objective>
Derive product, lot, terminal resolution, Command Center consistency, and shift-close runbook states from central facts.

Purpose: The guided validation path must prove real operational flow through central projection instead of generic fixtures or manual pass fields.
Output: API helpers/tests that convert prepared-turn facts into public-safe UAT step states and blocker causes.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-CONTEXT.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-RESEARCH.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-PATTERNS.md
@apps/api/src/command-center.ts
@apps/api/src/command-center.test.ts
@packages/contracts/src/capture.ts
@packages/contracts/src/command-center.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create central runbook fact derivation</name>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts</files>
  <read_first>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, packages/contracts/src/capture.ts, packages/contracts/src/command-center.ts</read_first>
  <action>Add a local helper near `projectionFromCentralPrepareTurn` that derives named facts from `PrepareTurnResponse`: `centralReadReady`, `hasRealProductInput`, `hasRealLotRegistration`, `hasTerminalResolution`, `hasCommandCenterConsistency`, and `hasOpenCentralBlockers`. Use existing prepared-turn fields: `prepared.store.source`, `prepared.store.readiness`, `prepared.store.centralReadAt`, `prepared.products`, `prepared.lots`, `prepared.activeTasks`, `prepared.resolvedHistory`, `prepared.conflicts`, `prepared.cache`, and `prepared.store.blockers`. Do not inspect or emit real product names or readable lot identities for UAT labels. Thread these facts into `buildPilotUatChecklist` instead of using the broad `hasOperationalFacts` boolean to imply product/lot/resolution proof.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/api test</verify>
  <acceptance_criteria>
    - `buildPilotUatChecklist` input distinguishes product, lot, terminal resolution, and command-center consistency facts.
    - Product step passes only when a central product fact exists; evidence label is `Produto real confirmado` or another bounded safe label.
    - Lot step passes only when a central lot fact exists; evidence label is `Lote real confirmado` or another bounded safe label.
    - Terminal resolution step passes only when `prepared.resolvedHistory.length > 0`; evidence label is `Resolucao terminal confirmada` or another bounded safe label.
    - Generic `hasOperationalFacts` no longer passes product, lot, or resolution steps by itself.
  </acceptance_criteria>
  <done>Central runbook steps advance from specific central facts.</done>
</task>

<task type="auto">
  <name>Task 2: Fail missing central facts closed with route/action copy</name>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts</files>
  <read_first>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, .planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-CONTEXT.md</read_first>
  <action>Update `buildPilotUatChecklist` so missing product, lot, terminal resolution, command-center consistency, and safe-close central proof remain `pending` unless there is an actionable central blocker. When `prepared.store.blockers`, critical active tasks, critical sync conflicts, or discarded critical actions prevent proof, set the affected step to `blocked` with public-safe `cause`, `nextAction`, and `evidenceReferenceLabel`. Use next actions that route product/lot/resolution/sync/close work to `Operacao` or physical execution, not `Aparelhos` or `Atualizacoes`.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/api test</verify>
  <acceptance_criteria>
    - API tests cover product missing as `pending`, not `passed`.
    - API tests cover lot missing as `pending`, not `passed`.
    - API tests cover terminal resolution missing with active critical task as `blocked`.
    - API tests cover sync conflict or central blocker producing a `blocked` UAT step with cause and nextAction.
    - Blocked central-proof step text does not include product names, lot identities, URLs, tokens, raw device ids, or photos.
  </acceptance_criteria>
  <done>Missing central proof cannot look green and always names the next action.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve safe close as central proof, not absence of tasks</name>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts</files>
  <read_first>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, apps/api/src/shift-close.ts, packages/contracts/src/shift-close.ts</read_first>
  <action>Keep the `shift_close` UAT step blocked or pending unless a central safe-close proof is available from an existing projection field such as `shiftHistory`/accepted safe closure data. Do not pass `shift_close` merely because `activeTasks.length === 0` or `centralVerdict.state === \"safe\"`. If no central safe-close source is currently wired into Command Center, keep the step `blocked` when any UAT step or operational blocker remains and `pending`/`external_blocked` with next action `Concluir fechamento seguro no mobile e atualizar Validacao` when the only missing fact is physical safe-close proof.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/api test</verify>
  <acceptance_criteria>
    - API tests prove no-active-tasks alone does not pass `shift_close`.
    - API tests prove unresolved UAT steps keep `shift_close` blocked.
    - API tests prove safe-close proof, if wired, uses sanitized evidence label `Fechamento seguro confirmado`.
    - `shift_close` blocked/external states include cause and nextAction.
  </acceptance_criteria>
  <done>Safe close remains a real proof gate and not a derived absence-of-work shortcut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile-produced central facts to API UAT state | API must distinguish specific proof from generic operational activity. |
| Central projection to public evidence labels | Product/lot/resolution facts cannot leak real store data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Spoofing | Product/lot UAT proof | mitigate | Tests prove generic operational facts do not pass product/lot steps. |
| T-16-05 | Tampering | Terminal resolution proof | mitigate | Resolution step passes only from `resolvedHistory`; active critical tasks block it. |
| T-16-06 | Repudiation | Safe close proof | mitigate | Shift close cannot pass from no-active-tasks alone; central safe-close proof is required. |
| T-16-07 | Information disclosure | API evidence labels | mitigate | Public-safe labels only; contract tests deny sensitive strings. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/api test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts test`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Product, lot, and resolution steps pass only from specific central facts.
- Missing facts fail closed with owner/action copy.
- Safe close remains explicit central proof.
- API projection stays public-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-02-SUMMARY.md`.
</output>
