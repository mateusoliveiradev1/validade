---
phase: 16-loja-18-validation-runbook-and-go-no-go-proof
plan: "03"
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - apps/api/src/command-center.ts
  - apps/api/src/command-center.test.ts
  - packages/contracts/src/command-center.test.ts
autonomous: true
requirements: ["VAL-01", "VAL-02", "VAL-03"]
user_setup: []
must_haves:
  truths:
    - "D-09: Approved APK install passes only when a real device reports approved version/build through central readiness."
    - "D-10: Remote push passes only with a safe push-test timeline on an approved device; provider acceptance alone is not physical execution."
    - "D-11: Second-device proof requires another approved mobile device reading the same central fact set."
    - "D-12: Missing APK, push, camera/evidence, second-device, or safe-close proof keeps validation out of Go."
    - "D-13: Go requires every runbook step passed and no critical or external blocker."
    - "D-14: No-Go is reserved for actionable critical blockers."
    - "D-15: Aguardando prova externa is used when no actionable critical blocker exists but real proof is missing."
  artifacts:
    - path: "apps/api/src/command-center.ts"
      provides: "External gate synthesis for pilot validation"
      contains: "buildPilotOperationalBlockers"
    - path: "apps/api/src/command-center.test.ts"
      provides: "API tests for device/build/push/camera/second-device gates"
      contains: "push-provider-external"
  key_links:
    - from: "apps/api/src/command-center.ts"
      to: "apps/web/src/command-center/ValidacaoRoute.tsx"
      via: "pilotBlockers and pilotUat external states"
      pattern: "external_blocked"
---

<objective>
Make external proof gates explicit in API projection.

Purpose: Installed APK, remote push, camera/fallback, second-device convergence, and physical proof must remain visible gates rather than inferred from repo tests, mocks, or a single device.
Output: API blocker and UAT derivation for approved build/device, push timeline, camera/fallback, and second-device convergence.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-CONTEXT.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-RESEARCH.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-PATTERNS.md
@apps/api/src/command-center.ts
@apps/api/src/command-center.test.ts
@packages/contracts/src/command-center.ts
@packages/contracts/src/alerts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive approved-device and second-device gates</name>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts</files>
  <read_first>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, packages/contracts/src/command-center.ts, apps/web/src/command-center/command-center-view-model.ts</read_first>
  <action>Add API-side helper predicates for validation devices: confirmed device means no `invalid_store_or_user` blocker, bounded masked id, and a current central read for the store when available. Set `second_device_convergence` to `passed` only when at least two confirmed approved mobile device records read the same current store projection after the central product/lot/resolution facts exist; use evidence label `Aparelho Loja 18 #2`. If there is one or zero confirmed devices, set the step to `external_blocked` with next action to prepare turn on another approved mobile device. Do not count the web Command Center as second-device proof.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/api test</verify>
  <acceptance_criteria>
    - API tests show one approved device leaves `second_device_convergence` as `external_blocked`.
    - API tests show two confirmed approved devices can pass `second_device_convergence` only after central product/lot/resolution facts exist.
    - Device evidence labels use `Aparelho Loja 18 #1` / `Aparelho Loja 18 #2` or existing masked labels, not raw ids.
    - Web-only projection/read does not pass second-device convergence.
  </acceptance_criteria>
  <done>Second-device convergence is a real mobile-device gate.</done>
</task>

<task type="auto">
  <name>Task 2: Keep approved APK/build install as explicit blocker</name>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts</files>
  <read_first>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, packages/contracts/src/command-center.ts, apps/mobile/src/build-info.ts</read_first>
  <action>Ensure `buildPilotOperationalBlockers` emits a `category: \"build\"` blocker whenever a validation-relevant device has `buildCompatibility` of `desatualizado`, `desconhecido`, or `incompativel`, even if the device-level blocker list is missing that code. Use severity `critical` for `incompativel` or `desatualizado`, and `external` for `desconhecido` when the approved install proof is simply absent. Use next actions that route to `Atualizacoes`, and never include build URLs or private artifact links. Keep `approvedArtifactLabel`, version, and build labels public-safe.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/api test</verify>
  <acceptance_criteria>
    - API tests show incompatible build produces a critical build blocker.
    - API tests show outdated build produces a critical or warning blocker that prevents Go.
    - API tests show unknown build produces an external build proof blocker when no actionable critical blocker exists.
    - Blocker text contains no `buildUrl`, `eas://`, private URL, token, or secret.
  </acceptance_criteria>
  <done>Approved APK install proof remains explicit and public-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Classify push and camera proof as passed, critical, or external</name>
  <files>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, packages/contracts/src/command-center.test.ts</files>
  <read_first>apps/api/src/command-center.ts, apps/api/src/command-center.test.ts, packages/contracts/src/alerts.ts, packages/contracts/src/command-center.ts, apps/mobile/src/capture/TaskResolutionPanel.tsx</read_first>
  <action>Update UAT and blocker synthesis so `safe_push_test` passes only when an approved device has a safe push-test timeline item with state `provider_accepted` or `opened`, while `local_only` stays `external_blocked` and `provider_failed`, `token_invalid`, or `permission_denied` become critical/actionable push blockers. Keep `camera_evidence_or_fallback` `external_blocked` until central evidence/fallback metadata is available; if a safe fallback/evidence label is available from existing projection data, pass it with `Camera indisponivel com fallback registrado` or `Camera validada no aparelho aprovado`. Add tests that no raw photo, object key, provider payload, token, or private URL appears in push/camera evidence text.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/api test && cmd /c pnpm.cmd --filter @validade-zero/contracts test</verify>
  <acceptance_criteria>
    - Provider accepted/opened timeline can pass `safe_push_test` but does not resolve task state or safe close.
    - `local_only` push remains external proof gap.
    - `provider_failed`, `token_invalid`, and `permission_denied` create actionable push blockers.
    - Camera step remains external when only repo tests/mocks exist.
    - Push/camera texts pass public-safe contract tests.
  </acceptance_criteria>
  <done>External proof gates are represented honestly in validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device readiness to rollout proof | Device telemetry can prove install/readiness only when it is current and approved. |
| Push provider timeline to validation | Push timeline is diagnostic and cannot prove physical task execution. |
| Camera/evidence metadata to public repo | Real photos/object keys cannot enter public evidence text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Spoofing | Second-device convergence | mitigate | Require two confirmed approved mobile device records; web does not count. |
| T-16-09 | Tampering | APK/build readiness | mitigate | Build blockers derived from installed-vs-approved device facts, not repo version alone. |
| T-16-10 | Repudiation | Push proof | mitigate | Timeline states preserve local/external/critical outcomes with next action. |
| T-16-11 | Information disclosure | Camera/push evidence | mitigate | Public-safe labels only and denylist tests. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/api test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts test`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Second-device proof requires a second approved mobile device.
- Build/install proof is not inferred from repo or artifact generation.
- Push and camera outcomes stay explicit gates.
- External blockers are not mislabeled as Go or operator failure.
</success_criteria>

<output>
After completion, create `.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-03-SUMMARY.md`.
</output>
