---
phase: 16-loja-18-validation-runbook-and-go-no-go-proof
plan: "04"
type: execute
wave: 4
depends_on: ["16-03"]
files_modified:
  - apps/web/src/command-center/ValidacaoRoute.tsx
  - apps/web/src/command-center/command-center-view-model.ts
  - apps/web/src/command-center/command-center.test.tsx
  - apps/web/e2e/v1-readiness.spec.ts
  - apps/web/e2e/fixtures/v1-readiness.ts
autonomous: true
requirements: ["VAL-01", "VAL-02", "VAL-03", "VAL-04"]
user_setup: []
must_haves:
  truths:
    - "D-01: Web Validacao renders the mandatory sequence in exact order."
    - "D-02: Web conducts validation while mobile remains the real operational flow."
    - "D-04: Pending/blocked/external-blocked rows point to Aparelhos, Atualizacoes, Operacao, or physical execution."
    - "D-13: Go appears only when every runbook step passed and no critical/external blocker remains."
    - "D-14: No-Go appears for actionable critical blockers with direct next action."
    - "D-15: Aguardando prova externa appears when evidence is missing without an actionable critical blocker."
    - "D-16: Copy names the missing proof and the route/action to resolve it."
  artifacts:
    - path: "apps/web/src/command-center/ValidacaoRoute.tsx"
      provides: "Phase 16 proof sequencer UI"
      contains: "Validacao"
    - path: "apps/web/src/command-center/command-center-view-model.ts"
      provides: "Go/No-Go verdict derivation and route references"
      contains: "deriveValidationVerdict"
    - path: "apps/web/e2e/v1-readiness.spec.ts"
      provides: "Route navigation proof"
      contains: "Validacao"
  key_links:
    - from: "apps/web/src/command-center/ValidacaoRoute.tsx"
      to: "apps/api/src/command-center.ts"
      via: "projection.pilotUat and projection.pilotBlockers"
      pattern: "pilotUat"
    - from: "apps/web/src/command-center/ValidacaoRoute.tsx"
      to: ".planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md"
      via: "UI contract"
      pattern: "Mandatory Runbook Sequence"
---

<objective>
Make the web `Validacao` route match the Phase 16 guided proof contract.

Purpose: Leadership needs one clear screen that answers Go/No-Go from central/external proof, not a generic dashboard or manual checklist.
Output: Validacao UI, view-model copy, component tests, and web E2E coverage aligned with `16-UI-SPEC.md`.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-CONTEXT.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-RESEARCH.md
@.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-PATTERNS.md
@apps/web/src/command-center/ValidacaoRoute.tsx
@apps/web/src/command-center/command-center-view-model.ts
@apps/web/src/command-center/command-center.test.tsx
@apps/web/e2e/v1-readiness.spec.ts
@apps/web/e2e/fixtures/v1-readiness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade verdict copy to reason plus next action</name>
  <files>apps/web/src/command-center/command-center-view-model.ts, apps/web/src/command-center/ValidacaoRoute.tsx, apps/web/src/command-center/command-center.test.tsx</files>
  <read_first>apps/web/src/command-center/command-center-view-model.ts, apps/web/src/command-center/ValidacaoRoute.tsx, apps/web/src/command-center/command-center.test.tsx, .planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md</read_first>
  <action>Extend the `ValidationVerdict` model if needed so `deriveValidationVerdict` returns `label`, `tone`, `detail`, and a concrete `nextAction`. Use Phase 16 copy patterns: for missing proof, `Ainda nao e Go porque falta prova X. Faca Y em Z.`; for critical blockers, `No-Go: X impede a validacao. Corrija em Z antes de continuar.`; for success, `Go confirmado para Loja 18 com prova central completa e fechamento seguro.` Render the next action in the verdict band and keep it derived from `projection.pilotUat`, `projection.pilotBlockers`, and device/build gates.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Tests cover `Go`, `No-Go`, and `Aguardando prova externa` verdicts.
    - Each verdict renders a reason/detail and a next-action sentence.
    - `No-Go` appears when a critical blocker or blocked step exists.
    - `Aguardando prova externa` appears when only pending/external proof remains.
    - `Go` appears only when all UAT steps passed, devices/build are current/apt, and blockers are empty.
  </acceptance_criteria>
  <done>Verdict copy is direct, operational, and projection-derived.</done>
</task>

<task type="auto">
  <name>Task 2: Render the guided sequence exactly as UI-SPEC</name>
  <files>apps/web/src/command-center/ValidacaoRoute.tsx, apps/web/src/command-center/command-center.test.tsx</files>
  <read_first>apps/web/src/command-center/ValidacaoRoute.tsx, apps/web/src/command-center/command-center.test.tsx, apps/web/src/components/ui/button.tsx, apps/web/src/components/ui/badge.tsx, apps/web/src/index.css, .planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md</read_first>
  <action>Update `ValidacaoRoute` header action label to `Atualizar prova da validacao` and render the nine UAT rows in the exact contract order. Each row must show state badge, owner route/role, `Agora`, optional `Causa`, optional evidence label, updated timestamp, and masked device/generic role when present. Add route-owner buttons labelled `Abrir Aparelhos`, `Abrir Atualizacoes`, and `Abrir Operacao` where useful, while keeping existing specific blocker action buttons if tests still need them. Do not add checkbox, toggle, select, form, modal, or any control that marks a step passed.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test</verify>
  <acceptance_criteria>
    - Rendered UAT rows appear in the exact sequence from `prepare_turn` through `shift_close`.
    - Tests assert `Atualizar prova da validacao` is the refresh action.
    - Tests assert no button/input/checkbox text or aria label matches manual pass labels such as `Marcar passou`, `Aprovar etapa`, `Registrar status da validacao`, or `Passou manualmente`.
    - Every blocked/external row renders `Causa:` and `Agora:`.
    - Route owner buttons render without nested card UI.
  </acceptance_criteria>
  <done>Validacao behaves like a proof sequencer, not a manual checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Prove privacy, responsive route ownership, and E2E navigation</name>
  <files>apps/web/src/command-center/command-center.test.tsx, apps/web/e2e/v1-readiness.spec.ts, apps/web/e2e/fixtures/v1-readiness.ts</files>
  <read_first>apps/web/src/command-center/command-center.test.tsx, apps/web/e2e/v1-readiness.spec.ts, apps/web/e2e/fixtures/v1-readiness.ts, apps/web/src/shell/AppShell.tsx, .planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-UI-SPEC.md</read_first>
  <action>Update web component and Playwright fixtures so Validacao has sanitized Phase 16 proof states without real product names, readable lot IDs, photos, tokens, private URLs, raw device IDs, phone/email data, or personal names. Add tests that push/camera/device blockers route to Aparelhos, build blockers route to Atualizacoes, and product/lot/resolution/sync/shift-close blockers route to Operacao or physical execution. Update `v1-readiness.spec.ts` to navigate to Validacao and assert the verdict band, ordered sequence, external proof gap, and absence of push-test/update actions duplicated from owner routes.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/web test && cmd /c pnpm.cmd test:e2e:web</verify>
  <acceptance_criteria>
    - Component tests deny `/token|secret|password|ExpoPushToken|buildUrl|rawDeviceId|providerTicket|providerReceipt|objectKey|photoUri|base64/i` in rendered Validacao text.
    - Playwright asserts `Validacao`, `Go/No-Go`, and `UAT Loja 18` after navigating to the route.
    - Playwright asserts `Enviar teste seguro` remains outside Validacao.
    - Playwright asserts update artifact/link actions remain outside Validacao.
    - Fixtures use only fictitious/sanitized product, device, role, and evidence labels.
  </acceptance_criteria>
  <done>Web route is navigable, public-safe, and honors route ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API projection to web verdict | Web must explain projection truth without creating new truth. |
| Route owner navigation | Validacao can point to owner routes but must not duplicate privileged actions. |
| Public web fixtures | E2E/component fixtures are committed and must remain sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Tampering | Manual pass UI | mitigate | Tests deny manual pass controls; verdict remains projection-derived. |
| T-16-13 | Information disclosure | Validacao rendered text and fixtures | mitigate | Deny sensitive strings in tests and run `security:evidence`. |
| T-16-14 | Elevation of privilege | Route-owner actions | mitigate | Validacao only navigates; push/update/close actions remain in owner surfaces. |
| T-16-15 | Repudiation | Go/No-Go copy | mitigate | Verdict band shows reason and next action from visible gates. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/web typecheck`
- [ ] `cmd /c pnpm.cmd test:e2e:web`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Validacao matches `16-UI-SPEC.md`.
- No manual pass UI exists.
- Verdict copy is direct and next-step oriented.
- E2E proves route ownership and privacy boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/16-loja-18-validation-runbook-and-go-no-go-proof/16-04-SUMMARY.md`.
</output>
