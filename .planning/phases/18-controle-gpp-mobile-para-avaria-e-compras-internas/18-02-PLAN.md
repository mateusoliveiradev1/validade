---
phase: 18-controle-gpp-mobile-para-avaria-e-compras-internas
plan: "02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/ControleGppScreen.tsx
  - apps/mobile/src/capture/gpp-copy.ts
  - apps/mobile/src/capture/mobile-gpp-navigation.test.tsx
  - apps/mobile/src/capture/mobile-release-journeys.test.tsx
autonomous: true
requirements: ["GPP-08"]
user_setup: []
must_haves:
  truths:
    - "GPP-08: Mobile shows a separate Controle GPP entry for Registrar avaria and Solicitar compra interna."
    - "D-01: A user whose active role is `gpp` opens directly into Controle GPP after login when the feature flag and capability allow it."
    - "D-02: Collaborators and leadership keep `Hoje` as the daily validation surface and access Controle GPP through a separate app entry."
    - "D-03: The first Controle GPP screen is a hub with `Registrar avaria`, `Solicitar compra interna`, `Minhas pendencias`, and `Enviadas hoje`."
    - "D-04: The app hides Controle GPP when `controle_gpp_enabled` is false or the session lacks the required GPP action/capability; backend authorization remains mandatory."
  artifacts:
    - path: "apps/mobile/src/capture/CaptureApp.tsx"
      provides: "Role-aware route stack and separate Controle GPP app entry"
      contains: "gpp-control"
    - path: "apps/mobile/src/capture/ControleGppScreen.tsx"
      provides: "Controle GPP mobile hub"
      contains: "Registrar avaria"
    - path: "apps/mobile/src/capture/mobile-gpp-navigation.test.tsx"
      provides: "Navigation, feature flag, capability, and Today-boundary coverage"
      contains: "Controle GPP"
  key_links:
    - from: "packages/contracts/src/authorization.ts"
      to: "apps/mobile/src/capture/CaptureApp.tsx"
      via: "session.featureFlags.controle_gpp_enabled and session.actions.canCreateGppEntry/canReadGppQueue"
      pattern: "client-side visibility from server-owned session facts"
    - from: "apps/mobile/src/capture/CaptureApp.tsx"
      to: "apps/mobile/src/capture/ControleGppScreen.tsx"
      via: "CaptureRoute and routeStack"
      pattern: "separate mobile entry, not Today integration"
---

<objective>
Add the separate mobile Controle GPP route, hub, and role-aware entry behavior.

Purpose: Sector/GPP users need a direct mobile GPP surface without polluting `Hoje`. This plan adds the first-class route and hub from the UI contract, makes `gpp` role land there when allowed, keeps other roles on `Hoje`, and hides the entry when the feature flag or capabilities do not permit GPP work.
Output: The authenticated mobile shell can open `Controle GPP`; GPP users start there when enabled, collaborators keep `Hoje`, and the hub shows the four Phase 18 entries.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-CONTEXT.md
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-RESEARCH.md
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-UI-SPEC.md
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-VALIDATION.md
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-PATTERNS.md
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/AjustesScreen.tsx
@apps/mobile/src/capture/TodayScreen.tsx
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/capture/capture-theme.ts
@apps/mobile/src/capture/offline-sync-ui.tsx
@apps/mobile/src/capture/mobile-release-journeys.test.tsx
@packages/contracts/src/authorization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GPP route eligibility and initial-route resolution</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, packages/contracts/src/authorization.ts, .planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-UI-SPEC.md</read_first>
  <action>Add a `gpp-control` route variant to `CaptureRoute` and derive a `canUseControleGpp` boolean from `session.featureFlags.controle_gpp_enabled` plus at least one GPP action/capability (`canCreateGppEntry` or `canReadGppQueue`). Replace the fixed `initialRouteStack` usage for authenticated ready state with a resolver that chooses `{ name: \"gpp-control\" }` for `activeRole === \"gpp\"` only when `canUseControleGpp` is true; all other roles and disabled sessions keep `{ name: \"today\" }`. Keep prepare-turn/session initialization intact; if a GPP-only route should bypass a Today-specific readiness gate, document the exact guard in code and test it.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - `CaptureRoute` includes `gpp-control`.
    - A `gpp` active role with `controle_gpp_enabled` and GPP capability starts on `Controle GPP`.
    - Collaborator, lead, and admin sessions still start on `Hoje`.
    - Disabled feature flag or missing GPP actions starts on `Hoje` and does not render a GPP shell.
  </acceptance_criteria>
  <done>Initial mobile route honors the GPP role without moving other roles away from Hoje.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Controle GPP hub screen</name>
  <files>apps/mobile/src/capture/ControleGppScreen.tsx, apps/mobile/src/capture/gpp-copy.ts, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx</files>
  <read_first>apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/capture-theme.ts, apps/mobile/src/capture/AjustesScreen.tsx, apps/mobile/src/capture/offline-sync-ui.tsx, .planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-UI-SPEC.md</read_first>
  <action>Create `ControleGppScreen.tsx` as a one-column mobile `ScrollView` using `ScreenHeader`, existing action components, `captureColors`, and the approved copy. Render primary actions `Registrar avaria` and `Solicitar compra interna`, plus rows for `Minhas pendencias` and `Enviadas hoje`. Show compact queue/sent metadata when available, and show `StatusNotice` when there are local pending, retry, failure, or conflict states. Do not add a hero, dashboard stats, decorative cards, gradient, chart, or new UI library.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Hub title is exactly `Controle GPP`.
    - Hub body says `Registre avarias e compras internas sem misturar com Hoje. A central confirma o que foi recebido.`
    - The hub renders `Registrar avaria`, `Solicitar compra interna`, `Minhas pendencias`, and `Enviadas hoje`.
    - Actions meet existing 48px touch target patterns and use existing capture UI primitives.
    - Pending/conflict notices include text labels, not color-only state.
  </acceptance_criteria>
  <done>The hub communicates the next GPP actions and central-truth boundary in the approved mobile style.</done>
</task>

<task type="auto">
  <name>Task 3: Add separate shell entry without changing Today actions</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx, apps/mobile/src/capture/mobile-release-journeys.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/mobile-release-journeys.test.tsx, apps/mobile/src/capture/today-screen.test.tsx</read_first>
  <action>Add a separate authenticated shell/session-bar entry or equivalent mobile app entry labeled `Controle GPP` that navigates to the GPP route only when `canUseControleGpp` is true. Keep `TodayScreen` free of `Registrar avaria`, `Solicitar compra interna`, and GPP-specific action buttons. Preserve existing `Ajustes` navigation and Android/back route-stack behavior.</action>
  <verify>cmd /c pnpm.cmd --filter @validade-zero/mobile test</verify>
  <acceptance_criteria>
    - Eligible non-GPP roles can navigate to `Controle GPP` through a separate entry.
    - The GPP entry is hidden when the feature flag is disabled or required actions are missing.
    - `TodayScreen` tests or navigation regression tests assert no `Registrar avaria` or `Solicitar compra interna` action appears in Hoje.
    - Existing Ajustes route preservation tests still pass.
  </acceptance_criteria>
  <done>Controle GPP is discoverable without blending GPP work into Hoje.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server session to mobile navigation | Mobile visibility uses server-owned feature flags and actions but does not replace backend authorization. |
| GPP route to Hoje route | A new operational entry must not change daily validation semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Elevation of privilege | GPP entry visibility | mitigate | Gate visible entry on `controle_gpp_enabled` and GPP actions, and keep API denial handling for backend truth. |
| T-18-05 | Tampering | Initial route resolution | mitigate | Tests prove only eligible GPP role starts in Controle GPP; other roles keep Hoje. |
| T-18-06 | Spoofing | Role/capability display | mitigate | Use `SessionContextResponse` facts; do not introduce local role override controls. |
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile test`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- GPP role opens directly into Controle GPP when enabled and authorized.
- Collaborator/lead/admin keep Hoje as the first daily surface.
- Controle GPP entry hides when feature flag or GPP capability is absent.
- Hub shows the four Phase 18 entries using approved mobile UI primitives.
- Today remains free of GPP action integration.
</success_criteria>

<output>
After completion, create `.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-02-SUMMARY.md`.
</output>
