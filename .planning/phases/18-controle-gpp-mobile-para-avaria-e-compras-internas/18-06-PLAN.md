---
phase: 18-controle-gpp-mobile-para-avaria-e-compras-internas
plan: "06"
type: execute
wave: 6
depends_on: ["18-05"]
gap_closure: true
files_modified:
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/mobile-gpp-navigation.test.tsx
  - apps/mobile/src/capture/gpp-pending-screen.test.tsx
autonomous: true
requirements: ["GPP-08"]
user_setup: []
must_haves:
  truths:
    - "A central rejection remains visible as Conflito de GPP until the operator resolves it."
    - "Discard stays disabled without a local justification."
    - "After a justification is supplied, discard persists the reason locally and removes the record from the active queue."
  artifacts:
    - path: "apps/mobile/src/capture/CaptureApp.tsx"
      provides: "Route-level discard handler wired to the capture repository"
      contains: "discardGppPending"
    - path: "apps/mobile/src/capture/mobile-gpp-navigation.test.tsx"
      provides: "Integration regression from conflict UI through repository refresh"
      contains: "Descartar registro deste aparelho"
  key_links:
    - from: "apps/mobile/src/capture/GppPendingScreen.tsx"
      to: "apps/mobile/src/capture/CaptureApp.tsx"
      via: "onDiscardConflict"
      pattern: "justification -> repository.discardGppPending -> refreshGppLists"
---

<objective>
Close the Phase 18 UAT gap where the conflict discard button is enabled but performs no action.

Purpose: preserve accountable local conflict resolution without sending a discarded record to the central GPP service.
Output: route-level discard wiring plus an integration regression proving the justification is persisted and the conflict leaves the active queue.
</objective>

<context>
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-UAT.md
@.planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-05-PLAN.md
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/GppPendingScreen.tsx
@apps/mobile/src/capture/repository.ts
@apps/mobile/src/capture/gpp-pending-screen.test.tsx
@apps/mobile/src/capture/mobile-gpp-navigation.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire justified conflict discard through the repository</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx, apps/mobile/src/capture/gpp-pending-screen.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/GppPendingScreen.tsx, apps/mobile/src/capture/repository.ts, apps/mobile/src/capture/gpp-offline-queue.test.ts</read_first>
  <action>Add a CaptureApp handler for `onDiscardConflict` that trims and rejects an empty justification defensively, calls `repository.discardGppPending` with `localId`, the supplied justification, and a current ISO `discardedAt`, then refreshes the GPP pending/sent lists. Pass the handler to `GppPendingScreen`. Keep discard device-local: do not call the central GPP client and do not convert discard into central success. Add an integration regression that seeds a conflict, enters a reason, invokes discard through the routed screen, and verifies the active conflict disappears while the stored record is `discarded` with the exact justification. Preserve the existing component test that proves the destructive action remains disabled for an empty reason.</action>
  <verify>pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/gpp-pending-screen.test.tsx apps/mobile/src/capture/mobile-gpp-navigation.test.tsx apps/mobile/src/capture/gpp-offline-queue.test.ts &amp;&amp; pnpm.cmd --filter @validade-zero/mobile typecheck</verify>
  <acceptance_criteria>
    - `CaptureApp` passes a non-optional working `onDiscardConflict` handler in the sector pending route.
    - Empty or whitespace-only justification cannot discard a conflict.
    - Valid discard persists `state: discarded`, `discardJustification`, and `discardedAt` locally.
    - The discarded item is absent from `listGppPending()` and from the rendered active conflict queue after refresh.
    - No central GPP mutation is issued by the discard action.
  </acceptance_criteria>
  <done>The emulator UAT path that previously did nothing now resolves the local conflict accountably.</done>
</task>

</tasks>

<verification>
- [ ] Focused pending/navigation/offline-queue tests pass.
- [ ] Mobile typecheck passes.
- [ ] Emulator retest: justification enables discard; one press removes the active conflict.
</verification>

<success_criteria>
- The sole issue in `18-UAT.md` is closed without expanding Phase 18 into the future GPP central mobile surface.
- Discard remains local, justified, persisted, and auditable in device state.
</success_criteria>

<output>
After execution, update `18-UAT.md` Test 5 with the emulator retest and create `18-06-SUMMARY.md`.
</output>
