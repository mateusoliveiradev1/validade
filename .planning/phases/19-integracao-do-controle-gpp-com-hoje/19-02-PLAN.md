---
phase: 19-integracao-do-controle-gpp-com-hoje
plan: "02"
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - packages/domain/src/linked-gpp-removal.ts
  - packages/domain/src/linked-gpp-removal.test.ts
  - packages/domain/src/index.ts
  - packages/contracts/src/capture.ts
  - packages/contracts/src/capture.test.ts
  - packages/contracts/src/tasks.ts
  - packages/contracts/src/tasks.test.ts
  - packages/contracts/src/gpp.ts
  - packages/contracts/src/gpp.test.ts
autonomous: true
requirements: ["GPP-09"]
must_haves:
  truths:
    - "D-05/D-07: Removed quantity becomes independent physical truth; a partial remainder stays active in Hoje."
    - "D-08: Only structured `sales_area_presence_uncertain` conflict impact can reproject a removed quantity for physical review."
    - "D-10/D-11: Product, lot, quantity, and unit remain typed; above-pending removal is rejected rather than clamped."
    - "D-12: Missing code or unknown legacy unit can preserve removal but cannot produce a valid GPP transport payload."
    - "D-13: Same-code lots remain separate through source task and lot provenance."
    - "D-03/D-09: `awaiting_destination` is a durable actionable post-removal state, distinct from transport pending, and `ready` preserves a reviewed destination before send."
  artifacts:
    - path: "packages/domain/src/linked-gpp-removal.ts"
      provides: "Pure full/partial removal and physical-impact policy"
      exports: ["calculateLinkedRemoval", "resolveLinkedPhysicalImpact"]
    - path: "packages/contracts/src/gpp.ts"
      provides: "Strict linked-Hoje provenance, create request, and structured outcome schemas"
      contains: "GppHojeProvenanceSchema"
    - path: "packages/contracts/src/tasks.ts"
      provides: "Typed Today linked-GPP context and follow-up projection"
      contains: "TodayLinkedGppContextSchema"
  key_links:
    - from: "packages/contracts/src/tasks.ts"
      to: "packages/domain/src/linked-gpp-removal.ts"
      via: "pending quantity/unit and full/partial result contract"
      pattern: "remaining"
    - from: "packages/contracts/src/gpp.ts"
      to: "packages/contracts/src/tasks.ts"
      via: "source task/lot provenance and structured physical impact"
      pattern: "sourceTaskId|sourceLotId|physicalImpact"
---

<objective>
Define the additive domain and runtime contracts for a linked Hoje removal.

Purpose: Every later persistence, API, repository, and UI plan needs one strict vocabulary for removed versus remaining quantity, truthful unit recovery, source provenance, and conflict impact without breaking Phase 18 standalone payloads.
Output: Pure removal policy, strict additive capture/task/GPP schemas, exports, and focused contract tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-RESEARCH.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-01-SUMMARY.md
@packages/domain/src/tasks.ts
@packages/contracts/src/capture.ts
@packages/contracts/src/tasks.ts
@packages/contracts/src/gpp.ts
</context>

<interfaces>
From the approved Phase 19 research and pattern map:
- Canonical quantity is `GppQuantitySchema`: `{ value: positive finite number; unit: GppQuantityUnit }`.
- Linked provenance carries `source: "hoje"`, `sourceTaskId`, `sourceTaskActiveKey`, `sourceLotId`, `lotIdentity`, and `removalConfirmedAt`.
- Physical impact is exactly `"none" | "sales_area_presence_uncertain"`.
- Physical state and delivery state are separate discriminants. `awaiting_destination`, `awaiting_code`, `awaiting_unit`, and `ready` are durable actionable pre-transport states; none is a transport-pending record.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-02-01: Implement pure linked-removal arithmetic and physical-impact policy</name>
  <files>packages/domain/src/linked-gpp-removal.ts, packages/domain/src/linked-gpp-removal.test.ts, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/linked-gpp-removal.ts, packages/domain/src/index.ts, packages/domain/src/tasks.ts, packages/domain/src/tasks.test.ts, packages/contracts/src/gpp.ts, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-RESEARCH.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md</read_first>
  <behavior>
    - Full removal returns `physicalState: "removed"` and zero remaining in the same unit.
    - Partial removal returns `physicalState: "partially_removed"` and an exact positive remainder.
    - Zero, negative, NaN, infinity, unit mismatch, and removed-above-pending inputs are rejected with stable error codes; none is silently clamped.
    - `physicalImpact: "none"` preserves the recorded physical result.
    - `physicalImpact: "sales_area_presence_uncertain"` returns `physicalState: "review_required"` only for the affected quantity.
  </behavior>
  <action>Write failing table-driven Vitest cases first, then create dependency-free `calculateLinkedRemoval(input)` and `resolveLinkedPhysicalImpact(input)` in `linked-gpp-removal.ts`. Use typed quantity objects with explicit units, exact subtraction, and stable domain error codes `invalid_removed_quantity`, `quantity_unit_mismatch`, and `removed_above_pending`. Return discriminated full/partial results with removed and remaining quantities; never call React, Zod, repositories, or `Math.min`. Make the physical-impact function accept only the structured two-value impact union and require an affected quantity for `sales_area_presence_uncertain`. Export the module from `packages/domain/src/index.ts`.</action>
  <verify>
    <automated>cmd /c "pnpm.cmd exec vitest run --config vitest.config.ts --project domain packages/domain/src/linked-gpp-removal.test.ts &amp;&amp; pnpm.cmd exec stryker run stryker.config.json --mutate packages/domain/src/linked-gpp-removal.ts --thresholds.break 100 --reporters clear-text"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/src/linked-gpp-removal.test.ts` exists and covers full, partial, zero, negative, NaN, infinity, unit mismatch, above-pending, and both physical-impact variants.
    - No implementation path clamps removed quantity or defaults a unit.
    - `calculateLinkedRemoval` returns exact removed/remaining quantities and a discriminated physical state.
    - Only `sales_area_presence_uncertain` can produce `review_required`.
    - The targeted Stryker run mutates only `packages/domain/src/linked-gpp-removal.ts` and exits zero at the scoped 100% break threshold; any survivor in a safety-critical branch fails the task until killed or explicitly reviewed and converted into a stronger test before rerun.
    - `packages/domain/src/index.ts` exports the new module and the focused domain command passes.
  </acceptance_criteria>
  <done>D-05, D-07, D-08, and D-11 have one deterministic tested policy before persistence work begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 19-02-02: Add strict additive capture, Today, and linked-GPP contracts</name>
  <files>packages/contracts/src/capture.ts, packages/contracts/src/capture.test.ts, packages/contracts/src/tasks.ts, packages/contracts/src/tasks.test.ts, packages/contracts/src/gpp.ts, packages/contracts/src/gpp.test.ts</files>
  <read_first>packages/contracts/src/capture.ts, packages/contracts/src/capture.test.ts, packages/contracts/src/tasks.ts, packages/contracts/src/tasks.test.ts, packages/contracts/src/gpp.ts, packages/contracts/src/gpp.test.ts, packages/contracts/src/authorization.ts, packages/domain/src/linked-gpp-removal.ts, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md</read_first>
  <behavior>
    - Existing Phase 18 `GppAvariaCreateRequestSchema` payloads continue to parse unchanged.
    - A linked request requires strict Hoje source task/active key/lot/identity/removal timestamp and a valid quantity/unit.
    - New/known capture lots preserve quantity unit and source; legacy unitless lots project an explicit unit-recovery state instead of `"un"`.
    - A missing product code can exist in the physical receipt context but cannot parse as a linked transport request.
    - `awaiting_destination` parses only with immutable physical receipt/provenance and without destination or outbox fields; `ready` requires one reviewed linked destination and still is not transport pending.
    - Conflict variants require structured physical impact and affected quantity only when sales-area presence is uncertain.
  </behavior>
  <action>Write failing compatibility and negative tests first. Extend capture contracts additively with paired `quantityUnit` and `quantityUnitSource: "observed" | "operator_confirmed"` fields so both are present together for known data while legacy unitless rows remain parseable. Add `TodayLinkedGppContextSchema` as a discriminated known-unit versus `requires_unit_confirmation` projection, with structured product code, pending quantity, source task, and lot identifiers; do not overload display strings. Add strict `GppHojeProvenanceSchema`, `GppLinkedAvariaCreateRequestSchema`, linked receipt/follow-up schemas, and a structured linked mutation outcome union containing `central_confirmed`, `replayed`, `correction_required`, and `conflict` variants. Define the follow-up lifecycle so `awaiting_destination` is the first durable state created with the receipt and forbids destination/outbox fields; destination selection advances to `awaiting_code`, `awaiting_unit`, or `ready`, and `ready` requires reviewed destination/finality while remaining pre-transport. The conflict variant carries `physicalImpact: "none" | "sales_area_presence_uncertain"` and requires affected quantity only for the latter. Keep `GppAvariaCreateRequestSchema` and Phase 18 response variants behaviorally compatible. Use `.strict()`, discriminated unions, and `.superRefine()` to reject inconsistent task/lot/unit/quantity/idempotency provenance. Do not add a dependency or expose `transferencia` in the linked-Hoje destination subset.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project contracts packages/contracts/src/capture.test.ts packages/contracts/src/gpp.test.ts packages/contracts/src/tasks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Old Phase 18 standalone avaria requests and responses parse in regression tests.
    - Linked requests reject missing source task, active key, source lot, lot identity, removal timestamp, quantity unit, or idempotency key.
    - Capture contracts accept legacy unitless rows only as explicit recovery state and never synthesize unit `"un"`.
    - Missing product code is representable in a durable physical context but cannot enter `GppLinkedAvariaCreateRequestSchema`.
    - `awaiting_destination` is representable without network, code, unit, or chosen destination, rejects outbox/transport fields, and remains actionable after serialization; `ready` requires the reviewed destination and rejects transport-pending semantics.
    - Same product code with different source lot/task values remains distinguishable.
    - Structured conflicts reject text-only physical-impact inference and invalid affected-quantity combinations.
    - The focused contracts command and contracts typecheck pass.
  </acceptance_criteria>
  <done>D-08, D-10, D-11, D-12, D-13, and Phase 18 backward compatibility are runtime-enforced.</done>
</task>

</tasks>

<threat_model>
## ASVS Baseline

- Checklist: OWASP ASVS 5.0.0, Level 1. The repository pins `security_asvs_level: 1` but no version; Phase 19 uses the current canonical 5.0.0 checklist from the [official release](https://github.com/OWASP/ASVS/releases/tag/latest) and its [versioned requirements CSV](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.csv).

## Assets

- Sales-area quantity truth and lot identity.
- Linked source provenance and idempotency key.
- Backward-compatible Phase 18 GPP contracts.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted mobile values to runtime schemas | Quantity, code, unit, task, lot, and provenance require strict validation. |
| Legacy unitless capture data to linked GPP eligibility | Unknown units must trigger recovery rather than fabricated operational data. |
| Central conflict response to physical-risk projection | UI/repository behavior must consume typed impact, not localized message text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS 5.0.0 L1 controls | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------------------|-------------|-----------------|
| T-19-02-01 | Tampering | `calculateLinkedRemoval` and quantity schemas | HIGH | V2.2.1, V2.2.2 | mitigate | Reject invalid, mismatched, and above-pending quantities with stable error codes; prohibit silent clamps/default units. Evidence binding: Task 19-02-01 `<verify>` runs focused Vitest and targeted Stryker with `--mutate packages/domain/src/linked-gpp-removal.ts --thresholds.break 100`; acceptance requires every invalid quantity/unit case, exact full/partial results, no clamp/default, typed physical impact, and zero surviving safety-critical mutants. |
| T-19-02-02 | Spoofing / Tampering | `GppHojeProvenanceSchema` | HIGH | V2.2.1, V2.2.2, V8.2.2 | mitigate | Strict required task/active key/lot/identity fields and cross-field refinements bind the request to one source. Evidence binding: Task 19-02-02 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project contracts packages/contracts/src/capture.test.ts packages/contracts/src/gpp.test.ts packages/contracts/src/tasks.test.ts`; acceptance requires rejection of every missing provenance/unit/idempotency field, same-code/different-lot distinction, and invalid conflict combinations. |
| T-19-02-03 | Repudiation | Linked outcome union | MEDIUM | V2.3.1 | mitigate | Preserve idempotency/provenance and allow success states only as explicit `central_confirmed` or `replayed` variants. |

**Fail-closed rule:** Any unresolved HIGH-severity finding blocks this plan and every dependent plan/wave. It cannot be implicitly accepted or deferred; execution resumes only after the cited task `<verify>` succeeds and every cited `<acceptance_criteria>` item is evidenced.

## Verification

- Domain tests exercise malformed quantities and both physical-impact variants.
- Contract tests exercise strict provenance, legacy unit recovery, missing code, and Phase 18 compatibility.
</threat_model>

<verification>
- [ ] `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project domain packages/domain/src/linked-gpp-removal.test.ts`
- [ ] `cmd /c pnpm.cmd exec stryker run stryker.config.json --mutate packages/domain/src/linked-gpp-removal.ts --thresholds.break 100 --reporters clear-text`
- [ ] `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project contracts packages/contracts/src/capture.test.ts packages/contracts/src/gpp.test.ts packages/contracts/src/tasks.test.ts`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/domain typecheck`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/contracts typecheck`
</verification>

<success_criteria>
- Domain arithmetic cannot hide a partial or invalid physical quantity.
- Contracts preserve source task/lot/unit truth and separate incomplete physical receipts from valid GPP transport payloads.
- `awaiting_destination` and `ready` preserve actionable pre-transport work without being mislabeled or queued as offline transport failure.
- Structured conflict impact is the only contract path that can request physical review.
- Existing Phase 18 standalone avaria/purchase contracts remain compatible.
</success_criteria>

<output>
After execution, create `.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-02-SUMMARY.md`.
</output>
