---
phase: 19-integracao-do-controle-gpp-com-hoje
plan: "03"
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - packages/database/drizzle/0019_phase_19_linked_gpp_today.sql
  - packages/database/src/gpp-repository.ts
  - packages/database/src/repositories.test.ts
  - apps/api/src/gpp.ts
  - apps/api/src/gpp.test.ts
autonomous: true
requirements: ["GPP-09"]
must_haves:
  truths:
    - "D-03/D-04: A removed product creates one reviewed linked movement with queryable source provenance."
    - "D-10/D-13: Product and lot identity remain immutable and same-code lots stay separate."
    - "D-15/D-19: Idempotent replay returns the original central fact without duplicate rows or false success."
    - "D-16/D-17: Validation and conflict responses stay structured correction/conflict states."
    - "Actor and store claims are derived from authenticated server scope; cross-store provenance is rejected."
  artifacts:
    - path: "packages/database/drizzle/0019_phase_19_linked_gpp_today.sql"
      provides: "Queryable linked-Hoje provenance columns, constraints, and indexes"
    - path: "packages/database/src/gpp-repository.ts"
      provides: "Store-scoped idempotent linked avaria persistence and audit"
      contains: "createLinkedAvaria"
    - path: "apps/api/src/gpp.ts"
      provides: "Authenticated linked-Hoje create endpoint"
      contains: "/gpp/avarias/linked-hoje"
  key_links:
    - from: "apps/api/src/gpp.ts"
      to: "packages/database/src/gpp-repository.ts"
      via: "server-derived store/actor plus strict linked request"
      pattern: "createLinkedAvaria"
    - from: "packages/database/src/gpp-repository.ts"
      to: "packages/database/drizzle/0019_phase_19_linked_gpp_today.sql"
      via: "store/source-task/source-lot provenance and audit round-trip"
      pattern: "source_task_id|source_lot_id"
---

<objective>
Persist and authorize linked-Hoje GPP records centrally.

Purpose: A central acknowledgement is credible only when source provenance, actor/store authority, idempotency, audit, and conflict impact survive the database/API boundary.
Output: Forward-only migration, store-scoped repository behavior, dedicated additive endpoint, and database/API security tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-RESEARCH.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-02-SUMMARY.md
@packages/database/drizzle/0018_phase_17_gpp_control.sql
@packages/database/src/gpp-repository.ts
@apps/api/src/gpp.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-03-01: Persist linked provenance, replay, and append-only audit</name>
  <files>packages/database/drizzle/0019_phase_19_linked_gpp_today.sql, packages/database/src/gpp-repository.ts, packages/database/src/repositories.test.ts</files>
  <read_first>packages/database/drizzle/0019_phase_19_linked_gpp_today.sql, packages/database/drizzle/0018_phase_17_gpp_control.sql, packages/database/src/gpp-repository.ts, packages/database/src/repositories.test.ts, packages/contracts/src/gpp.ts, packages/contracts/src/tasks.ts, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md</read_first>
  <behavior>
    - Linked provenance and acknowledgement round-trip through central persistence.
    - Same code/different source lots create separate movements.
    - Same store/idempotency key replays one result without a second side effect.
    - Cross-store source task/lot references are rejected.
    - Audit state contains source task, lot, quantity, destination, actor, and acknowledgement.
  </behavior>
  <action>Write failing repository tests first. Add a forward-only `0019_phase_19_linked_gpp_today.sql` migration that makes `source_task_id`, `source_task_active_key`, `source_lot_id`, structured lot identity, and `removal_confirmed_at` queryable on linked GPP rows; add store-first source lookup indexes, check constraints, and an idempotency/provenance uniqueness rule that does not collapse different lots sharing a product code. Extend `gpp-repository.ts` with `createLinkedAvaria`, replay-before-side-effect behavior, store-scoped source task/lot validation, and append-only audit projections. Repository inputs receive authenticated store/actor separately from the parsed request. Do not perform a remote schema push, deploy, or alter existing standalone avaria/purchase persistence.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project database packages/database/src/repositories.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration contains store/source-task/source-lot indexes and constraints; provenance is not hidden only in JSON.
    - Same-code/different-lot tests produce distinct movements.
    - Duplicate idempotency tests return replay and create one row/audit sequence.
    - Cross-store provenance tests fail without leaking the other store's data.
    - Audit projections include source task, lot, quantity/unit, destination/finality, actor, and acknowledgement state.
    - Focused database tests pass without any remote schema push.
  </acceptance_criteria>
  <done>Central linked records are store-isolated, queryable, idempotent, and auditable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 19-03-02: Expose an authenticated additive linked-Hoje create route</name>
  <files>apps/api/src/gpp.ts, apps/api/src/gpp.test.ts</files>
  <read_first>apps/api/src/gpp.ts, apps/api/src/gpp.test.ts, packages/contracts/src/gpp.ts, packages/contracts/src/authorization.ts, packages/database/src/gpp-repository.ts, .planning/phases/17-controle-gpp-web-api-com-tempo-real/17-CONTEXT.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md</read_first>
  <behavior>
    - Disabled feature or missing `gpp.avaria.create` capability is denied server-side.
    - Client actor/store display claims never override authenticated actor/store.
    - Valid linked create returns central confirmation; duplicate request returns replay.
    - Business/validation rejection is correction-required, not offline.
    - Conflict response carries typed physical impact and sanitized reason.
  </behavior>
  <action>Write API integration/security cases first. Add `POST /gpp/avarias/linked-hoje` using `GppLinkedAvariaCreateRequestSchema`. Enforce `controle_gpp_enabled` and `gpp.avaria.create`, resolve the effective store and actor from authenticated membership, reject a mismatched requested store or source task/lot outside that store, and pass only server-derived actor/store into `createLinkedAvaria`. Return the linked discriminated outcomes: `central_confirmed`, `replayed`, `correction_required`, or `conflict` with structured `physicalImpact`; sanitize public errors and never map 4xx/409 business failures to transport pending. Keep `POST /gpp/avarias` and purchase routes behaviorally unchanged.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project api apps/api/src/gpp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Feature-disabled and capability-denied requests return denial without repository mutation.
    - Forged actor/store/source provenance cannot change server-derived actor/store or read another store.
    - Valid/replayed responses are the only success variants and duplicate retry creates no duplicate.
    - Validation, permission, feature, and business rejections remain correction/conflict states with public-safe messages.
    - Existing standalone Phase 18 avaria and purchase route tests still pass.
    - Focused API tests and API typecheck pass.
  </acceptance_criteria>
  <done>The central API can acknowledge a linked movement without trusting client authority or fabricating offline/success state.</done>
</task>

</tasks>

<threat_model>
## ASVS Baseline

- Checklist: OWASP ASVS 5.0.0, Level 1. The repository pins `security_asvs_level: 1` but no version; Phase 19 uses the current canonical 5.0.0 checklist from the [official release](https://github.com/OWASP/ASVS/releases/tag/latest) and its [versioned requirements CSV](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.csv).

## Assets

- Store-isolated source task/lot provenance, central GPP record, actor identity, and audit trail.
- Idempotency receipts and acknowledgement truth.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile request to authenticated API | All request fields are untrusted; actor/store authority belongs to the server session. |
| API to central repository | Store-scoped provenance lookup and replay must precede mutation. |
| Database outcome to public response | Errors must preserve correction/conflict semantics without leaking internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS 5.0.0 L1 controls | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------------------|-------------|-----------------|
| T-19-03-01 | Spoofing / Elevation of privilege | linked-Hoje API route | HIGH | V8.2.1, V8.2.2, V8.3.1 | mitigate | Enforce feature/capability and derive actor/store from authenticated membership. Evidence binding: Task 19-03-02 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project api apps/api/src/gpp.test.ts`; acceptance requires denial without mutation, rejection of forged actor/store/source provenance, and unchanged legacy routes. |
| T-19-03-02 | Tampering / Repudiation | repository idempotency/audit | HIGH | V2.3.1, V8.2.2 | mitigate | Replay before side effects, unique store-scoped provenance, append-only audit facts, and duplicate tests. Evidence binding: Task 19-03-01 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project database packages/database/src/repositories.test.ts`; acceptance requires distinct same-code lots, one-row replay, cross-store rejection, and complete audit projection. |
| T-19-03-03 | Information disclosure | API error boundary | HIGH | V8.2.2, V15.3.1 | mitigate | Return only typed public-safe codes/impact and do not reveal other-store records. Evidence binding: Task 19-03-02 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project api apps/api/src/gpp.test.ts`; acceptance requires forged cross-store provenance to reveal no data and permission/business errors to remain sanitized correction/conflict states. |

**Fail-closed rule:** Any unresolved HIGH-severity finding blocks this plan and every dependent plan/wave. It cannot be implicitly accepted or deferred; execution resumes only after the cited task `<verify>` succeeds and every cited `<acceptance_criteria>` item is evidenced.

## Verification

- Database tests cover round-trip, isolation, replay, and audit.
- API tests cover server-owned claims, capability gates, sanitized errors, and legacy route regression.
</threat_model>

<verification>
- [ ] Database and API focused commands pass.
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/database typecheck`
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/api typecheck`
</verification>

<success_criteria>
- Central linked records retain source lot/task truth and never merge same-code lots.
- Only authenticated same-store actors with the required capability can create them.
- Central success, replay, correction, and conflict remain distinct and auditable.
</success_criteria>

<output>After execution, create `.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-03-SUMMARY.md`.</output>
