---
phase: 19-integracao-do-controle-gpp-com-hoje
plan: "04"
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - apps/mobile/src/capture/linked-gpp-state.ts
  - apps/mobile/src/capture/linked-gpp-state.test.ts
  - apps/mobile/src/capture/linked-gpp-repository.ts
  - apps/mobile/src/capture/repository.ts
  - apps/mobile/src/capture/memory-repository.ts
  - apps/mobile/src/capture/sqlite-repository.ts
  - apps/mobile/src/capture/sqlite-migrations.ts
  - apps/mobile/src/capture/capture-repository.test.ts
  - apps/mobile/src/capture/sqlite-migrations.test.ts
autonomous: true
requirements: ["GPP-09"]
must_haves:
  truths:
    - "D-05/D-09: Physical removal is durable independently of GPP delivery and survives restart."
    - "D-07: Full and partial removal atomically update the linked receipt and Hoje remainder."
    - "D-11/D-12: Above-pending quantity is rejected; missing code/unit persists as recovery, never local transport pending."
    - "D-13/D-15/D-17: Lot/task/idempotency provenance and the physical receipt survive retry, conflict, and justified discard."
    - "D-03/D-09: Physical confirmation atomically creates actionable `awaiting_destination` before destination selection; navigation, process death, and missing network cannot hide it."
  artifacts:
    - path: "apps/mobile/src/capture/linked-gpp-state.ts"
      provides: "Discriminated physical and delivery state vocabulary"
    - path: "apps/mobile/src/capture/linked-gpp-repository.ts"
      provides: "Atomic linked removal receipt repository contract"
    - path: "apps/mobile/src/capture/sqlite-migrations.ts"
      provides: "Forward-only durable linked receipt/event schema"
  key_links:
    - from: "apps/mobile/src/capture/sqlite-repository.ts"
      to: "apps/mobile/src/capture/repository.ts"
      via: "one transaction persists physical receipt and full/partial Today update"
      pattern: "withTransactionAsync"
    - from: "apps/mobile/src/capture/memory-repository.ts"
      to: "apps/mobile/src/capture/sqlite-repository.ts"
      via: "shared parity suite"
      pattern: "confirmLinkedRemoval"
---

<objective>
Make the physical-removal receipt durable before any GPP delivery attempt.

Purpose: A crash, missing code, offline transport, central conflict, or local discard must never erase what left the sales area or the remaining active quantity.
Output: Discriminated linked state, repository contract, in-memory/SQLite implementations, forward migration, and parity tests.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-RESEARCH.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-03-SUMMARY.md
@apps/mobile/src/capture/repository.ts
@apps/mobile/src/capture/memory-repository.ts
@apps/mobile/src/capture/sqlite-repository.ts
@apps/mobile/src/capture/sqlite-migrations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-04-01: Define durable linked state and in-memory repository behavior</name>
  <files>apps/mobile/src/capture/linked-gpp-state.ts, apps/mobile/src/capture/linked-gpp-state.test.ts, apps/mobile/src/capture/linked-gpp-repository.ts, apps/mobile/src/capture/repository.ts, apps/mobile/src/capture/memory-repository.ts, apps/mobile/src/capture/capture-repository.test.ts</files>
  <read_first>apps/mobile/src/capture/linked-gpp-state.ts, apps/mobile/src/capture/linked-gpp-repository.ts, apps/mobile/src/capture/repository.ts, apps/mobile/src/capture/memory-repository.ts, apps/mobile/src/capture/capture-repository.test.ts, apps/mobile/src/capture/gpp-offline-queue.ts, packages/contracts/src/gpp.ts, packages/contracts/src/tasks.ts, packages/domain/src/linked-gpp-removal.ts</read_first>
  <behavior>
    - Confirmed removal atomically persists first as `awaiting_destination` without a destination or GPP outbox row.
    - Choosing a destination advances durably to `awaiting_code`, `awaiting_unit`, or `ready`; none of these pre-transport states is `pending_retry`.
    - Full removal resolves only the removed risk; partial removal preserves exact active remainder.
    - Above-pending input changes nothing and returns reconference error.
    - Discarding a delivery attempt retains the physical receipt and justification event.
  </behavior>
  <action>Write state/repository tests first. Create discriminated physical states `removed | partially_removed | review_required` and delivery states `awaiting_destination | awaiting_code | awaiting_unit | ready | sending | pending_retry | correction_required | conflict | central_confirmed | replayed | discarded` so invalid field combinations cannot be constructed. Define `confirmLinkedRemoval`, `selectLinkedDestination`, list/get follow-up, append delivery event, and reproject-for-review repository operations. Implement them in memory so physical fields (source task/lot, product identity, removed/remaining quantity/unit, actor label, removal timestamp, idempotency key) are immutable; delivery changes append auditable events. `confirmLinkedRemoval` invokes the Phase 19 domain policy and atomically persists the receipt plus Hoje full/partial mutation with delivery state exactly `awaiting_destination` before any destination is chosen. `selectLinkedDestination` appends the selected destination/finality and derives `awaiting_code`, `awaiting_unit`, or `ready`; it never sends, queues, or labels transport pending. Reject above-pending without mutation, keep a partial remainder active, and ensure missing code/unit never calls `saveGppPending`.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/linked-gpp-state.test.ts apps/mobile/src/capture/capture-repository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Physical and delivery truths are separate discriminated fields with no optional-field invalid combinations.
    - Memory tests cover full, partial, above-pending, `awaiting_destination`, destination-to-code/unit/ready transitions, discard, and same-code/different-lot records.
    - Partial remainder stays in active Today repository truth with unchanged unit.
    - Immediately after physical confirmation the receipt is queryable as `awaiting_destination`, including with no network, and creates zero GPP outbox records.
    - After destination selection, missing code is exactly `awaiting_code`/`Falta informar o código`; `ready` preserves the reviewed destination before send; both create zero GPP outbox records.
    - Discard retains immutable removal data plus non-empty justification/timestamp.
  </acceptance_criteria>
  <done>The in-memory adapter proves the two-truth aggregate before SQLite wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 19-04-02: Persist the linked aggregate atomically in SQLite</name>
  <files>apps/mobile/src/capture/sqlite-repository.ts, apps/mobile/src/capture/sqlite-migrations.ts, apps/mobile/src/capture/capture-repository.test.ts, apps/mobile/src/capture/sqlite-migrations.test.ts</files>
  <read_first>apps/mobile/src/capture/sqlite-repository.ts, apps/mobile/src/capture/sqlite-migrations.ts, apps/mobile/src/capture/sqlite-migrations.test.ts, apps/mobile/src/capture/capture-repository.test.ts, apps/mobile/src/capture/linked-gpp-repository.ts, apps/mobile/src/capture/memory-repository.ts, apps/mobile/src/capture/gpp-offline-queue.ts</read_first>
  <behavior>
    - App restart reloads `awaiting_destination` after interruption between physical confirmation and destination selection.
    - App restart reloads the selected destination and `ready` review after interruption before send.
    - Receipt plus full/partial Today mutation is one SQLite transaction.
    - A simulated transaction failure leaves neither a hidden resolved risk nor a partial receipt.
    - Forward migration preserves legacy rows and marks unknown unit for explicit recovery.
  </behavior>
  <action>Add a forward-only local migration with an immutable linked-removal receipt table and append-only delivery-event table, indexed by source task/lot and idempotency key. Implement the repository contract in SQLite using `withTransactionAsync` so receipt persistence in `awaiting_destination` and full/partial Today/lot update commit together before destination selection. Persist destination selection/review as an append-only event so `awaiting_destination`, `awaiting_code`, `awaiting_unit`, and `ready` all restore deterministically after process death. Preserve legacy rows; when their unit is absent after destination selection, project `awaiting_unit` and require operator confirmation rather than writing `"un"`. Run the identical repository behavior suite against memory and SQLite, plus migration-from-legacy, rollback, navigation-away, and restart tests. Do not create a second retry queue.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/capture-repository.test.ts apps/mobile/src/capture/sqlite-migrations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - SQLite migration is forward-only, indexed, and preserves existing capture/GPP rows.
    - Full and partial transactions cannot persist only one side of physical receipt versus Today quantity state.
    - Restart tests reload interruption immediately after physical confirmation as `awaiting_destination`, and after destination review before send as `ready`, with the same source/quantity/destination and no outbox record.
    - Restart tests also reload missing-code/unit, partial, conflict, confirmed, and discarded states identically to memory.
    - Legacy unknown unit is explicit recovery and never silently `"un"`.
    - Memory/SQLite parity and mobile typecheck pass.
  </acceptance_criteria>
  <done>The durable physical receipt and active remainder survive process death with adapter parity.</done>
</task>

</tasks>

<threat_model>
## ASVS Baseline

- Checklist: OWASP ASVS 5.0.0, Level 1. The repository pins `security_asvs_level: 1` but no version; Phase 19 uses the current canonical 5.0.0 checklist from the [official release](https://github.com/OWASP/ASVS/releases/tag/latest) and its [versioned requirements CSV](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.csv).

## Assets

- Device-local physical-removal receipt, remaining sales-area quantity, and append-only delivery history.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI command to local transaction | A crash must not split physical receipt from Today quantity mutation. |
| Legacy SQLite data to linked eligibility | Unknown unit/code must remain explicit recovery state. |
| Delivery mutation to immutable physical fact | Retry/conflict/discard may update delivery only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS 5.0.0 L1 controls | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------------------|-------------|-----------------|
| T-19-04-01 | Tampering | removal quantity/local aggregate | HIGH | V2.2.1, V2.2.2 | mitigate | Domain validation plus immutable physical fields and above-pending no-mutation behavior. Evidence binding: Task 19-04-01 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/linked-gpp-state.test.ts apps/mobile/src/capture/capture-repository.test.ts`; acceptance requires full/partial/above-pending/missing-data/discard cases, exact active remainder, and immutable justified receipt. |
| T-19-04-02 | Repudiation | delivery event history | MEDIUM | V2.3.1 | mitigate | Append-only events retain actor, timestamps, idempotency, conflicts, and discard justification. |
| T-19-04-03 | Denial of service / Integrity | SQLite transaction/restart | HIGH | V2.3.1 | mitigate | Commit receipt and Today mutation as one ordered business operation with rollback/restart parity. Evidence binding: Task 19-04-02 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/capture-repository.test.ts apps/mobile/src/capture/sqlite-migrations.test.ts`; acceptance requires atomic full/partial persistence, restart parity, forward migration, and no fabricated unit. |
| T-19-04-04 | Integrity / Repudiation | post-confirmation destination gap | HIGH | V2.3.1 | mitigate | `confirmLinkedRemoval` atomically creates `awaiting_destination` with the receipt before navigation can leave the flow; destination review persists as `ready`, and both states restore after restart without outbox/transport semantics. Evidence binding: Tasks 19-04-01 and 19-04-02 run the focused state/repository commands; acceptance requires immediate queryability, navigation/restart recovery before destination and before send, preserved provenance/quantity/destination, and zero outbox rows. |

**Fail-closed rule:** Any unresolved HIGH-severity finding blocks this plan and every dependent plan/wave. It cannot be implicitly accepted or deferred; execution resumes only after the cited task `<verify>` succeeds and every cited `<acceptance_criteria>` item is evidenced.

## Verification

- Repository and migration suites cover atomicity, restart, legacy recovery, and retained audit facts.
</threat_model>

<verification>
- [ ] Both focused mobile repository commands pass.
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Physical removal can never be lost or falsely centralized because code/unit, transport, or central processing is incomplete.
- A crash or navigation away between physical confirmation, destination selection, review, and send always leaves an actionable durable follow-up.
- Partial remainder remains active and accurate after restart.
- Discard changes delivery state only and remains justified/auditable.
</success_criteria>

<output>After execution, create `.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-04-SUMMARY.md`.</output>
