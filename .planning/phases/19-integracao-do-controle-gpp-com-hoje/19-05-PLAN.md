---
phase: 19-integracao-do-controle-gpp-com-hoje
plan: "05"
type: execute
wave: 5
depends_on: ["19-04"]
files_modified:
  - apps/mobile/src/capture/gpp-client.ts
  - apps/mobile/src/capture/gpp-client.test.ts
  - apps/mobile/src/capture/gpp-offline-queue.ts
  - apps/mobile/src/capture/gpp-offline-queue.test.ts
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/mobile-gpp-navigation.test.tsx
autonomous: true
requirements: ["GPP-09"]
must_haves:
  truths:
    - "D-14/D-16: Only genuine transport failure after a valid request becomes `Pendente neste aparelho`; central rejection becomes `Corrigir envio`."
    - "D-15/D-18/D-19: Automatic/manual retry reuses original payload provenance and idempotency key; success requires confirmed/replayed acknowledgement."
    - "D-08/D-17: Typed conflict impact controls physical review; justified discard retains removal audit."
    - "D-21/D-22: Reuse the Phase 18 queue and existing refresh/resume behavior without Phase 20 queue or Phase 21 realtime work."
  artifacts:
    - path: "apps/mobile/src/capture/gpp-client.ts"
      provides: "Linked-Hoje API client and transport-versus-central result classifier"
    - path: "apps/mobile/src/capture/gpp-offline-queue.ts"
      provides: "Valid linked request retry/replay lifecycle using existing outbox"
    - path: "apps/mobile/src/capture/CaptureApp.tsx"
      provides: "Route-owned automatic/manual linked delivery orchestration"
  key_links:
    - from: "apps/mobile/src/capture/gpp-client.ts"
      to: "apps/mobile/src/capture/gpp-offline-queue.ts"
      via: "only `offline_pending_candidate` saves a valid linked request"
      pattern: "offline_pending_candidate"
    - from: "apps/mobile/src/capture/CaptureApp.tsx"
      to: "apps/mobile/src/capture/linked-gpp-repository.ts"
      via: "delivery outcomes append without erasing physical receipt"
      pattern: "central_confirmed|replayed|correction_required|conflict"
---

<objective>
Deliver linked records truthfully through the existing GPP transport and retry path.

Purpose: Connectivity, validation, conflict, replay, and discard must produce distinct durable outcomes while physical truth remains independent.
Output: Linked client method, existing-outbox extension, route-owned retry orchestration, and transport/idempotency regressions.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-RESEARCH.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-04-SUMMARY.md
@apps/mobile/src/capture/gpp-client.ts
@apps/mobile/src/capture/gpp-offline-queue.ts
@apps/mobile/src/capture/CaptureApp.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-05-01: Classify linked delivery outcomes and reuse the valid-payload outbox</name>
  <files>apps/mobile/src/capture/gpp-client.ts, apps/mobile/src/capture/gpp-client.test.ts, apps/mobile/src/capture/gpp-offline-queue.ts, apps/mobile/src/capture/gpp-offline-queue.test.ts</files>
  <read_first>apps/mobile/src/capture/gpp-client.ts, apps/mobile/src/capture/gpp-client.test.ts, apps/mobile/src/capture/gpp-offline-queue.ts, apps/mobile/src/capture/gpp-offline-queue.test.ts, apps/mobile/src/capture/gpp-flow-state.ts, packages/contracts/src/gpp.ts, apps/api/src/gpp.ts, apps/mobile/src/capture/linked-gpp-state.ts</read_first>
  <behavior>
    - Network/timeout failure after strict valid payload returns `offline_pending_candidate`.
    - HTTP validation, permission, feature, and business rejection return `correction_required`.
    - Typed conflict remains conflict with physical impact; it is not offline.
    - Confirmation/replay are the only success results.
    - Retry keeps the original idempotency key and creates no duplicate.
  </behavior>
  <action>Write client/outbox cases first. Add `createLinkedAvaria` against `/gpp/avarias/linked-hoje` and extend the current result classifier with strict linked outcomes. Only fetch reachability/transport exceptions after an explicit send attempt may return `offline_pending_candidate`; 4xx, feature/capability denial, validation, and business rejection map to `correction_required`, while typed conflicts preserve submitted/current data and `physicalImpact`. Extend the existing GPP pending payload union with only fully parsed linked requests after transport failure; do not enqueue `awaiting_destination`, `awaiting_code`, `awaiting_unit`, or `ready` receipts and do not add a second queue. Preserve payload, source task/lot, actor label, and original idempotency key through retry/replay.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/gpp-client.test.ts apps/mobile/src/capture/gpp-offline-queue.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Transport-only tests are the only path to `Pendente neste aparelho`.
    - 400/403/404/409 business responses never create local-pending or central-success state.
    - `awaiting_destination`, `awaiting_code`, `awaiting_unit`, and `ready` receipts are rejected by outbox payload parsing; only a genuine attempted-send transport failure creates `pending_retry`.
    - Retry/replay sends the original idempotency key and retains task/lot provenance.
    - Central confirmation/replay produces one local success fact; duplicate retry does not duplicate it.
  </acceptance_criteria>
  <done>D-14 through D-16 and D-19 are enforced in transport code rather than copy parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 19-05-02: Orchestrate retry, structured conflict impact, and retained discard audit</name>
  <files>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/gpp-offline-queue.test.ts, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx</files>
  <read_first>apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx, apps/mobile/src/capture/gpp-offline-queue.ts, apps/mobile/src/capture/GppPendingScreen.tsx, apps/mobile/src/capture/linked-gpp-repository.ts, apps/mobile/src/capture/sync-engine.ts, .planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-06-SUMMARY.md</read_first>
  <behavior>
    - App resume/connectivity return and `Sincronizar pendências GPP` call the same idempotent helper.
    - Conflict `physicalImpact: none` leaves removed risk resolved.
    - `sales_area_presence_uncertain` reprojects only affected quantity as physical review.
    - Justified local discard removes active delivery attempt but leaves physical receipt/audit.
  </behavior>
  <action>In `CaptureApp`, create one route-owned linked retry helper used by existing resume/connectivity hooks and the exact manual action `Sincronizar pendências GPP`; do not add a timer, dependency, realtime subscription, or Phase 20 queue. Apply outcomes to the linked repository: confirmation/replay append success; correction preserves submitted data/reason; conflict preserves submitted/current data; only structured `sales_area_presence_uncertain` invokes `reprojectLinkedRemovalForReview` for the affected quantity. Reuse Phase 18 justified discard and ensure it appends the reason/timestamp while leaving the linked physical receipt queryable. Refresh route projections from repository truth after every mutation.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/gpp-offline-queue.test.ts apps/mobile/src/capture/mobile-gpp-navigation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Automatic and manual retry use one helper and the original idempotency key.
    - Conflict-without-impact leaves active-risk count unchanged; conflict-with-impact reprojects only affected quantity as `Revisão física necessária`.
    - Correction/conflict remains visible until resolved; neither is labeled offline or success.
    - Whitespace discard is rejected; justified discard removes active delivery attempt and retains receipt/justification/timestamp.
    - No new queue, realtime code, timer dependency, build/deploy action, or Phase 20 response action is added.
  </acceptance_criteria>
  <done>D-08 and D-17 through D-22 have a durable, idempotent route orchestration.</done>
</task>

</tasks>

<threat_model>
## ASVS Baseline

- Checklist: OWASP ASVS 5.0.0, Level 1. The repository pins `security_asvs_level: 1` but no version; Phase 19 uses the current canonical 5.0.0 checklist from the [official release](https://github.com/OWASP/ASVS/releases/tag/latest) and its [versioned requirements CSV](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.csv).

## Assets

- Original valid payload/idempotency key, delivery state, central acknowledgement, and immutable physical receipt.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device transport to central API | Reachability failures differ from authenticated business responses. |
| Central outcome to local repository | Only typed impact may alter physical review projection. |
| Local retry/discard UI to audit | Repeated actions must be idempotent and justified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS 5.0.0 L1 controls | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------------------|-------------|-----------------|
| T-19-05-01 | Spoofing / Tampering | client result classifier | HIGH | V2.2.1, V2.2.2 | mitigate | Strict response parsing prevents business rejection from masquerading as offline/success. Evidence binding: Task 19-05-01 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/gpp-client.test.ts apps/mobile/src/capture/gpp-offline-queue.test.ts`; acceptance requires transport-only pending, no 4xx success/pending, strict complete payloads, preserved idempotency/provenance, and no duplicate confirmation. |
| T-19-05-02 | Denial of service / Repudiation | retry helper | MEDIUM | V2.3.1 | mitigate | Reuse the existing queue and one helper with the original idempotency key and replay tests. |
| T-19-05-03 | Tampering | physical-impact/discard transition | HIGH | V2.2.1, V2.3.1 | mitigate | Accept structured impact only; append discard reason/timestamp while retaining the immutable receipt. Evidence binding: Task 19-05-02 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/gpp-offline-queue.test.ts apps/mobile/src/capture/mobile-gpp-navigation.test.tsx`; acceptance requires impact-scoped re-projection, visible correction/conflict, rejected whitespace discard, and retained receipt/justification/timestamp. |

**Fail-closed rule:** Any unresolved HIGH-severity finding blocks this plan and every dependent plan/wave. It cannot be implicitly accepted or deferred; execution resumes only after the cited task `<verify>` succeeds and every cited `<acceptance_criteria>` item is evidenced.

## Verification

- Focused client, queue, and navigation tests cover transport, correction, conflict, replay, and discard.
</threat_model>

<verification>
- [ ] Both focused mobile commands pass.
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- Only real transport failure creates local pending.
- Retry is idempotent and central success appears only after confirmed/replayed acknowledgement.
- Conflict/discard cannot silently rewrite or erase physical sales-area truth.
</success_criteria>

<output>After execution, create `.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-05-SUMMARY.md`.</output>
