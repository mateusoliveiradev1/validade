---
phase: 19-integracao-do-controle-gpp-com-hoje
plan: "06"
type: execute
wave: 6
depends_on: ["19-05"]
files_modified:
  - apps/mobile/src/capture/LinkedGppRemovalFlow.tsx
  - apps/mobile/src/capture/TaskResolutionPanel.tsx
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/task-resolution.test.tsx
  - apps/mobile/src/capture/mobile-capture.accessibility.test.tsx
autonomous: true
requirements: ["GPP-09"]
must_haves:
  truths:
    - "D-01/D-02/D-03: GPP choices appear only after removal confirmation; `Confirmar esgotado` stays separate and creates zero GPP records."
    - "D-04/D-10: Review locks product/lot identity and shows code, quantity/unit, destination/finality, actor, and consequences."
    - "D-05/D-06/D-07: Exact physical confirmation resolves only removed quantity and warns that partial remainder stays active."
    - "D-11/D-12: Above-pending quantity routes to reconference; missing code shows `Falta informar o código`, never local pending."
    - "D-09/D-19: Receipt labels `Área de venda` and `Envio GPP` separately; success copy requires confirmed/replayed."
    - "D-03/D-09: Confirming physical removal immediately persists `awaiting_destination`; leaving before destination or before send resumes actionable GPP work."
  artifacts:
    - path: "apps/mobile/src/capture/LinkedGppRemovalFlow.tsx"
      provides: "Approved progressive linked removal, review, and receipt UI"
    - path: "apps/mobile/src/capture/TaskResolutionPanel.tsx"
      provides: "Terminal flow owner preserving inherited evidence and sold-out path"
    - path: "apps/mobile/src/capture/mobile-capture.accessibility.test.tsx"
      provides: "48dp, accessible state/order, wrapping, and reduced-motion assertions"
  key_links:
    - from: "apps/mobile/src/capture/TaskResolutionPanel.tsx"
      to: "apps/mobile/src/capture/LinkedGppRemovalFlow.tsx"
      via: "eligible found-product branch after inherited evidence"
      pattern: "Confirmo que a quantidade informada saiu da área de venda"
    - from: "apps/mobile/src/capture/CaptureApp.tsx"
      to: "apps/mobile/src/capture/linked-gpp-repository.ts"
      via: "persist physical receipt before linked GPP submission"
      pattern: "confirmLinkedRemoval"
---

<objective>
Implement the approved one-handed terminal interaction and two-truth receipt.

Purpose: Operators must explicitly remove product, review locked provenance, and understand physical versus GPP delivery state without weakening evidence or sold-out semantics.
Output: Focused linked-flow component, terminal/CaptureApp wiring, renderer and accessibility coverage.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-05-SUMMARY.md
@apps/mobile/src/capture/TaskResolutionPanel.tsx
@apps/mobile/src/capture/GppAvariaFlow.tsx
@apps/mobile/src/capture/capture-ui.tsx
@apps/mobile/src/capture/capture-theme.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-06-01: Add the progressive physical-removal and destination sequence</name>
  <files>apps/mobile/src/capture/LinkedGppRemovalFlow.tsx, apps/mobile/src/capture/TaskResolutionPanel.tsx, apps/mobile/src/capture/task-resolution.test.tsx, apps/mobile/src/capture/mobile-capture.accessibility.test.tsx</files>
  <read_first>apps/mobile/src/capture/LinkedGppRemovalFlow.tsx, apps/mobile/src/capture/TaskResolutionPanel.tsx, apps/mobile/src/capture/task-resolution.test.tsx, apps/mobile/src/capture/mobile-capture.accessibility.test.tsx, apps/mobile/src/capture/GppAvariaFlow.tsx, apps/mobile/src/capture/capture-ui.tsx, apps/mobile/src/capture/capture-theme.ts, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md</read_first>
  <behavior>
    - Before confirmation, no GPP destination is visible.
    - Exact confirmation first records the physical receipt as `awaiting_destination`, then reveals only Registrar avaria, reaproveitamento, and produção interna.
    - `Confirmar esgotado` is a separate consequential action and makes zero GPP calls.
    - Partial quantity shows the exact remaining-risk warning.
    - Above-pending and missing code/unit block submit with recovery action.
  </behavior>
  <action>Write renderer/accessibility cases first. Extract `LinkedGppRemovalFlow` so `TaskResolutionPanel` remains route owner. Preserve inherited evidence/reconference rules. Render locked product/lot identity as read-only label/value content, numeric removed quantity with visible inherited unit, and the exact `SelectionRow` confirmation `Confirmo que a quantidade informada saiu da área de venda`. Treat confirmation as an async durable command: keep destinations unavailable until `confirmLinkedRemoval` returns the persisted `awaiting_destination` receipt, then reveal exactly three singular destination rows: `Registrar avaria`, `Enviar para reaproveitamento`, `Enviar para produção interna`; exclude `transferencia`. Keep `Confirmar esgotado` visually separate, explain that it creates no GPP record, and use its existing terminal confirmation. Above-pending routes to explicit reconference with retained history; missing code/unit offers scan/manual confirmation and does not block recording physical removal. Use only existing capture UI/theme primitives, one primary action per step, 48dp targets, wrapping labels, visible focus/accessibility state, and no decorative/new animation or dependency.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/task-resolution.test.tsx apps/mobile/src/capture/mobile-capture.accessibility.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UI-SPEC scenarios 1-4 have renderer assertions, including zero GPP calls for sold-out.
    - Destination rows cannot render before exact physical confirmation and expose no transfer option.
    - Renderer order asserts `confirmLinkedRemoval` resolves with `awaiting_destination` before destination rows become enabled; failed persistence leaves the risk unresolved and destinations unavailable.
    - Existing evidence and reconference controls remain mandatory.
    - Partial warning is exactly `A quantidade restante continuará como risco ativo em Hoje.`
    - All interactive targets assert at least 48dp; TalkBack order/state and narrow wrapping are tested.
  </acceptance_criteria>
  <done>D-01 through D-03, D-06, D-07, D-11, and D-12 are visible and testable in terminal order.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 19-06-02: Wire locked review, submit, and separate physical/GPP receipt</name>
  <files>apps/mobile/src/capture/LinkedGppRemovalFlow.tsx, apps/mobile/src/capture/TaskResolutionPanel.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/task-resolution.test.tsx, apps/mobile/src/capture/mobile-capture.accessibility.test.tsx</files>
  <read_first>apps/mobile/src/capture/LinkedGppRemovalFlow.tsx, apps/mobile/src/capture/TaskResolutionPanel.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/linked-gpp-state.ts, apps/mobile/src/capture/linked-gpp-repository.ts, apps/mobile/src/capture/gpp-client.ts, apps/mobile/src/capture/task-resolution.test.tsx, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md</read_first>
  <behavior>
    - Review shows product, code, lot, quantity/unit, destination/finality, actor, and both resulting truths without a modal.
    - Physical receipt persists before delivery call.
    - Missing code is `Falta informar o código`; transport pending is `Pendente neste aparelho`.
    - Confirmation/replay are the only `Registrado na central`/replayed success states.
    - Structured physical-impact conflict renders separate review-required physical state and GPP conflict.
    - Navigation/process restart immediately after physical confirmation resumes destination selection from `awaiting_destination`.
    - Navigation/process restart after destination review but before send resumes the same locked review from `ready`, with no transport attempt or outbox row.
  </behavior>
  <action>Wire `CaptureApp` callbacks so `confirmLinkedRemoval` completes and returns durable `awaiting_destination` before destination selection, request construction, or send. Persist `selectLinkedDestination` before showing inline locked review; when valid, the durable pre-send state is `ready`. Add CTA `Revisar envio ao GPP`, then `Enviar registro ao GPP`; lock product/lot and prefilled valid code. On route entry and app restart, query actionable linked receipts: deep-link `awaiting_destination` back to destination selection and `ready` back to the exact preserved review, without a transport attempt. Render immediate receipt as two label/value facts: `Área de venda` with `Retirada confirmada`, `Retirada parcial`, or `Revisão física necessária`; `Envio GPP` with `Escolher destino GPP`, `Revisar e enviar ao GPP`, missing code/unit, sending, local pending, correction, conflict, central confirmed, or replayed state. Use exact approved copy (`Falta informar o código`, `Corrigir envio`, `Pendente neste aparelho`, `Registrado na central`, `Registro já confirmado na central`) and `StatusNotice` semantics. Keep form/identity visible while sending, disable duplicate submit, announce critical states as alerts and ordinary changes politely, and make reduced-motion behavior independent of animation.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/task-resolution.test.tsx apps/mobile/src/capture/mobile-capture.accessibility.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UI-SPEC scenarios 5-9 and 11-12 have renderer/static assertions.
    - Review exposes every D-04 datum inline and product/lot cannot be edited.
    - Test order proves physical receipt persistence occurs before any client send.
    - Integration tests unmount/remount after physical confirmation and before destination, then after destination review and before send; both restore the correct step and preserved provenance while asserting zero GPP client calls/outbox rows.
    - Missing-code, transport, correction, conflict, confirmed, and replayed states use distinct exact labels.
    - No success copy appears for local persistence, transport attempt, rejection, or conflict.
    - No modal-first review, new registry/library, decorative motion, or full-screen spinner is added.
  </acceptance_criteria>
  <done>The terminal flow communicates physical and GPP truth separately through every outcome.</done>
</task>

</tasks>

<threat_model>
## ASVS Baseline

- Checklist: OWASP ASVS 5.0.0, Level 1. The repository pins `security_asvs_level: 1` but no version; Phase 19 uses the current canonical 5.0.0 checklist from the [official release](https://github.com/OWASP/ASVS/releases/tag/latest) and its [versioned requirements CSV](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.csv).

## Assets

- Operator intent, inherited evidence, locked lot/product identity, physical confirmation, and truthful receipt.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human confirmation to repository mutation | Accidental or ambiguous input must not resolve physical risk. |
| Repository receipt to network submission | Physical persistence must occur first and remain visible independently. |
| Outcome state to accessible UI | Copy/semantics must not misrepresent local, transport, or central success. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS 5.0.0 L1 controls | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------------------|-------------|-----------------|
| T-19-06-01 | Spoofing / Tampering | terminal confirmation/review | HIGH | V2.2.1, V2.3.1 | mitigate | Require exact explicit selection, locked provenance, evidence preservation, and quantity validation in the ordered terminal flow. Evidence binding: Task 19-06-01 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/task-resolution.test.tsx apps/mobile/src/capture/mobile-capture.accessibility.test.tsx`; acceptance requires post-confirmation-only destinations, zero-GPP sold-out, mandatory evidence/reconference, exact partial warning, and accessible controls. |
| T-19-06-02 | Repudiation / Integrity | review/receipt interruption | HIGH | V2.1.1, V2.3.1 | mitigate | Display actor, task/lot, quantity, destination, and separate labeled outcomes; persist `awaiting_destination` before navigation and `ready` before send. Evidence binding: Task 19-06-02 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/task-resolution.test.tsx apps/mobile/src/capture/mobile-capture.accessibility.test.tsx`; acceptance requires all review data, immutable product/lot, receipt-before-send order, distinct delivery labels, interruption/remount recovery before destination and before send with zero client/outbox calls, and no premature success copy. |
| T-19-06-03 | Information disclosure / Integrity | status/error UI | MEDIUM | V2.2.1, V15.3.1 | mitigate | Render only required public-safe typed copy, no raw server details, accessible alerts, and success only for confirmed/replayed. |

**Fail-closed rule:** Any unresolved HIGH-severity finding blocks this plan and every dependent plan/wave. It cannot be implicitly accepted or deferred; execution resumes only after the cited task `<verify>` succeeds and every cited `<acceptance_criteria>` item is evidenced.

## Verification

- Renderer/accessibility tests cover scenarios 1-9 and automated portions of 11-12.
</threat_model>

<verification>
- [ ] Focused task-resolution/accessibility command passes after each task.
- [ ] `cmd /c pnpm.cmd --filter @validade-zero/mobile typecheck`
</verification>

<success_criteria>
- The operator cannot choose a GPP destination before explicit physical confirmation.
- Sold-out remains separate and creates no GPP record.
- Review and receipt preserve locked provenance, partial risk, accessibility, and two-truth copy.
</success_criteria>

<output>After execution, create `.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-06-SUMMARY.md`.</output>
