---
phase: 19-integracao-do-controle-gpp-com-hoje
plan: "07"
type: execute
wave: 7
depends_on: ["19-06"]
files_modified:
  - apps/mobile/src/capture/TodayScreen.tsx
  - apps/mobile/src/capture/CaptureApp.tsx
  - apps/mobile/src/capture/today-screen.test.tsx
  - apps/mobile/src/capture/mobile-gpp-navigation.test.tsx
  - .maestro/phase19-linked-gpp-central.yaml
  - .maestro/phase19-linked-gpp-offline-retry.yaml
  - .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
  - .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-TESTING.md
  - .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UAT.md
autonomous: true
requirements: ["GPP-09"]
must_haves:
  truths:
    - "D-05/D-07/D-08/D-09: Hoje counts only physical active risk; linked follow-up never disguises a resolved or review-required quantity."
    - "D-12/D-16/D-17/D-18: Compact follow-up routes missing-code, correction/conflict, pending retry, and justified discard without duplicating Phase 20."
    - "D-20: Internal purchases remain separate and cannot resolve or hide Hoje risk."
    - "D-21/D-22: No full central queue or realtime hints are introduced; existing refresh/fallback remains."
    - "D-23: No Expo/build identifier, APK, push, deployment, or false native-proof claim is produced."
    - "D-03/D-09: `awaiting_destination` and `ready` remain actionable in Hoje and deep-link to the exact interrupted step."
    - "D-18: `Sincronizar pendências GPP` renders only when at least one genuine `pending_retry` transport record exists."
  artifacts:
    - path: "apps/mobile/src/capture/TodayScreen.tsx"
      provides: "Compact `Pendências GPP após retirada` projection separate from active risk"
    - path: ".planning/phases/19-integracao-do-controle-gpp-com-hoje/19-TESTING.md"
      provides: "Actual focused/full automated evidence"
    - path: ".planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UAT.md"
      provides: "Public-safe later native scenario checklist with pending proof truth"
  key_links:
    - from: "apps/mobile/src/capture/TodayScreen.tsx"
      to: "apps/mobile/src/capture/CaptureApp.tsx"
      via: "repository-derived linked follow-up and deep-link/sync callbacks"
      pattern: "Pendências GPP após retirada"
    - from: ".planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md"
      to: ".planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UAT.md"
      via: "automated scenario matrix separated from later native-only proof"
      pattern: "GPP-09"
---

<objective>
Expose actionable linked follow-up in Hoje and close Phase 19 with honest evidence.

Purpose: Removed items may still need GPP work, but that follow-up must not inflate or hide physical risk, become a Phase 20 queue, or claim an unperformed native release.
Output: Compact Hoje follow-up/deep links, regression matrix, testing/UAT evidence, and full repository gate.
</objective>

<execution_context>
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Liiiraa/Documents/estudos/validade/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-PATTERNS.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md
@.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-06-SUMMARY.md
@apps/mobile/src/capture/TodayScreen.tsx
@apps/mobile/src/capture/CaptureApp.tsx
@apps/mobile/src/capture/ControleGppScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 19-07-01: Add compact post-removal GPP follow-up without changing active-risk counts</name>
  <files>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx</files>
  <read_first>apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/CaptureApp.tsx, apps/mobile/src/capture/today-screen.test.tsx, apps/mobile/src/capture/mobile-gpp-navigation.test.tsx, apps/mobile/src/capture/linked-gpp-repository.ts, apps/mobile/src/capture/GppPendingScreen.tsx, apps/mobile/src/capture/today-copy.ts, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md</read_first>
  <behavior>
    - Actionable linked follow-up appears under `Pendências GPP após retirada` and does not increment active-task/risk counts.
    - Physical-review items sort before awaiting destination/review, missing code/unit, correction/conflict, and transport pending.
    - Success-only history is omitted; no follow-up section appears when none is actionable.
    - `awaiting_destination` deep-links to destination selection; `ready` deep-links to preserved review; other rows deep-link to preserved correction/retry context.
    - Section sync uses the existing helper and renders only when at least one `pending_retry` record exists.
    - Internal purchase never appears and cannot resolve Hoje.
  </behavior>
  <action>Write Today/navigation regressions first. Project only actionable linked states (`review_required`, `awaiting_destination`, `ready`, `awaiting_code`, `awaiting_unit`, `correction_required`, `conflict`, `pending_retry`) into a compact `Pendências GPP após retirada` section after safety/refresh status and before active task sections. Sort by physical implication, interrupted destination/review work, missing data, correction/conflict, then transport pending. Use existing bordered row rhythm and 48dp target without active-risk card styling; exclude confirmed/replayed/discarded success-only history and do not render an empty section. Render the one section-level `Sincronizar pendências GPP` action using Plan 19-05 helper only when at least one projected record has state exactly `pending_retry`; `awaiting_destination`, `ready`, `awaiting_code`, `awaiting_unit`, `correction_required`, or `conflict` alone must never expose it. Deep-link `awaiting_destination` to destination selection, `ready` to locked review, missing-data/correction to the preserved linked draft, and pending/conflict to the existing Phase 18 pending surface. Compute active counts solely from physical task projection; internal purchase and Phase 20 central response actions never enter this section.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/today-screen.test.tsx apps/mobile/src/capture/mobile-gpp-navigation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - UI-SPEC scenario 10 and Hoje route/count behavior have automated assertions.
    - A removed/pending GPP item is visible as follow-up but absent from active-risk count.
    - `review_required` restores only affected quantity to physical active risk.
    - Sorting, exact heading/sync copy, deep links, and 48dp rows are asserted.
    - Positive renderer assertion shows sync for `pending_retry`; negative assertions hide it for each isolated `awaiting_destination`, `ready`, `awaiting_code`, `awaiting_unit`, `correction_required`, and `conflict` case.
    - Navigation tests restore destination selection from `awaiting_destination` and preserved review from `ready` after remount/restart, with zero implicit send/outbox calls.
    - Internal purchases, confirmed history, Phase 20 response actions, and realtime hints are absent.
  </acceptance_criteria>
  <done>Hoje keeps physical risk and post-removal GPP work visible as separate operational categories.</done>
</task>

<task type="auto">
  <name>Task 19-07-02: Create executable Maestro coverage for central and offline-retry essential flows</name>
  <files>.maestro/phase19-linked-gpp-central.yaml, .maestro/phase19-linked-gpp-offline-retry.yaml</files>
  <read_first>.maestro/smoke.yaml, .maestro/v1-readiness.yaml, package.json, apps/mobile/src/capture/LinkedGppRemovalFlow.tsx, apps/mobile/src/capture/TodayScreen.tsx, apps/mobile/src/capture/TaskResolutionPanel.tsx, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md</read_first>
  <action>Create two real Maestro 2.6.1 flows using `appId: com.validadezero.app`, deterministic public-safe test-fixture environment variables, visible labels/accessibility IDs, assertions, and no placeholder-only steps. The central flow must cover Hoje task entry, physical confirmation, durable destination selection, locked review, send, `Registrado na central`, return to Hoje, and separation from active-risk count. The offline/retry flow must cover interruption after physical confirmation before destination, app stop/relaunch, Hoje `awaiting_destination` follow-up/deep-link, interruption after destination review before send, stop/relaunch to the preserved `ready` review, an actual airplane-mode transport failure after send, `Pendente neste aparelho`, airplane mode off, `Sincronizar pendências GPP`, central confirmation/replay, and no duplicate. Preserve D-23: create/format the specifications now, but do not build, install, launch, push, or deploy an APK in this phase. Document the later native commands in the files' comments and 19-TESTING.md using `maestro test .maestro/phase19-linked-gpp-central.yaml` and `maestro test .maestro/phase19-linked-gpp-offline-retry.yaml`; execution requires an intentionally approved build containing Phase 19 plus the sanitized fixture.</action>
  <verify>
    <automated>cmd /c pnpm.cmd exec prettier --check .maestro/phase19-linked-gpp-central.yaml .maestro/phase19-linked-gpp-offline-retry.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Both YAML files parse/format through the non-watch Prettier command and contain executable commands rather than commented placeholders.
    - The central flow asserts Hoje → physical receipt → linked GPP → central-confirmed outcome and physical/count separation.
    - The offline flow asserts both interruption boundaries, `awaiting_destination` and `ready` deep-link recovery, genuine offline `pending_retry`, manual retry, acknowledgement/replay, and no duplicate.
    - Neither flow treats `awaiting_destination`, `ready`, missing code/unit, correction, or conflict as transport pending.
    - No APK/build/install/run/push/deploy action occurs; native result remains pending until an intentionally approved Phase 19 build exists.
  </acceptance_criteria>
  <done>Executable native E2E artifacts exist, while the native execution gate remains explicitly unperformed and blocking.</done>
</task>

<task type="auto">
  <name>Task 19-07-03: Run the mandatory scenario/regression matrix and record truthful evidence</name>
  <files>.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-TESTING.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UAT.md</files>
  <read_first>.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-VALIDATION.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-TESTING.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UAT.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-UI-SPEC.md, .planning/phases/19-integracao-do-controle-gpp-com-hoje/19-CONTEXT.md, .planning/phases/18-controle-gpp-mobile-para-avaria-e-compras-internas/18-HUMAN-UAT.md, apps/mobile/app.json, apps/mobile/package.json, package.json</read_first>
  <action>Run every focused command recorded in `19-VALIDATION.md`, including the targeted Stryker command for `packages/domain/src/linked-gpp-removal.ts`, then `cmd /c pnpm.cmd check` and `cmd /c pnpm.cmd security:evidence`. Update task statuses with actual results and create `19-TESTING.md` mapping automated UI-SPEC scenarios, interruption/restart cases, renderer sync positive/negative cases, mutation score/survivor review, and the two Maestro specifications to commands. Create public-safe `19-UAT.md` for the later intentionally approved Android build: all scenarios, both Maestro flows, real airplane-mode/restart/retry/no-duplicate proof, denied-camera/manual-code recovery, one-handed use, TalkBack order, reduced motion, and partial quantity. Mark native items pending unless actually performed. Record that Phase 18 standalone avaria/purchase still works, internal purchase cannot resolve Hoje, transfer is absent from linked choices, existing refresh fallback remains, and no app/build identifiers changed. Do not build/install an APK, push, deploy, add Phase 20/21 behavior, or claim renderer tests as native proof. Do not mark GPP-09 or the phase fully complete while either Maestro command lacks passed evidence on an intentionally approved Phase 19 build; record the phase as blocked at the native E2E gate even when repository gates pass.</action>
  <verify>
    <automated>cmd /c pnpm.cmd check &amp;&amp; pnpm.cmd security:evidence</automated>
  </verify>
  <acceptance_criteria>
    - `19-VALIDATION.md` contains real pass/fail/blocked evidence for every final task ID and focused command.
    - `19-TESTING.md` maps UI-SPEC scenarios 1-10, repository/API security, adapter parity, accessibility/layout, and Phase 18 regressions.
    - `19-TESTING.md` records the targeted Stryker result at the scoped 100% threshold or zero unreviewed survivors.
    - `19-TESTING.md` maps both Maestro files and later exact native commands; `19-UAT.md` leaves them pending until run on an approved Phase 19 build.
    - `19-UAT.md` lists all later native checks and leaves unperformed checks pending.
    - Full check and security evidence pass, or the phase remains honestly blocked with the failing command/output summarized.
    - Diff audit confirms no Expo/build identifier, APK, push/deploy config, new dependency, internal-purchase integration, Phase 20 queue, or Phase 21 realtime implementation.
  </acceptance_criteria>
  <done>Repository evidence is complete, but GPP-09 and Phase 19 remain blocked until both Maestro flows pass on an intentionally approved Phase 19 build.</done>
</task>

</tasks>

<threat_model>
## ASVS Baseline

- Checklist: OWASP ASVS 5.0.0, Level 1. The repository pins `security_asvs_level: 1` but no version; Phase 19 uses the current canonical 5.0.0 checklist from the [official release](https://github.com/OWASP/ASVS/releases/tag/latest) and its [versioned requirements CSV](https://github.com/OWASP/ASVS/blob/v5.0.0/5.0/docs_en/OWASP_Application_Security_Verification_Standard_5.0.0_en.csv).

## Assets

- Active sales-area risk counts, actionable GPP follow-up, regression evidence, and public-safe release truth.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linked repository projection to Hoje counts | Delivery follow-up must not be counted as physical risk unless typed impact reprojects quantity. |
| Follow-up row to correction/retry surface | Deep links must preserve store/task/lot context and not expose Phase 20 actions. |
| Automated evidence to native/release claim | Renderer/repository tests do not prove future Android behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | ASVS 5.0.0 L1 controls | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------------------|-------------|-----------------|
| T-19-07-01 | Tampering | Today active-risk/follow-up projection | HIGH | V2.2.1, V2.3.1 | mitigate | Separate selectors, consume structured physical impact, and exclude internal-purchase projection. Evidence binding: Task 19-07-01 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/today-screen.test.tsx apps/mobile/src/capture/mobile-gpp-navigation.test.tsx`; acceptance requires follow-up/count separation, affected-quantity-only review, deterministic sorting/deep links, and scope exclusions. |
| T-19-07-02 | Elevation of privilege / Information disclosure | follow-up deep links | HIGH | V8.2.1, V8.2.2, V8.3.1, V15.3.1 | mitigate | Reuse capability-gated Phase 18 routes, preserve store scope, and render only required public-safe reasons. Evidence binding: Task 19-07-01 `<verify>` runs `cmd /c pnpm.cmd exec vitest run --config vitest.config.ts --project mobile apps/mobile/src/capture/today-screen.test.tsx apps/mobile/src/capture/mobile-gpp-navigation.test.tsx`; acceptance requires preserved deep-link context and absence of internal purchases, Phase 20 actions, confirmed history, and realtime hints. |
| T-19-07-03 | Repudiation | validation/UAT completion | HIGH | V2.3.1, V15.3.1 | mitigate | Record actual commands and pending native gates while prohibiting false deployment/native claims. Evidence binding: Task 19-07-03 `<verify>` runs `cmd /c pnpm.cmd check &amp;&amp; pnpm.cmd security:evidence`; acceptance requires actual evidence for every task, mutation evidence, scenario/security/regression mapping, pending unperformed native checks, an honest blocked state on failure, and a build/deploy scope diff audit. |
| T-19-07-04 | Integrity / Repudiation | interrupted follow-up and native E2E closure | HIGH | V2.3.1 | mitigate | Project `awaiting_destination`/`ready`, scope sync to `pending_retry`, create central/offline Maestro flows, and fail closed until both run on an approved Phase 19 build. Evidence binding: Tasks 19-07-01, 19-07-02, and 19-07-03; acceptance requires renderer positive/negative sync assertions, restart deep links, executable YAML, and passed later-native Maestro evidence before GPP-09 closure. |

**Fail-closed rule:** Any unresolved HIGH-severity finding blocks this plan and every dependent plan/wave. It cannot be implicitly accepted or deferred; execution resumes only after the cited task `<verify>` succeeds and every cited `<acceptance_criteria>` item is evidenced.

## Verification

- Today/navigation tests cover count separation, ordering, deep links, and scope exclusions.
- Full check/security evidence and public-safe UAT record verify the phase boundary.
</threat_model>

<verification>
- [ ] Focused Today/navigation command passes.
- [ ] `cmd /c pnpm.cmd exec prettier --check .maestro/phase19-linked-gpp-central.yaml .maestro/phase19-linked-gpp-offline-retry.yaml`
- [ ] `cmd /c pnpm.cmd exec stryker run stryker.config.json --mutate packages/domain/src/linked-gpp-removal.ts --thresholds.break 100 --reporters clear-text`
- [ ] All final task rows in `19-VALIDATION.md` have actual evidence.
- [ ] `cmd /c pnpm.cmd check`
- [ ] `cmd /c pnpm.cmd security:evidence`
</verification>

<success_criteria>
- Hoje keeps linked GPP follow-up actionable without hiding or inflating sales-area risk.
- Internal purchases, Phase 20 queue actions, Phase 21 realtime, and release/build work remain out of scope.
- GPP-09 has automated coverage for scenarios 1-10 and explicit later native proof for scenarios 11-12 plus airplane-mode/restart.
- GPP-09 and Phase 19 stay blocked until both Phase 19 Maestro flows pass on an intentionally approved build; repository-only evidence cannot close the phase.
</success_criteria>

<output>After execution, create `.planning/phases/19-integracao-do-controle-gpp-com-hoje/19-07-SUMMARY.md` and proceed to `$gsd-verify-work 19` only when automated gates justify it.</output>
