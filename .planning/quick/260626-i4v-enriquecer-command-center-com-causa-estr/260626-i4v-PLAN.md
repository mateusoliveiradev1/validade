---
status: completed
created: 2026-06-26
---

# Quick Task 260626-i4v: Enriquecer Command Center com causa estruturada

## Objetivo

Dar ao painel admin dados suficientes para explicar "por que venceu" sem depender apenas de texto solto:

- causa estruturada por lote critico;
- evento de auditoria/sync que originou o bloqueio;
- responsavel e momento da acao quando disponiveis;
- acao recomendada consistente para vencido, conflito e risco critico.

## Escopo

1. Enriquecer `CommandCenterCriticalLot` no contrato com `cause` estruturada.
2. Popular `cause` no Command Center audit-backed a partir dos metadados de sync/auditoria.
3. Incluir timestamps seguros nos metadados de auditoria de sync.
4. Atualizar o web para usar `cause` primeiro e cair no `reason` legado se necessario.
5. Atualizar fixtures e testes de API/web.

## Fora do escopo

- Migrar banco.
- Adicionar BI historico por periodo.
- Expor vendas, estoque, segredo, URL assinada ou dados reais.
- Resolver login publicado neste passo; o login entra na verificacao/deploy final.

## Verificacao esperada

- `pnpm.cmd --filter @validade-zero/contracts test -- command-center`
- `pnpm.cmd --filter @validade-zero/api test -- command-center sync`
- `pnpm.cmd --filter @validade-zero/web test -- command-center`
- `pnpm.cmd --filter @validade-zero/web typecheck`
- `pnpm.cmd --filter @validade-zero/web build`
- `pnpm.cmd performance:budgets`
