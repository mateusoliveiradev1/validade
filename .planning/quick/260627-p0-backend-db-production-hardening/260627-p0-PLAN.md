---
status: blocked
created: 2026-06-27
---

# Quick Task 260627-p0: Backend e banco production-hardening P0

## Objetivo

Endurecer o staging para se comportar como ambiente production-like: sem evidence em memoria por acidente, com health profundo, rate limit persistente e novas garantias de banco.

## Escopo

1. Configurar evidence store do Worker para usar R2 quando `EVIDENCE_STORE_MODE=r2` e bloquear `memory` fora de ambiente local.
2. Adicionar `/health/deep` com verificacao sanitizada de banco, migration/schema esperado, evidence store e auth config.
3. Adicionar migration para tabela de rate limit persistente e constraints/FKs centrais seguras.
4. Trocar o login limiter do Worker para repositorio Neon-backed quando `NEON_DATABASE_URL` existe.
5. Testar e aplicar migration na branch Neon staging.

## Fora do escopo

- Trocar provedor de identidade por OIDC/SSO externo.
- Push real completo com registro persistente de device.
- RLS/app DB role com secret novo, porque exige rotacao operacional fora do repo.
- Reescrever todo o repositorio central para transacoes de ponta a ponta se isso exigir mudanca ampla demais neste slice.

## Verificacao esperada

- `pnpm.cmd --filter @validade-zero/api test -- worker-runtime authentication`
- `pnpm.cmd --filter @validade-zero/database test`
- `pnpm.cmd check`
- Migration aplicada no Neon staging.
- Deploy API staging.

## Resultado em 2026-06-27

- `pnpm.cmd check` passou completo: typecheck, lint, format check, 517 testes, 269 smokes, build, security checks e performance budgets.
- Migration `0008_phase_10_production_hardening.sql` aplicada na branch Neon `staging`.
- Verificacao Neon staging:
  - `auth_login_attempts`: presente.
  - `auth_login_attempts_identifier_attempted_idx`: presente.
  - `auth_login_attempts_attempted_idx`: presente.
  - `central_product_drafts_review_status_check`: atualizado com `validated` e `discarded`.
  - FKs centrais adicionadas: 5.
  - Uniques centrais adicionadas: 3.
- Preflight de dados antes das FKs: 0 drafts/lotes/observacoes/tarefas/conflitos orfaos.
- Deploy API staging ficou bloqueado porque a conta Cloudflare autenticada no `wrangler` ainda nao habilitou R2. A CLI retorna `Please enable R2 through the Cloudflare Dashboard. [code: 10042]`.

## Proximo desbloqueio

1. Fazer login no Cloudflare Dashboard no navegador integrado.
2. Habilitar R2 para a conta Cloudflare usada pelo `wrangler`.
3. Criar/verificar bucket `validade-zero-evidence-staging`.
4. Rodar `pnpm.cmd --filter @validade-zero/api deploy:staging`.
5. Validar `https://validade-zero-api-staging.validadezero.workers.dev/health/deep`.
