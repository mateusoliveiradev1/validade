---
status: complete
created: 2026-06-27
---

# Quick Task 260627-p1: Banco production-ops hardening

## Objetivo

Avancar o staging para um banco mais proximo de producao real, cobrindo tres lacunas levantadas na revisao: categoria como entidade central, limpeza operacional automatizada e backup executavel.

## Escopo

1. Criar tabela `central_categories` com isolamento por loja, perfil de regra e historico minimo.
2. Popular `central_categories` a partir dos produtos existentes e proteger produtos com FK por `(store_id, category_id)`.
3. Fazer o fluxo central de criacao de produto dar upsert na categoria antes do produto.
4. Adicionar manutencao de banco para limpar somente dados tecnicos expirados, sem apagar lotes/produtos/auditoria operacional.
5. Fazer o cron do Worker staging executar manutencao quando `NEON_DATABASE_URL` estiver configurado.
6. Adicionar script de backup de Neon staging com preflight claro.

## Fora do escopo

- R2/evidencia binaria real, bloqueado por cartao/provedor.
- RLS nativo/app DB role e rotacao de segredo, exige decisao operacional de credenciais.
- Arquivamento frio de auditoria/lotes; este slice nao apaga dados de negocio.

## Verificacao esperada

- Testes de schema/repositorio/API cron.
- `pnpm.cmd check`.
- Migration aplicada na branch Neon staging.
- Verificacao do catalogo vivo no Neon staging.

## Resultado em 2026-06-27

- `pnpm.cmd --filter @validade-zero/database test -- schema repositories`: passou com 38 testes.
- `pnpm.cmd --filter @validade-zero/api test -- worker-runtime index`: passou com 74 testes.
- `pnpm.cmd check`: passou completo com 522 testes principais e 272 smokes.
- Migration `0009_phase_10_categories_retention.sql` aplicada no Neon staging.
- Verificacao Neon staging:
  - `central_categories`: 1.
  - `central_products`: 1.
  - `central_lots`: 0.
  - `audit_events`: 49.
  - memberships ativas: 4.
  - FK `central_products_category_fkey`: presente.
  - trigger `audit_events_append_only_guard`: presente.
- Backup/checkpoint Neon criado e verificado:
  - branch: `backup-staging-260627-p1`.
  - branch id: `br-curly-queen-ac86pfhv`.
- Deploy Worker staging concluido.
  - version id: `820046de-80c1-47fe-a0c5-8c7a87e6944b`.
- `/health/deep`: `503 degraded` esperado, com `database.ok=true`, `auth.ok=true`, `evidence.mode=disabled`.
- Manutencao manual contra staging executada:
  - `authLoginAttempts`: 0 removidos.
  - `authSessions`: 0 removidos.
  - `authRecoveryTokens`: 0 removidos.
  - `authInvites`: 0 removidos.

## Limite restante

Este slice ainda nao substitui backup offsite nem RLS nativo. O backup atual e um checkpoint branch no Neon; para producao real, ainda falta automatizar retencao de branches/exports externos e criar app DB role/RLS com segredo separado.
