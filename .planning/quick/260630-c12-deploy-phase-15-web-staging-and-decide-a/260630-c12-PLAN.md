---
quick_id: 260630-c12
status: complete
created: 2026-06-30
---

# Quick Task 260630-c12: Deploy Phase 15 Web/API Staging

## Goal

Deploy the Phase 15-complete staging surfaces needed before installing and validating the new local APK.

## Decision

Deploy both API and web:

- Web changed directly in Phase 15 through Command Center vocabulary and route ownership.
- API must be redeployed because it bundles changed workspace packages (`@validade-zero/contracts` and `@validade-zero/domain`), even though `apps/api/src` runtime files did not change directly.

## Tasks

1. Run focused pre-deploy builds/checks for API and web.
2. Deploy API staging Worker.
3. Deploy web to Vercel production alias used by staging (`https://validade-five.vercel.app`).
4. Validate public API/web health and record deployment evidence.

## Verification

- API build passes before deploy.
- Web build passes before deploy.
- Worker deploy exits successfully and returns the staging URL/version.
- Vercel deploy exits successfully and the public alias is reachable.
- `security:evidence` passes after recording deploy evidence.
