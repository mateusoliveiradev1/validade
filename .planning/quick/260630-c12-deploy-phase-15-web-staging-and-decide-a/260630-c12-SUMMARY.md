---
status: complete
quick_id: 260630-c12
date: 2026-06-30
---

# Quick Task 260630-c12 Summary

**Task:** Deploy Phase 15 web staging and decide API
**Completed:** 2026-06-30
**Status:** Complete

## Result

Deployed both staging surfaces needed for the new Phase 15 APK:

- API staging Worker: `https://validade-zero-api-staging.validadezero.workers.dev`
- API Worker version: `bb2f0b80-90ba-4ef1-aa84-f937b0f549ba`
- Web production alias used for staging: `https://validade-five.vercel.app`
- Vercel deployment: `dpl_7wBJzearn7AoayxZDztLzMDZFE84`
- Vercel deployment URL: `https://validade-elcr0ilj5-mateusoliveiradev1s-projects.vercel.app`

## API Decision

API deploy was required even though `apps/api/src` runtime files did not change directly. The Worker bundles changed workspace packages from Phase 15:

- `@validade-zero/contracts` changed capture/product schemas.
- `@validade-zero/domain` changed product policy and shift-close rules.

Redeploying API keeps the new APK, product classifier, policy-driven lot registration, and safe-close semantics aligned with staging.

## Commands

| Command | Result |
|---------|--------|
| `cmd /c pnpm.cmd --filter @validade-zero/api build` | Pass |
| `cmd /c pnpm.cmd --filter @validade-zero/web build` | Pass |
| `cmd /c pnpm.cmd --filter @validade-zero/api deploy:staging` | Pass: Worker version `bb2f0b80-90ba-4ef1-aa84-f937b0f549ba` |
| `cmd /c pnpm.cmd --filter @validade-zero/api test -- --run apps/api/src/shift-close.test.ts` | Pass: 12 files / 84 tests |
| `cmd /c pnpm.cmd dlx vercel@latest --prod --yes` | Pass: deployment `dpl_7wBJzearn7AoayxZDztLzMDZFE84`, aliased to `https://validade-five.vercel.app` |

## Public Checks

| Check | Result |
|-------|--------|
| `https://validade-zero-api-staging.validadezero.workers.dev/health` | 200 |
| `https://validade-zero-api-staging.validadezero.workers.dev/health/deep` | 503 degraded because `EVIDENCE_STORE_MODE=disabled`; database/auth deep body was not exposed by PowerShell exception stream |
| `https://validade-five.vercel.app` | 200, contains `Validade Zero` |
| `https://validade-five.vercel.app/health` | 200 via API rewrite |
| Live JS bundle check | Contains `leitura central`, `fila local`, `autorizacao do aparelho`, and `revisao de produto` |

## Boundaries

- No database migration was needed for Phase 15; schema drift was already clean.
- No Android install, Maestro, provider push, camera proof, second-device convergence, or physical Loja 18 UAT was run here.
- Evidence storage remains intentionally degraded/disabled in staging until a private storage path is configured.

## Next Step

Install the new local APK `artifacts\validade-zero-staging-0.12.0-132.apk` and start Phase 16 validation with the freshly deployed API and web surfaces.
