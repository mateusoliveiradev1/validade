---
quick_id: 260630-d6v
status: complete
created: 2026-06-30
---

# Quick Task 260630-d6v: Debug Unchanged Pending Central Lot Sync

## Goal

Fix the follow-up issue where the installed APK still appears unchanged: the local sync UI can show queue `0` while a saved lot remains pending central replay, and the rebuilt APK cannot be distinguished from the previous `0.12.0 (132)` build.

## Findings

- The visible sync queue counts only `sync_commands`.
- Locally saved lots use `capture_lots.central_sync_state` values such as `pending_central` or `local`, so they can be invisible to the queue even though they still need central replay.
- The previous APK was rebuilt with the same Android `versionCode` `132`, making it hard to prove the device installed the corrected code.

## Tasks

1. Count pending central/local lots in `listSyncQueue` for memory and SQLite repositories.
2. Make manual sync feedback avoid "Tudo sincronizado" when pending lot replay remains.
3. Bump the staging Android build to `133` with a visible artifact label.
4. Update tests and build metadata.
5. Rebuild and verify the local APK.

## Verification

- Focused mobile tests pass.
- Mobile typecheck passes.
- APK metadata shows versionCode `133`.
- APK checksum recorded in SUMMARY.
