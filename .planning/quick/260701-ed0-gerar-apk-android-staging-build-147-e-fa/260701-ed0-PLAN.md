---
quick_id: 260701-ed0
status: planned
created: 2026-07-01
task: Gerar APK Android staging build 147 e fazer deploy staging da API e web
---

# Quick Task 260701-ed0: Release APK 147 And Deploy Staging

## Goal

Generate an installable Android staging APK for build `147`, deploy the staging API Worker, deploy the web staging alias, and record honest evidence about what was deployed.

## Tasks

### 1. Validate and publish release source

- Confirm pending source changes are the intended build `147` release set.
- Run repository validation before external deployment.
- Commit the pending release changes and synchronize `main` with `origin/main` without force-pushing.
- Push the reconciled branch before deployment when possible.

### 2. Generate Android APK

- Use `scripts/build-mobile-apk.ps1` through `pnpm.cmd build:android:local`.
- Use a fresh short build root/store directory if Windows or Gradle locks interfere.
- Verify the produced APK path, version name, version code, and checksum.

### 3. Deploy staging API and web

- Deploy API through `cmd /c pnpm.cmd --filter @validade-zero/api deploy:staging`.
- Deploy web through Vercel production alias used for staging.
- Validate public API/web health after deployment.

### 4. Record release evidence

- Create `260701-ed0-SUMMARY.md` with APK path, checksum, deployment URLs/ids, checks run, and remaining physical proof gates.
- Update `.planning/STATE.md` quick task table.
- Commit the quick task artifacts.

## Must Haves

- APK points to `https://validade-zero-api-staging.validadezero.workers.dev`.
- APK reports app version `0.12.0`, Android build `147`, and artifact label `uat17-shift-close-alerts-apk-147`.
- API staging deploy succeeds or is explicitly blocked with CLI evidence.
- Web deploy succeeds or is explicitly blocked with CLI evidence.
- No claim is made that APK was installed, push was received, camera proof exists, second device converged, or Loja 18 physical UAT passed.
