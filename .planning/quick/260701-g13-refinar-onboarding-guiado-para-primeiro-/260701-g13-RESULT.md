# Resultado - Primeiro turno assistido

Data: 2026-07-01

## Implementado

- Onboarding mobile virou um gate de primeiro uso antes do Hoje, chamado `Primeiro turno assistido`.
- O cartao permanente do Hoje foi removido.
- Ajustes ganhou entrada para rever `Primeiros passos`.
- Progresso do onboarding e versionado por `subjectId`, `storeId`, `flowId` e `version`.
- Persistencia local no mobile cobre reabertura no mesmo aparelho.
- API central ganhou endpoints para ler/gravar progresso e esconder onboarding quando ja ha fatos operacionais centrais.
- Banco ganhou migration `0016_phase_16_first_turn_onboarding.sql` para `user_onboarding_progress`.
- Build aprovado foi atualizado para `uat19-first-turn-onboarding-apk-149`.

## Validacao

- `pnpm.cmd check` passou completo.
- APK local gerado: `artifacts/validade-zero-staging-0.12.0-149.apk`.
- API staging deployada: `https://validade-zero-api-staging.validadezero.workers.dev`.
- Web publicado com prebuilt no projeto `web`: `https://web-pitc2eam3-mateusoliveiradev1s-projects.vercel.app`.
- Health da API retornou 200.
- Web retornou 200.

## Pendencia externa

- A migration central nao foi aplicada porque o `NEON_DATABASE_URL` local de `apps/api/.dev.vars` falhou com `password authentication failed for user 'neondb_owner'`.
- O Worker foi deployado, mas a gravacao central do onboarding depende da tabela `user_onboarding_progress` existir no Neon staging.
