---
quick_id: 260725-x9l
status: planned
created: 2026-07-25
task: Gerar nova build Android local incluindo a correcao do cadastro de produto
---

# Quick Task 260725-x9l: Build Android staging com correção de cadastro

## Goal

Produzir e validar um APK Android staging local instalável com a correção de GTIN/cadastro de produto já presente no worktree, identificado coerentemente como versão `0.12.0` build `173`, sem incluir segredos nem alterar o alvo da API staging.

## Tasks

### 1. Atualizar a identidade pública do build staging

**Files:**
- `apps/mobile/app.json`
- `apps/mobile/src/build-info.ts`
- `apps/mobile/src/build-info.test.ts`
- `apps/mobile/src/capture/AjustesScreen.tsx`
- `apps/mobile/src/capture/mobile-release-journeys.test.tsx`

**Action:**
- Manter `expo.version` e a versão do pacote mobile em `0.12.0`.
- Incrementar `expo.android.versionCode` e `VALIDADE_ZERO_APPROVED_BUILD` de `172` para `173`.
- Usar uma identidade única e explícita para o hotfix: `product-registration-fix-staging-apk-173` como artifact label e `product-registration-fix-staging-173` como build ref.
- Atualizar fallbacks e expectativas de testes que representam o build aprovado, sem modificar fixtures históricas que exercitam outros builds.
- Preservar integralmente as mudanças não commitadas em `apps/mobile/src/capture/ProductFormScreen.tsx`, `product-lookup.test.tsx`, `product-form-input.ts` e `product-form-input.test.ts`.
- Manter `EXPO_PUBLIC_API_URL=https://validade-zero-api-staging.validadezero.workers.dev` e `VALIDADE_ZERO_APP_ENV=staging`.

**Verify:**
```powershell
rtk rg -n "173|product-registration-fix-staging" apps/mobile/app.json apps/mobile/src/build-info.ts apps/mobile/src/build-info.test.ts apps/mobile/src/capture/AjustesScreen.tsx apps/mobile/src/capture/mobile-release-journeys.test.tsx
rtk git diff --check
rtk git status --short
```

**Done:** Configuração, fallbacks e testes descrevem o mesmo build `0.12.0 (173)`, e o diff ainda contém a correção original de cadastro.

### 2. Validar a correção e a segurança do source set

**Files:**
- `apps/mobile/src/capture/ProductFormScreen.tsx`
- `apps/mobile/src/capture/product-form-input.ts`
- `apps/mobile/src/capture/product-form-input.test.ts`
- `apps/mobile/src/capture/product-lookup.test.tsx`
- arquivos de identidade alterados na tarefa 1

**Action:**
- Executar typecheck e toda a suíte mobile para confirmar o fluxo de GTIN vazio, normalizado e inválido, além das expectativas de build.
- Executar o scanner de segredos antes da cópia/build. Não adicionar `.env`, `.env.*`, `.dev.vars`, logs, credenciais ou dados reais; o script oficial já exclui esses padrões do build root.
- Inspecionar o diff para garantir que somente a correção de cadastro, sua regressão e a identidade do build `173` entram no source set.

**Verify:**
```powershell
rtk pnpm --filter @validade-zero/mobile typecheck
rtk pnpm --filter @validade-zero/mobile test
rtk pnpm security:secrets
rtk git diff --check
rtk git diff -- apps/mobile
```

**Done:** Typecheck, testes mobile, scanner de segredos e diff-check passam sem apagar ou adulterar a correção existente.

### 3. Gerar e validar o APK local

**Files:**
- `scripts/build-mobile-apk.ps1`
- `artifacts/validade-zero-staging-0.12.0-173.apk`
- `.planning/quick/260725-x9l-gerar-nova-build-android-local-incluindo/260725-x9l-SUMMARY.md`

**Action:**
- Executar exclusivamente o script oficial via `build:android:local`, mantendo o build root curto e externo ao repositório e a store pnpm local configurada pelo script.
- Confirmar que o APK final foi copiado para `artifacts/validade-zero-staging-0.12.0-173.apk`.
- Validar package, `versionName`, `versionCode`, assinatura e SHA-256. Registrar no SUMMARY caminho absoluto, tamanho, hash, identidade pública, API staging e resultados das verificações; não afirmar instalação ou UAT físico.

**Verify:**
```powershell
rtk pnpm build:android:local
rtk powershell -NoProfile -Command "Get-Item -LiteralPath '.\artifacts\validade-zero-staging-0.12.0-173.apk' | Select-Object FullName,Length,LastWriteTime"
rtk powershell -NoProfile -Command "Get-FileHash -Algorithm SHA256 -LiteralPath '.\artifacts\validade-zero-staging-0.12.0-173.apk'"
rtk powershell -NoProfile -Command "$sdk = if ($env:ANDROID_HOME) { $env:ANDROID_HOME } else { Join-Path $env:LOCALAPPDATA 'Android\Sdk' }; $aapt = Get-ChildItem -LiteralPath (Join-Path $sdk 'build-tools') -Filter aapt.exe -Recurse | Sort-Object FullName -Descending | Select-Object -First 1; & $aapt.FullName dump badging '.\artifacts\validade-zero-staging-0.12.0-173.apk' | Select-String '^package:'"
rtk powershell -NoProfile -Command "$sdk = if ($env:ANDROID_HOME) { $env:ANDROID_HOME } else { Join-Path $env:LOCALAPPDATA 'Android\Sdk' }; $signer = Get-ChildItem -LiteralPath (Join-Path $sdk 'build-tools') -Filter apksigner.bat -Recurse | Sort-Object FullName -Descending | Select-Object -First 1; & $signer.FullName verify --print-certs '.\artifacts\validade-zero-staging-0.12.0-173.apk'"
```

**Done:** APK existe, tem package `com.validadezero.app`, `versionName=0.12.0`, `versionCode=173`, assinatura válida e SHA-256 registrado, sem alegação de instalação no aparelho.

## Must Haves

- O APK contém as mudanças não commitadas que corrigem o cadastro de produto.
- Toda a identidade pública e nativa converge em `0.12.0 (173)`.
- O APK aponta para a API staging oficial e não contém arquivos de segredo do repositório.
- A entrega termina com um artefato verificável e evidência honesta; instalação/UAT físico permanecem passos separados.
